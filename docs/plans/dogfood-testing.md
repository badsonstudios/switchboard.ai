# Dogfood testing tracker

> **Live state — maintained automatically, without asking** (standing rule in
> `.claude/CLAUDE.md`, added 2026-08-15). Every session updates this file when:
> a user-facing feature merges (add it as UNTESTED), Dan reports a hand-test
> result (move it to TESTED or FOUND-A-BUG with the ticket number), or a fix
> for a found bug ships (move it back to UNTESTED for re-verification).
> When Dan asks "what should I test?", answer FROM this file: the UNTESTED
> and RE-TEST sections, phrased as steps + expected result.

Last updated: 2026-09-01 — **v0.8.7 SHIPPED, and it is the one that unblocks the
backlog on this page.** Three rows sat UNTESTED purely because the code was on
`main` and not in any installed build: **#716's composer half, #724's auto-trust,
and #719's watcher CPU fix**. All three are now installable. **Install v0.8.7
before running any of them.** Order them by feedback speed, not importance:
#724 and #716 answer in minutes; #719's row needs the app left running for a
full working day, so start that one FIRST and let it run underneath the others.
Two of the three carry a caveat worth reading before you judge them — #716 is
deliberately half a fix, and #719 is 2 of 5 bugs.

Previously, 2026-08-31 — **laptop pass on v0.8.6. #729 PR-1 PASSES: the MCP
panel now shows every server on the work laptop** (owner, 2026-08-31) — the
3-vs-16 bug that this whole MCP arc was filed for is fixed and confirmed on the
machine that had it. Moved to the 2026-08-31 pass section below. **Still
untested on that trip: everything you DO to a server** — Turn off / Reconnect /
Load it now (#729 PR-2) and Sign in / Sign out (#734). Those rows stay UNTESTED
and are the next laptop asks.

⚠️ **Also from that trip, and the thing to not misread: composer typing still
feels sluggish on the laptop — but v0.8.6 does NOT contain the #716 fix.** That
fix is in the `0.8.7 — unreleased` CHANGELOG section, along with #724. So the
observation is a **baseline confirmation** (the bug is real and severe on that
machine), NOT a verdict on the fix, and it does not settle whether #740 is
needed. **Nothing about #716 or #724 can be hand-tested until v0.8.7 is cut** —
which is now the blocker on two rows, and an argument for cutting it soon.

Previously: **v0.8.6 SHIPPED** (2026-08-30) carrying **both halves of #729**
plus **#734** (signing in to a connector); v0.8.5 could never have fixed the
short list — what it carried was #723's footer EXPLAINING it, which is why
testing it on 2026-08-29 looked like a broken feature rather than an absent one.
#734's row is the one no machine here could test at all — the OAuth success path
has never run, only its refusals are measured. Previously: v0.8.4 shipped with the MCP Manager (#632),
its add/remove half (#714) and a not-started session's rail row (#687); v0.8.3
carried the eleven-item merge train (#268 #577 #648 #295 #606 #656 #657 #502
#491 #642 #471). Owner statement 2026-08-20: **"we're not using terminals anymore
within the app"** — every Terminal-pane check is marked MOOT below rather than
deleted; they revive if PTY-transport sessions come back into use.

## Untested — never exercised by hand

| What | How to test | Note |
|---|---|---|
| **Click the model name at the bottom of a session to switch it in one click (#747)** | **This is the express lane you asked for, and the thing that pays for the extra click #746 added — judge whether it does.** In a **Direct** session that has replied at least once, click the small grey model name at the bottom, next to the autonomy chip. A menu should open — **upward**, if the card is near the bottom of the window — with a ✓ on the model you are on. Click a different one: it should apply **immediately**, the menu should close, and the name on the button should become the model you picked, with no OK anywhere. **Then the check no machine can make: send a prompt and confirm the reply really comes from that model** — only the real CLI proves `set_model` took effect rather than being merely acknowledged. Second, the placement, which is the reason this does not use the anchoring the ⋯ menu uses (#695): open it on a card in the **top** row of a 2×2 split — the menu should open **downward** and must not cover the button you just clicked — and again on a card at the very bottom, where it should open upward. **Third, the popout, which is the case most likely to be wrong:** pop a session out with **⤢**, click the model name in the popped-out window, and confirm the menu appears **in that window**, anchored to that button, and switches **that** session. Fourth, the fresh card: open a brand-new session that has not replied and confirm the button reads **model?**, opens anyway, ticks nothing, and explains why — then pick one and confirm it takes. Fifth, the one that must NOT be a button: switch a session to **Terminal** mode from **⋯** and confirm the model name there is **plain text**, with a tooltip pointing at `/model` in the Terminal tab. Do the same on a **stopped** session — also plain text, and its tooltip must NOT mention a Terminal tab it never had. Finally the feel: `Esc`, clicking away, `Tab`, and clicking the button a second time should all just close it, and the arrow keys should walk the rows. **Say whether one click with no confirmation is right here, given the picker deliberately asks** | ⚠️ **UNTESTED. NOT RELEASED** — on `main` for 0.8.8; the latest release is v0.8.7, which does NOT contain it. Automated: 32 tests, **all 25 mutations caught**. Pinned — one click is exactly one `set_model` carrying the model's id rather than its display name; clicking the model it already runs sends **nothing** (a no-op switch would make "it did something" indistinguishable from "it didn't"); a refusal **holds the menu open** in the CLI's own words with nothing moved; exactly one row ticks on the default model (`default` and `opus[1m]` share a resolved id — a per-row test lights both); the menu never asks main which model is current, because the button already knows; Terminal mode and a dead session both keep the plain span. ⚠️ **Review caught two blockers before this shipped, and they are what items 3 and 5 are really checking.** First, the button was a way to tear the menu down **while a switch was still on the wire** — reachable by keyboard — which would have lost the CLI's refusal silently and left the session on the old model while you believed it had changed. It now goes dead, visibly, until the answer lands. Second, a session **ending or restarting** under an open menu left the menu floating, aimed at a session id that no longer resolved. **What no test can judge is the feel of one-click-no-confirm, or whether the menu lands in the right place on a real screen** — items 1, 2, 3 and the last one |
| **The model name at the bottom of a session updates the moment you switch (#746 part 1)** | **This is the bug you reported on 2026-09-03.** Open a session that has replied at least once (so it has a model to show — a brand-new card has not told anyone yet, which is by design), note the small grey model name along the bottom, then `/model`, pick a different one, press OK. **The name at the bottom must change immediately** — not on the session's next reply, which is what it used to wait for. Then the second half of the same claim: open a session that has been running a while and confirm the name is there at all, without you switching anything. Third, the one that would be a regression rather than a miss: the **cost** figure and token counts must still behave exactly as before — those are deliberately still fed from the conversation itself, not from the picker, because they are about what actually ran | ⚠️ **UNTESTED. NOT RELEASED** — on `main` for 0.8.8; the latest release is v0.8.7, which does NOT contain it. Automated: the footer moves on the push and does so without a new turn (mutation-verified — putting the old wiring back shows **the stale model winning over the new one**); a card mounting mid-session pulls the model it missed; a session that changes nothing announces nothing (the init arrives every turn, so an unconditional push would be one message and one repaint per turn per session); a refused switch pushes **nothing**; and a usage snapshot can no longer overwrite a model the picker has reported, which is the bug itself. Merged 2026-09-03 as PR #750 (`28ef273`). ⚠️ **Review caught a blocker before this shipped, and it is the thing to check hardest:** the first cut read ONLY the picker's source, which would have made the model name **vanish entirely** on Terminal-mode sessions and on any session that had not replied yet — the picker's source is fed only by Direct-mode traffic. So please confirm the name is present on a **Terminal-mode** session too, and on a **resumed** card the moment it appears |
| **The model picker asks you to confirm — select, then OK (#746 part 2)** | **You asked for this shape; only you can say whether it lands.** `/model`, click a model — it should mark your choice and **nothing should happen yet**. Press **Cancel**: the session must be exactly as it was, no switch, nothing to undo. Do it again and press **Esc**, then again and click outside the dialog — all three must behave identically to Cancel. Then click a model and press **OK**: it switches and the dialog closes. **Judge the trade honestly**: this is one more click than before on a thing you may do often, which is exactly why the footer quick-switch (#747) exists as the express lane — say whether the dialog now feels too slow on its own, and whether closing-on-success is enough confirmation or you want a message first. Also check the small stuff: OK should be dead until you have actually chosen something different, and picking the model the session is already on should leave it dead | ⚠️ **UNTESTED. NOT RELEASED** — on `main` for 0.8.8. Automated: a row click sends nothing (mutation-verified — restoring apply-on-click puts it back on the wire and reddens five tests), OK sends exactly once and sends the model's id rather than its display name, Cancel/Esc/click-away all send nothing, OK is disabled until the choice would change something, a refusal keeps the dialog open in the CLI's own words with the selection intact, and a second press cannot put two switches on the wire. **What no test can judge is whether the extra click is worth it** — that is the whole of item 1 |
| **Opening a session with a long history no longer freezes the app (#742, bug 2 of #719's 5)** | **You need a genuinely big conversation for this — a short one never showed the problem.** Open a session on a folder whose conversation has thousands of messages in it (this repo and BrainHarbor both qualify), and watch the moment it picks the conversation up. Before this, the whole app went unresponsive for about a quarter of a second as it swallowed the file: terminals stopped printing, the window stopped repainting. It should now come up without any stall you can feel. **The regression that would matter most is the one to check hardest: the conversation must still come back COMPLETE and from the beginning.** Scroll right up to the top of that session and confirm the history is all there — that replay is deliberate (it's what fixed resumed cards showing an empty Session view) and this change was only allowed to alter *when* the lines arrive, never which ones. Second, open several big sessions at once, or relaunch with a workspace full of them, and confirm the app stays usable the whole way through rather than freezing once per session. Third, and this one is a judgement call only you can make: a very long history now fills in over a few hundred milliseconds instead of appearing all at once — say whether that reads as the app working or as the app being slow. | ⚠️ **UNTESTED. NOT RELEASED** — merged 2026-09-03 as PR #749 (`958ac42`), in the `0.8.8 — unreleased` section; the latest release is v0.8.7, which does NOT contain it. **Do not run this row until 0.8.8 is cut.** Automated: 12 tests, every one mutation-verified red first, with the defect's own numbers — putting the bug back reads all 200 lines inside a single tick and the event loop gets **zero** turns across the whole replay. Also pinned: the sliced replay produces a byte-identical snapshot to the unsliced one; a multi-byte character (é / 日本語 / 😀) split across a slice boundary is not corrupted; a JSON line split across a boundary parses exactly once; a poll tick landing mid-replay neither duplicates nor skips (removing the guard reads 24 lines where 4 were held); a session that dies mid-replay still gets its last words (removing that reads **6 lines of 100**); and a stopped watcher runs no further slices. **Measured, not estimated: 269.5 ms of frozen main thread on the 33.22 MB / 11,295-line transcript, and that figure excludes the per-line work entirely.** ⚠️ **Known limit, deliberate:** a session that dies *while* its history is still loading still pays the old freeze once, because the alternative is discarding transcript you can never get back |
| **switchboard stops using more and more CPU the longer it's open (#719, bugs 1+3 of 5)** | **This is the row for the incident that cost you a forced reboot.** It cannot be checked in a minute — the whole defect was that cost grew with UPTIME, so the test is *leave it running*. On the work laptop, open your usual set of sessions and **leave switchboard up for a full working day**, ideally longer, then look at its CPU in Task Manager while sessions are idle. It should sit near nothing. **The specific thing to watch for is the shape from the report: CPU that is unremarkable for the first few hours and creeps up over a day**, and the app burning hard while MINIMIZED. Both should be gone. Second check, cheaper: with several sessions actively replying at once, watch CPU during the replies — it should not spike in proportion to how many conversation files are being written. Third, and this is the regression that would matter most: **start a brand-new session and confirm it still finds its conversation as fast as it always did** — that is the thing this change traded against, and if a session now takes visibly longer to come alive, say so immediately. **Two caveats.** This is 2 of the 5 bugs in that report — the other three are filed (#742, #743, #744) and not fixed, so a recurrence is not a surprise and IS worth telling me about. And **the spinning line was never found**: this removes a large amount of measured pointless work, which is not the same as proving it was the cause. | ⚠️ **UNTESTED. RELEASED in v0.8.7** (2026-09-01) — installable now. Automated: the discovery ladder now climbs through an append storm (mutation-verified — putting the defect back gives **40 sweeps per 10 seconds instead of 6**), a busy turn costs a bounded number of sweeps, the conversation-head memo is not re-read every sweep (mutation-verified — **64 quarter-megabyte reads where there should be 0**), and the memo invalidates when a file gains its first identifying line (mutation-verified — keying it by name alone means a session **never binds at all**, which would be a worse bug than the one being fixed). The fail-open fence — a brand-new conversation file is still noticed immediately — is asserted and passed before AND after, deliberately. **What no test can do is run for 26 hours on your laptop**, which is the entire claim |
| **Typing in the prompt box lags less (#716) — HALF a fix** | ⚠️ **EXPECT THIS TO BE BETTER, NOT FIXED, AND SAY WHICH.** Only the composer half shipped; the feed half was built, failed CI and was reverted (see the note), so the honest prediction is: **clearly better on the desktop, possibly still heavy on the laptop in a long session.** Your answer decides whether #716 closes. Open the **longest** session you have — this does nothing measurable on a short one, because the cost it removes was priced by conversation length. Type a couple of sentences at your normal speed and say whether characters appear **as you press them**. Then: (1) with a **long draft** already in the box — paste a paragraph or two so the box is at its twelve-line cap, then keep typing at the bottom of it; (2) while the session is **actively working**; (3) scrolled **up** into the history rather than at the tail. Then confirm the box itself still behaves: it should still grow as you type, still stop at twelve lines and scroll inside itself, still shrink back when you delete, and still give the conversation its room back. **Typing at the bottom of a full box must not jump you to the top of your own draft** — that hazard is now structurally impossible rather than guarded against, so if you see it, something is badly wrong. Finally, on the desktop, compare a long session against a fresh one: typing should now feel the **same** in both, which is the specific thing that changed. | ⚠️ **UNTESTED and deliberately INCOMPLETE. RELEASED in v0.8.7** (2026-09-01) — installable now, and this row is finally answerable. ⚠️ **2026-08-31, owner, work laptop: typing still sluggish — but he was on v0.8.6, which does NOT contain this fix. That is a BASELINE confirmation, not a test of it.** Do not read it as "the fix didn't work"; the fix was not present. **Install v0.8.7 first, then re-run this row** — his answer is what decides whether #716 closes or waits for #740. Automated: the arithmetic that decides where the box stops, the structural guard that typing reads no layout and writes no height (the regression that would bring the lag straight back), and — unchanged on purpose, as the evidence behaviour survived — the real-engine e2e that grows the box with wrapped text, caps it at twelve lines on both branches, shrinks it back and re-fits it on window shrink. **Measured at 400 turns with a long draft: unthrottled, median keystroke latency 80ms → 40ms and the stalls went 58 → 0 — typing now costs what an EMPTY conversation costs. Under a 4× CPU throttle (the laptop proxy) there is NO measured improvement: 152ms/key either way.** The reverted feed half took that to 56ms and 0 stalls, and is what the follow-up item is for. **So the laptop is the real question and only you can answer it** — if it still bursts for you, that is expected rather than a surprise, and it means the follow-up has to land before #716 closes |
| **Auto-trust actually applies now (#724)** | Set the title-bar chip to **🔓 auto-trust** (the default), then start a **Terminal**-mode session in a folder Claude Code has never run in — auto-trust only has a visible effect there, because Direct mode never gets asked the trust question at all. You should go straight to the composer with **no trust prompt**. **The case this fixes needs a lower-case drive letter**, which Windows produces readily: open a folder by dragging it in from Explorer, or via a shell integration, rather than through the folder picker — before this, that wrote the trust flag under `c:/...` while Claude Code read `C:/...`, so the prompt appeared anyway and kept appearing on every later run. Do it twice on the same folder: the second run is where the old bug was worst, because our own phantom entry made switchboard think it had already succeeded. **Then the diagnostic one:** this repo is one of five folders on the dev desktop that already has TWO entries in `~/.claude.json` under different drive-letter cases. Run a Terminal session here and check the log for `folder has more than one entry in the CLI config` — it should appear once, with both spellings named, and both should end up trusted. | ⚠️ **UNTESTED. RELEASED in v0.8.7** (2026-09-01) — on `main` as #738 (`f4ff920`), installable now. v0.8.6 did NOT contain it, which is why this row could not be run before today. Automated: the key lookup (reuse an existing spelling, invent one only when nothing matches, never add a third), the second-run trap, the array/malformed-config guards, and — the one review caught — that two case-different spellings are confirmed by RESOLUTION rather than spelling, because a `//wsl.localhost/...` UNC path is a win32 path over a case-SENSITIVE backend and trusting both would hand the hooks-enabling flag to a directory you never opened. Measured on this machine; the live config already carries such a key. **What no test can do is prove the real `claude` binary skips its trust prompt on your disk** — that is items 1 and 2. |
| **Sign in to an MCP connector, and sign out again (#734)** | ⚠️ **THE WORK LAPTOP IS THE ONLY MACHINE THAT CAN TEST THIS AT ALL.** The dev machine has no claude.ai connectors, so the entire success path was built without ever being seen — only the CLI's *refusals* are measured. On the laptop, open `/mcp` in a **Direct** session and find a connector row. If any row says **needs sign-in**, it should now have a **Sign in** button beside it — before this it said the words and offered nothing. Click it and report what actually happens: does a browser window open? **Then leave the panel open and watch the row for a full minute** — it keeps re-checking for about that long and should update itself when the sign-in lands, without you closing anything. Say whether it actually does. **Three specific outcomes worth telling apart, because we could not:** (a) it works and the row changes — say how long it took; (b) you get *"Claude Code hasn't answered yet…"* after about ten seconds — that is the control channel's timeout, it is EXPECTED if Claude Code waits for the browser flow, and the question is whether the row still updates afterwards; (c) nothing happens at all — a real result, worth a ticket, not you doing it wrong. Next, **Sign out**, which only appears on rows that are already not working (needs-sign-in, not-connecting, unknown) — never on a connected one, deliberately, because we cannot tell an OAuth connector from a server using an API-key header and signing the wrong one out would break it with no proven way back. Confirm it really does clear the sign-in rather than just saying so. Then the negative case, which IS measured: a stdio row (`claude mcp add selftest -- claude mcp serve`, then reopen) must show **neither** button, because the CLI refuses both by type. Finally the clutter judgement: a row can now carry Sign in, Sign out, Turn off and Reconnect at once — say whether that is too many. Clean up with `claude mcp remove selftest -s local` | ⛔ **UNTESTED — and the 2026-08-31 laptop trip did NOT cover it either.** Dan confirmed the panel lists every server; he did not click Sign in or Sign out. Still untestable anywhere but the laptop, and still the only path in the app whose success case has never once run. **RELEASED in v0.8.6** (2026-08-30), as #736. Automated: the transport reader (including the regression that started this — an absent `config` must report `unknown`, never `stdio`, or every connector gets labelled as the one transport that cannot authenticate), the full sign-in/sign-out rule table, both IPC gates on both channels, the request builders, the post-sign-in watch window (mutation-checked — review caught the panel promising a self-updating list it did not actually deliver), and — the one that matters most — an explicit test that `ok: true` is **never** rendered as "Signed in", because that payload has never been observed. **What no test here can do is exercise a single real OAuth flow.** Outcomes (a), (b) and (c) in the how-to-test column are that gap, and (b) in particular is a guess about the CLI's own behaviour that only your laptop can settle |
| **Turn MCP servers off/on and reconnect them without restarting (#729, PR 2 of 2)** | In a **Direct** session open `/mcp`. Every row now has **Turn off** and **Reconnect**. Click **Turn off** on a server you can live without — it should go grey and read *turned off*, and the message must say it stays off **for this project**, NOT "for this session". **Then prove that claim, because it is the one a machine cannot**: close the session, start a new one in the same folder, open `/mcp` — the server must still be off. Turn it back on and confirm it reconnects. Next the add flow end to end: with a session running, run `claude mcp add selftest -- claude mcp serve` in a terminal, reopen `/mcp`, and the server should appear under **In your files, not loaded by this session** with a **Load it now** button — click it and the row should move up into the main list as *connected*, with no restart. Then **Reconnect all** and confirm it reports how many came back. **The judgement call:** with per-row Turn off and Reconnect on every row plus three buttons at the bottom, say whether the row now reads as cluttered — this is the most controls a row has ever carried. Finally, the case I could NOT measure: **try Turn off on a claude.ai connector row** (the laptop has them, this machine has none). Either it works, or the CLI refuses and the panel shows its reason — tell me which, because the manual currently promises neither. Clean up with `claude mcp remove selftest -s local` | ⛔ **STILL UNTESTED — the 2026-08-31 laptop trip did NOT cover this row.** Dan confirmed the panel LISTS every server (#729 PR-1); he did not turn one off, reconnect one, or use Load it now. This is now the **top laptop ask**, because the list being right is exactly what makes the actions worth trusting. **RELEASED in v0.8.6** (2026-08-30) — on `main` as #732 (`ce04bf4`). The hold that kept it off v0.8.5 is over; this is now installable and testable. Automated: the strict-boolean validator (the CLI treats an absent `enabled` as "disable" and answers success — measured), both IPC gates, the control-channel failure vocabulary, the toggle/reconnect wiring, the not-loaded Load-it-now button, and the Reconnect-all partial-failure count. 6579 tests. **What no test can do is prove the toggle survives a restart on your machine, or that it works on a real connector** — items 2 and the last one |
| **The MCP panel shows what the session REALLY has (#729, PR 1 of 2)** — ✅ **core claim PASSED, remainder still untested** | ✅ **2026-08-31, work laptop: the panel shows all the servers now.** The 3-vs-16 defect this arc was filed for is CONFIRMED FIXED on the machine that had it — do not re-run the count. **What is still unchecked on this row, all of it judgement or edge cases:** the scope headings (*Added at startup*, *Built into Claude Code*, *Set by your organisation*) reading sensibly to a human; a connector row showing *read-only* with **no Remove button at all** (not a greyed-out one); the ~2s *connecting…* window settling to *connected* on its own within ~5s without reopening the panel; the three fallbacks naming themselves (suspended card, Terminal-mode session); and Remove still working on a config-file server (`claude mcp add selftest -- claude mcp serve`, remove from the panel, confirm it goes). Original instructions follow. — **This is the row the work laptop exists for** — the machine with 16 servers where the panel showed 3. On it, open `/mcp` in a **Direct** session and count: the list should now match what the CLI's own `/mcp` picker shows, connectors and plugin servers included. Check the grouping reads sensibly with the new headings (*Added at startup*, *Built into Claude Code*, *Set by your organisation*) — **that wording is a guess at what those scopes mean to a human and only you can say whether it lands**. Then the read-only rule: a connector row should show the words *read-only* and **no Remove button at all** — confirm it is not a greyed-out button, because "not right now" and "not ever" are different sentences. Hover it for the explanation. Then the timing, which is the finding nobody predicted: **start a brand-new session and open `/mcp` within about two seconds**. Everything should say *connecting…*, and then settle to *connected* on its own within about five seconds **without you closing and reopening the panel** — judge whether the wait reads as working or as broken. Then the three fallbacks, each of which should name itself at the bottom of the panel: open `/mcp` on a **suspended** card (should say to start the session), and on a **Terminal**-mode session (should say Terminal mode can't be asked, and should NOT tell you to switch to a terminal you are already in). Finally confirm Remove still works on a server that IS in a config file — `claude mcp add selftest -- claude mcp serve`, remove it from the panel, confirm it goes | **RELEASED in v0.8.6** (2026-08-30) — on `main` as #730 (`ed70ef6`). ⚠️ **v0.8.5 did NOT contain it**, which is what cost Dan a laptop trip on 2026-08-29: v0.8.5 carries #723's footer EXPLAINING the short list rather than fixing it, so the released build showed the same three servers and always would. **Install v0.8.6 before testing this row.** Automated: the parser against the real captured cold AND warm payloads, the config join (including the two cases where a name match must be REFUSED so Remove cannot delete the wrong scope), all four `mcp:status` failure reasons, the second folder gate, the settle poll and its bound, and the read-only row. 6540 tests. **What no test can do is show you sixteen real servers** — the fixture has one. Item 1 is that check, and it needs the laptop. Your eye also judges the new scope headings and whether the *connecting…* window reads as patience or as failure |
| **Change a session's model with `/model` (#721)** | In a **Direct** session, type `/model` and press Enter. A picker should open listing the models Claude Code offers — Opus/Sonnet/Haiku and friends, each with its own description. Click **Haiku**: it should tick immediately and say so, with no restart and the conversation intact. Then send a prompt and confirm the reply actually comes from the model you picked — **this is the check no machine can make**, because only the real CLI proves `set_model` took effect rather than merely being acknowledged. Then the case that looks like a bug and is not: open `/model` on a **brand-new** card that has never replied. Nothing should be ticked, and a line should explain that Claude Code only reports its model once a session has answered. Judge whether that line reads as honest or as broken. Then switch a session to **Terminal** mode from **⋯** and type `/model` there — you must get the CLI's own picker, NOT this dialog. Finally check the near-misses reach the CLI untouched: `/model sonnet` and `/model-x` should both be sent as ordinary commands rather than opening anything | **RELEASED in v0.8.5** (2026-08-28). Automated: the correlator (timeouts, refusals, out-of-order replies, the nested request_id trap), the alias-vs-resolved tick matching, the unknown-model state, refusal wording, and the whole path end to end against the fake provider — 39 tests. **What no test covers is the real `claude` binary applying the switch**, which is item 1. Your eye also judges the not-known-yet wording and whether the picker's rows read clearly |
| **The MCP panel says what it does NOT show (#723)** | Open `/mcp` in any session and read the bottom of the panel. Below the buttons there should now be a line saying the list comes from your configuration files, that Claude Code also connects claude.ai account connectors and plugin-supplied servers which cannot be shown yet, and how to see the full list. **Judge the stacking, which is the one thing no test can**: the panel can now show up to four grey footnote blocks at once (health-check-unavailable, a click notice, this one, and the existing add/remove note) — say whether the bottom of the panel now reads as a wall of small print. Then check it in the empty case, which is what the bug was about: open `/mcp` on a folder with no configured servers and confirm you get BOTH "No MCP servers are configured in this session's files" AND the new line — an empty list with no explanation was what made 3-vs-16 look like data loss. Finally confirm the escape route is true for you: the line says to run `/mcp` in a terminal, and in a **Direct** session there isn't one, so check it does not tell you to use a Terminal tab that has nothing in it | **RELEASED in v0.8.5** (2026-08-28). This is a wording/disclosure change only; **the servers themselves are unchanged and your sessions were never missing any of them** — the panel was reading three config files, and connectors and plugin servers live in none. The real fix (reading the session's live list) needs #721. Automated: the note renders with rows, renders in the empty case, and stays away when there is no session — all three mutation-checked. Only your eye judges the small-print weight and whether the wording lands |
| **`/mcp` opens the MCP Manager (#632, PR 1 of 2)** | In a **Direct** session, type `/mcp` in the prompt box and press Enter. The panel should open and the draft should clear — before this, typing it did nothing at all (the CLI answers `/mcp` with a picker in its own terminal, and Direct mode has none). **The check that matters most is the status column**, because the bug review caught was that it could never work on Windows: run `claude mcp add selftest -- claude mcp serve`, reopen the panel, and confirm the row reads **connected** within a few seconds rather than sitting on *status unknown*. Then `claude mcp add broken -- no-such-binary` and confirm that one reads **not connecting** — both rows visible at once, the list appearing instantly and the states filling in after. Then the things only you can judge: switch a session to **Terminal** mode from **⋯** and type `/mcp` there — you must get the CLI's own picker, NOT this panel, because in Terminal mode the real picker works and can do things this pane deliberately cannot. Press Escape and confirm the cursor lands back in the prompt box. Pop a session out, type `/mcp` in it, and confirm the main window comes forward carrying the panel rather than your draft vanishing into nothing. Finally, if you have a repo with its own `.mcp.json`, open a session there and confirm those servers show as *waiting for your approval* rather than as broken. Clean up with `claude mcp remove selftest` / `claude mcp remove broken` | **RELEASED in v0.8.4** (2026-08-27). **Read-only: no add/remove yet (that is #714).** Automated: the three scope readers on real captured file shapes, the health parser on real captured CLI output, the folder gate, the two-call shape, approval-beats-health, secret redaction, and the CLI launch spec on both platform branches — four mutation-checked. **No automated test can prove the launch actually works on your machine, which is exactly how the blocker got in** — item 2 is that check. Your eye also judges whether the three scope headings read clearly and whether *waiting for your approval* explains itself |
| **Add, remove, Reconnect and Reset approvals in the MCP panel (#714, PR 2 of 2)** | Open `/mcp` in a session and click **Add server…**. Add a real one — name `selftest`, kind *A program on this machine*, command `claude`, arguments `mcp` and `serve` on separate lines — and confirm it appears in the list immediately, then reads **connected** when you reopen the panel. **This is the check no machine can make**: the whole family launches the CLI through a hardened Windows path that was rewritten twice in this item, and only a real run proves it. Next, add one with an **Environment variable** (`API_KEY` = anything) and confirm the row says *Carries: API_KEY* and never shows the value anywhere. Try a name with a space, and a name starting with `-`: both should be refused in the form, before anything runs. Try typing a `"` into any box — it should be refused with a sentence explaining why. Then click **Remove** on `selftest`, confirm it asks first, and confirm the row goes. Add a server while a **Terminal**-mode session is open and click **Reconnect**: `/mcp` should be typed into that terminal and the CLI's own picker should open. Do the same in a **Direct** session: nothing should be sent, and it should tell you to restart. Finally, in a repo with its own `.mcp.json`, confirm **Reset approvals** appears (it should be absent otherwise), asks first, and afterwards the session asks about the shared servers again | **RELEASED in v0.8.4** (2026-08-27). Automated: the argv builders against the real CLI's documented forms, validation on both sides, the folder gate on all six channels, the reconnect transport decision, the re-list-after-failure behaviour, and the Windows launcher end-to-end through a real `cmd.exe` shim including eleven recorded injection payloads. **What no test covers is the real `claude` binary on your machine** — item 1. Your eye also judges whether the form's field labels and the "put keys in Environment variables" line actually read clearly |
| **A session that didn't start is still in the Sessions list (#687)** | Start a new session on a folder you can make disappear — a USB drive, or a folder you rename right after opening the card. Open the card so it tries to start and fails: it shows **Session didn't start** with **Try again** and **Close**. Now look at the **Sessions list** — before this the card was simply not there. It should now have a row reading *not started*. Then work through what that buys, because the row is the whole point: click the row (the card comes to the front), press its `Ctrl+1`…`Ctrl+9` number, and **double-click another session's header to maximize it** — the workspace should fold around the not-started card properly instead of ignoring it, and un-maximizing should put everything back. Right-click the row: **Rename** must be visibly greyed out and do nothing when clicked, and the **Move to group** section must be absent entirely (make a group first, or you will not be able to tell an absent section from an empty one). Try to **drag** the row onto a group card — the cursor should show no-drop and nothing should happen. Double-click the row: no rename box may open. Pin it and Close it — both must work. **The one that matters most:** maximize another card so the not-started one collapses away, then bring it back from the list — it must reopen with its overlay and its **Try again** button intact. Finally plug the drive back in (or rename the folder back) and click **Try again**: the row should turn into an ordinary running session, and Rename and Move to group should both come back | **RELEASED in v0.8.4** (2026-08-27). Automated: the store join and its dedupe, the rail row's paint, the two withdrawn actions and both of their second doors (double-click, drag), and the layout plan (maximize now resolves) — each with an explicit pre-fix control, and five of them mutation-checked. **The collapse-and-reopen case is the one review caught**: giving the card a row made it collapsible for the first time, and without the reveal fix it would have been a row you could see, click and never open. Your eye judges whether *not started* reads clearly against *suspended* in the list |
| **REGRESSION CHECK ONLY — right-click menus and the rail edge still behave (#642)** | Nothing should look different in English; this row exists because the arithmetic under **every** pointer-anchored menu was rewritten to be direction-aware, and "identical in English" is the claim worth one human glance. Right-click a session row in the rail: the menu appears at the pointer, arrow keys walk it, Escape closes it and returns focus to the row. Shrink the window to about half your screen height and right-click a row low in the rail — the whole menu, including the bottom **Focus** section, must be on screen. Open it a few times and watch for a flicker in the window's top-left corner before it lands at the pointer (there should be none). Then drag the rail's right edge in and out: it must follow the pointer exactly and survive a restart | **v0.8.3.** Filed **Internal** in the changelog because no right-to-left language ships yet, so there is no new behaviour to see. Automated: 13 unit cases on the placement arithmetic, 5 on the rail-width mirror, 2 e2e (short-window squeeze + a forced `dir="rtl"` window), and the RTL case was mutation-checked (menu lands 726px off with the fix backed out) |
| **A prompt shows what you attached to it (#491)** | In a **Direct** session, paste a screenshot (`Ctrl+V` in the prompt box), type a question beside it and press Enter. The prompt in the conversation should carry a small grey *1 image attached* line where the chips used to be — judge its weight: it is meant to read as a footnote on the prompt, not as another status line. Then the case that used to vanish entirely: paste a screenshot and send it with **nothing typed**. You should get an entry reading just *1 image attached* — before this there was no entry at all and Claude's reply hung under thin air. Then mixed kinds: drag two source files and a picture on in one go, send, expect *1 image and 2 files attached* — **and say whether calling a `.md` a "file" and a `.png` an "image" reads naturally, that is the one wording choice here**. Then attach a picture to a prompt long enough to collapse (500+ characters): the marker must stay visible above the folded header, since that is the case where hiding it would hide the only evidence left. Finally send a few plain prompts and confirm nothing changed for them at all | **v0.8.3.** Automated: the derivation (counts, plurals, attachment-only turns, tool-returned images not miscounted), the wording in every singular/plural combination, survival of a collapsed prompt, and one full e2e round trip pasting a real PNG. Only your eye judges the weight of the line and the file/image wording |
| **Reviewed events are quiet, not faded (#268)** | Open the Events drawer (`Ctrl+E`) with a few sessions in it, and get at least one row into **Ready** (open a session that says *Done.* — that acknowledges it). Compare a Ready row with a live one: it should still read as the log tail rather than the to-do list, but every word on it should be plainly readable, including the small task label under the title and the word **Ready** itself. Do it on **daylight** and on **nordic**, the two where the old treatment measured worst. **This is a look Dan tuned by hand (0.82 opacity, 2026-07-26) and the fix changes it — say if the row no longer recedes enough, or recedes too much.** Three specific things to look at: the title is now the app's secondary grey instead of a faded white, and on a Ready row it is therefore the same colour as the task label under it (only the bold weight separates them); and the two things the old opacity used to fade that now sit at full strength are the 3px coloured session stripe down the left edge and the **Dismiss** chip in the bottom-right corner | **v0.8.3.** Automated: the row's fill and its ink are measured in all four themes by the drift test (4.7-14.2:1, was 3.6-3.7:1 on the label). Only your eye judges whether "already dealt with" still reads at a glance |
| **A stronger frame around session windows and group cards (#648)** | With two or more sessions on screen, look at the border around each session window in the grid and around each group card in the sessions rail — on **nordic** and on **daylight**. It should read as a real boundary against the workspace behind it, against the card's own body, and against the strip along its top (the session tabs in the grid, the group's name row in the rail). This is a bigger nudge than the last two — say if it now reads as a cage rather than an edge. Nothing else about the frame changed (same colour family, same one-edge-everywhere treatment); the question is only whether the new strength looks right or now shouts | **v0.8.3.** Automated: seven surface pairs x four themes in the drift test (nordic's tightest was 2.29:1, now 3.07:1; daylight's 2.66:1, now 3.08:1), plus the running-app check in the rail e2e, whose floor moved 1.55 -> 3. Only your eye judges "right, not shouty" |
| **One focus ring over the whole Events drawer (#577)** | With the drawer SHUT, `Tab` around until focus lands on the **EVENTS** tab on the right edge — it should get switchboard.ai's blue ring, the same one every other control gets, not the browser's default outline. Then open it with `Ctrl+E`: the drawer itself takes focus and now draws the ring around its own edge, where before it drew nothing at all. Then `Tab` through everything inside — the ✕, any **Restore layout** / **Update now** / **Got it** / **Not now** buttons on the notices, then each row's body and its **Dismiss**. Nothing along that walk should draw a differently-coloured outline | **v0.8.3.** Automated: a unit test walks every focusable the open drawer renders and fails any that is not on the ring rule read out of `tokens.css`. Only your eye confirms the ring reads the same on all four themes |
| **Docking a lone session back (#656)** | Make a persistent group in the sidebar (**Create a persistent group**) and start a session inside it. Pop that session out with **⤢** so it is alone in its own window, then dock it back with **⤡**. It must come back to its own spot, still listed under its group heading, still running (no **Resume** button), with no sliver of an empty pane left behind. Then repeat with the session sharing its window with a second one, so both branches of ⤡ are covered — the two should now behave identically | **v0.8.3.** **Note the ticket's premise was measured wrong:** the group was never actually lost on `main` (an unrelated ordering inside dockview was preventing it), so the group half of this is a guard rather than a fix. What genuinely changed is where the card lands. Automated: an e2e drives the round trip and reads the membership back out of the app |
| **A session started in a pop-out never steals a slot (#657)** | Split the grid so two sessions sit side by side (A left, B right). Pop **A** out, then press **＋** inside that window to start **C** in it. Now close **A**'s card from the sidebar (its **✕**), leaving C alone in the window with nobody's slot to inherit. Dock C back with **⤡**: it must arrive as a tab beside **B**, never alone in the left half A used to own, and no 1px sliver of a pane may be left behind. Then do the whole thing again but close the pop-out **from its title bar** instead of ⤡: same placement, except this time C comes back **suspended** with a **Resume** button, which is correct | **v0.8.3.** Automated: both paths are e2e'd, including the suspend. Your eye judges whether the layout looks settled rather than merely correct |
| **Expanding a stacked session comes back full size (#502)** | Get three sessions with two of them sharing one half of the grid. Stack the third and then one of the pair (`Ctrl+Shift+P` → **Stack session with the tabbed sessions**). Pop the remaining card out of that half with **⤢** — the half it leaves is now an empty placeholder. Now expand the stacked session back (**Expand session to its full card**): it must come back at full size in that half. Before this it came back about a pixel wide — in the layout, on the right window, and invisible | **v0.8.3.** Automated: the e2e MEASURES the width, because `toBeVisible()` passes at 1.33px and that is exactly how this class of bug hides |
| **Pop-ups speak the app's language (#471)** | Title bar → click the language chip to **pseudo**. Every label in the window turns into accented look-alikes wrapped in `⟦ ⟧`. Now make a session ask for permission while switchboard is NOT the front window (start something that needs approval, then click away). The desktop pop-up must be in `pseudo` too — the body, and the **Allow**/**Deny** buttons on it — *except* the command itself (`npm run build`) and the session's own name, which must stay exactly as they are. Switch back to **en** without restarting and trigger another one: it must be English immediately. If you have the phone push set up, check the message on the phone matches. Turn **🗣 announce** on and confirm the SPOKEN sentence follows the language too. Windows only: open the Action Center and confirm the pop-up is filed under **switchboard**, not *Electron* — then let a permission pop-up **time out** and press **Allow** on it from the Action Center: the verdict must still reach the session (Windows documents re-activating an expired toast as needing a registered ToastActivatorCLSID, which the installer does not write, so this is the one behaviour the AUMID line may NOT have fixed). Last: `npm run dev` now claims a brand-new notification identity, so confirm dev pop-ups still appear at all | **v0.8.3.** Automated: the locale switch, the ICU plural, the Allow/Deny labels and the untranslated command are all unit-pinned, and an e2e asserts the real bundled main process got its interpolator. What only you can see is a REAL OS toast rendering the accented text (no harness can read one) and the Action Center identity in a packaged build |
| **Turn boundaries you can scan for (#640)** | Open a session with a long conversation in it — several of your prompts, with replies and tool boxes between them. Above every prompt except the first there should now be a full-width rule with **NEW PROMPT** under it and a clear gap above. Scroll fast through the whole session and judge the actual ask: does your eye land on the boundaries without reading? Then switch through all four themes (title bar → theme) and check it reads the same on each — especially **daylight**, where the old line was effectively invisible. Last check, and the one that decides whether it is too loud: get a session to ask you for permission while you look at another one's feed, and confirm the boundary still reads as furniture next to the attention signal, not as competition for it | Superseded: owner hand-tested on v0.8.2 (2026-08-26) — improved but NOT "easily spotted"; follow-up #717 filed. Was: Not yet released — on main after #640. Automated: contrast is measured in all four themes by the drift test (5.20 / 6.00 / 15.57 / 8.76:1, was 1.30:1), and unit tests pin one boundary per turn and none above the first block. Only your eye judges "unmissable at a glance" and whether the **NEW PROMPT** caption earns its place or reads as clutter |
| **Claude's questions, answerable in the card (#563)** | In a **Direct** session ask for something genuinely ambiguous ("I want to add caching — ask me which approach before you start"). A panel appears above the prompt box with the question and clickable answers: round buttons = pick one, boxes = pick several, always an **Other** with a text field. Answer, hit **Send answer**, and Claude should carry on using what you chose — including free text typed into Other. Try the arrow keys/Space instead of the mouse. Half-answer it, go to **Changes**, come back — your ticks should still be there. Unfocused: the pop-up says "A question for you: …" and has **no Allow button**. Note a **Terminal**-mode session keeps its questions in the terminal by design | **v0.7.0.** The fake-provider path is e2e-covered; only the real CLI proves the model actually *reaches for* the tool, and only you can judge whether Other feels like a first-class answer |
| **Several questions arrive as tabs (#566)** | Get a Direct session to ask you **two or more** questions in one go ("ask me three separate questions before you start"). They should arrive as tabs across the top of the question panel, labelled with Claude's own short names, one question showing at a time. Check: every tab carries ✓ or ○ for answered/unanswered; with nothing answered **Send answer** is dead and the panel says "Still to answer: …" naming the ones still empty (it goes live at one answer since #567 — see the row below); **Left/Right** move between tabs and **Up/Down** stay inside the question you are on; half-answer it, go to **Changes** and come back — it should reopen on the question still missing an answer, with your ticks intact. Then ask ONE question and confirm there are no tabs at all | Not yet released — on main after #566. The fake-provider path is e2e-covered; only the real CLI proves a multi-question call happens naturally and that the headers read well as tab names |
| **Answer only SOME of Claude's questions (#567)** | Get a Direct session to ask you **two or more** questions at once, then answer only ONE of them. **Send answer** must go live at that first answer. Before you send, check you can see what you are leaving out: the unanswered tab goes **dashed and struck through**, the question itself (click its tab) says *"Not answered — will be sent as skipped"*, and the line beside the button reads *"Sending now skips: …"* rather than *"Still to answer: …"*. Send it, and watch what Claude does with the gap — it should treat that question as skipped (usually saying so and offering to ask again), never as though you answered it with silence. Then confirm the floor: with NOTHING answered, **Send answer** stays dead — a blank answer is what **Don't answer** is for | Not yet released — on main after #567. The CLI's reading of a partial answer is MEASURED (probe, 2026-08-19, findings §3a) and the partial round trip is e2e-covered against the fake; only the real CLI shows you how Claude phrases the gap, and only you can judge whether the skip warnings read as clear rather than as a telling-off |
| **Feeds restore at the tail (#555)** | Scroll to the end of a busy session, quit switchboard, relaunch. It should come back showing the END of the conversation, not the top. Then drag the panel to another part of the workspace — it should keep its position rather than jumping | **v0.7.0.** Was: every session restored scrolled to the top |
| **Find is a bar first (#557)** | Ctrl+F in a session — you should get the bar with a match count, and Enter / Shift+Enter walking the hits. The results list should stay shut behind its **▸** until you click it | **v0.7.0** |
| **Find in a RESUMED session (#496 + #495)** | Resume a session, send one new turn, then Ctrl+F for something that appears both before and after the resume. Post-resume hits should jump normally; only the older hydrated ones stay unjumpable, and the notice should be about that one hit rather than over the whole session | **v0.7.0.** The ticket's premise was measured wrong first — see PROGRESS |
| **File menu: Open File / Exit (#569)** | A **File** menu at the top left. **Open File…** browses — it should start in the folder of the session you are looking at, and after that wherever you last browsed. The file opens in a document tab BESIDE that session, in its dock section, never inside the session’s own tab strip. Try it with two sessions docked side by side and the SECOND one focused: the document must appear next to that one. Also try it right after typing a prompt (composer focused) — it must still open | **v0.8.0.** |
| **Ctrl+O, and the CLI’s own ctrl+o (#569)** | **Ctrl+O** anywhere in the app opens the file browser — including while typing a prompt. **But click into a session’s Terminal tab and press Ctrl+O there: it must reach Claude Code** (its transcript view), NOT open our dialog. That is the P7 line, and it is the one check no automated test in this repo can make | **TESTED (app half) 2026-08-20:** Dan pressed Ctrl+O in a (Direct) session — dialog opened to that session's folder, as designed. **Terminal-pane half MOOT** while terminals are unused (owner, 2026-08-20); revive with PTY use |
| **No empty window after a restart (#494)** | Open a file in the viewer (the ↗ beside a changed file), pop it into its own window with the ↗ in the document header, then **quit switchboard while that window is still open** and start it again. You should get exactly ONE window back — the document viewer is not restored (by design, for now) and neither is the empty window it was in. Then do it again with a **session** popped out into that same window: that window MUST come back, with its session in it | **0.8.1.** Was: an empty second window could be left on screen, depending on machine timing |
| **Popped-out window comes forward (#571)** | Pop a session out, put another window in front of it, then click that session's row in the sidebar — its window should come to the front. Then click the main switchboard window: your popped-out windows must NOT come forward with it | **v0.8.0** |
| **Changes tab remembers your file (#562)** | Open **Changes** on a session, pick a file, scroll into the diff. Switch to **Session**, switch back — same file, same line. Then commit or discard that file and come back: the tab should open CLEAN, not on a blank comparison | **v0.8.0** |
| **Documents keep their place (#562)** | Open two files in the viewer (the ↗ beside a changed file). Read halfway down one, click the other tab, click back — you should still be halfway down | **v0.8.0** |
| **[UNRESOLVED] Terminal scrollback across a panel move (#562)** | Needs a session with real scrollback: run something long in the **Terminal**, scroll UP into the output, then click that card’s row in the sidebar (with two sessions docked side by side) and see whether the terminal keeps its position. The automated harness could not fill one screen, so this is the one panel measured only halfway | **MOOT** while terminals are unused (owner, 2026-08-20). If PTY sessions return: if it jumps to the bottom it needs the same fix the document viewer got |
| **Right-click menu fits a short window (train fix)** | Shrink the switchboard window to a short height (or use a small screen), right-click a session row near the bottom — the menu should flip above the pointer or clamp to the edge, with every item reachable; a menu taller than the window scrolls | Not yet released — lands with the 2026-08-20 train |
| **A visible ✕ on the Events drawer (#556)** | Open the drawer (tab, `Ctrl+E`, or the palette). There should be a **✕** at the top right of the drawer, beside the word EVENTS — the thing you went hunting for. Click it: the drawer closes and your cursor goes back where it was, exactly as `Esc` does. Then open it again and press **Tab** once — focus should land on that ✕ | Not yet released — on main after #556 |
| **Close all documents + honest tab ✕ (#543)** | Open four or five files in the viewer, then `Ctrl+Shift+P` → **Close all documents (keeps popped-out ones)**. Every document tab should vanish at once, with no confirmation, and your sessions and their **Changes** tabs must be untouched. Then repeat with one document popped out to another monitor (⤢ in its header): that window must survive, and if it is the ONLY document open the palette entry should be greyed out saying "No documents to close". Also hover a document tab's ✕ — the tooltip must read **Close document**, not "Close (ends the session)", and clicking it must not ask you to confirm | Not yet released — on main after #543. Automated tests cover the docked close-all and the three tooltip strings; the popped-out window is unit-pinned only |
| **An attached file survives a tab switch (#546)** | In a **Direct** session, paste a screenshot into the prompt box and type NOTHING. Switch that card to **Terminal**, then back to **Session** — the chip must still be there and the send button still lit. Repeat with a file dragged in from Explorer, and with words typed beside it. Then quit switchboard with a file attached and relaunch: your **words** come back, the chip does **not**, and a line under the box should read "Not restored: <the file name>…". That last part is the point — it must never just empty itself | Not yet released — on main after #546. Automated tests cover the remount and the relaunch notice against the fake provider; only a hand-test proves a real pasted screenshot behaves |
| **Permission-mode hover copy (#534)** | Hover the 🛡 chip in the **title bar**, the 🛡 button **under the prompt box**, and the small mode marker in a **card's header** (visible once a session is on anything but `ask`). Each should pop a description of the mode plus a line saying what that particular control does — and the three must agree. Cycle to **full-auto** and read it: it must say `bypassPermissions` / `--dangerously-skip-permissions`, not imply guardrails. Judge whether the copy reads like something a person would want on hover, or is too long | Not yet released — on main after #534. Unit + component tests already pin that all three surfaces carry the same text and that the full-auto wording matches the CLI; only you can judge readability and hover feel |
| **`ask` mode actually asks (#587)** | Start a session at **ask** and open its **Terminal** tab. The CLI's status line should say **`⏸ manual mode on`** — before this fix a Pro/Max account on CLI 2.1.228+ would have shown **`⏵⏵ auto mode on`** instead, meaning a classifier was reviewing actions rather than you. Then check nothing got taken away: **Shift+Tab** inside that terminal must still cycle to auto. Repeat on a **Direct** session (there is no status line there — the check is only that it still starts, resumes and holds normally). Watch for whether `ask` now prompts noticeably more than it used to; a few more is the fix working, a flood is worth a ticket | Not yet released — on main after #587. No automated test can read the CLI's own status line, so this row is the only proof the flag landed where it counts |
| **Maximizing a suspended session (#216)** | Open two sessions. Pop one out into its own window, then close that window with its **X** — the card comes home showing **Session suspended**. It should now have a proper header (name, colour dot/badge, the word *suspended*). **Double-click that header**: the workspace should fill with that one card and the other session should fold into the Collapsed strip. Double-click again to put it back. Through all of it the card must stay suspended — the **Resume** button still there, nothing restarted | Not yet released — on main after #216 |
| **A pinned session stops scrolling away (#295)** | Open enough sessions that the Sessions list has to scroll (or shrink the window until it does). Pin one near the BOTTOM — it jumps to the top, as before. Now scroll the list to the end: the pinned row must stay parked at the top while everything else slides underneath it. Pin a second one and check the two park as a pair rather than overlapping. Tab through the list from the top and confirm the highlight never lands on a row hidden behind the pinned block. Then the judgement call: with GROUPS, a pinned session only stays put while its group card is on screen — scroll past the whole group and it goes with it. Decide whether that reads as sensible or as the promise half-kept | **v0.8.3.** Automated: an overflowing rail, scrolled to its end, is e2e-checked to still show the pinned row, and the keyboard guard with it. Only your eye judges whether a floating row without its group header reads right, and whether it needs a shadow or a divider to separate it from the rows sliding under |
| **A dead card that finally has a header (#606)** | Get a card into the **Session didn't start** state — easiest is to open a session on a folder, quit, rename that folder, and relaunch. That card should now carry a header like every other card: the session's name, its colour and badge, and the words **not started**. Before this it had no header at all. Check it has no buttons of its own (**Try again** and **Close** stay in the panel below), and that double-clicking the header does not start anything. Worth knowing, and worth your opinion: that card still has NO ROW in the Sessions list, because the session was never registered — so double-clicking its header cannot maximize it the way a live or suspended card's can. Tell me if the missing row bothers you more than the missing maximize | **v0.8.3.** Automated: unit + e2e cover the header, the identity, the pill word, the absence of controls, the maximize round trip through the store, and that nothing respawns. Only your eye judges whether a card with nothing running in it wearing a header reads as informative or as a card pretending to be alive |
| **The app says when it moved a card's conversation (#539)** | On your **next launch after this ships**, open the Events drawer (the tab on the right edge, `Ctrl+E`). Expect a notice saying **Switchboard.ai-2** and **Switchboard.ai** were both pointing at one conversation, that **Switchboard.ai** kept it and Switchboard.ai-2 starts a new one — plus, if the sweep reattached an orphaned card, a second notice naming that card. Then open both: **Switchboard.ai** must be in the conversation it was already in, and **Switchboard.ai-2** must be EMPTY (a new conversation) — never the same transcript as its twin, and never some unrelated session's history. Now the part that matters most: quit **without** pressing Got it and relaunch — the notices must still be there. Press **Got it** on each, relaunch again: gone for good, and both cards land where they were left | Not yet released — on main after #539. Automated tests cover the policy, the persistence, the dismissal and the notice UI; only your real workspace has the actual duplicate pair |
| **Reorder the Sessions list (#559)** | Open three sessions and drag one onto the top half of another — a thin line should show where it lands, and letting go should put it there. Quit and relaunch: the order must come back exactly as you left it. Then right-click a row → **Move up** / **Move down** under *Order in this group*, and try **`Ctrl+Alt+↑`** / **`Ctrl+Alt+↓`** while you are in a session. Now **pin** the top session and try to drag another above it — it should stop just underneath, and **Move up** should go grey there. Dragging onto a *different* group must still just move it there, at the bottom | Not yet released — on main after #559. Automated: the drag + relaunch round-trip and the menu path are e2e; only your eye judges whether the insertion line reads right mid-drag |
| **Find works on a Terminal you never opened (#517)** | Start a fresh session in **Terminal** mode and let it print something, WITHOUT clicking the Terminal tab. From the Session tab press **Ctrl+F** and search for a word you know it printed: you should get a real count in the **Terminal (scrollback only)** group, plus a line saying to open the Terminal tab if you want to scroll to them. Now open the Terminal tab and search again — the same matches should highlight in place and step with Enter. Also check a **Direct** session still shows no Terminal group at all | Not yet released — on main after #517 |
| **The Events drawer (0.6.0)** | The 220px column is gone — look for the slim badge tab on the right edge; it shows the attention count, tinted by the hottest event. Click it, or the hotkey, or the palette; the drawer overlays the grid. Check the update/reconnect/incident notices appear inside it, Esc closes and gives focus back, and the status bar now carries a queue count | Shipped in v0.6.0 |
| **Quiet hours (0.6.0)** | Palette → quiet hours (or the About button); set a window covering now; a session needing you must NOT sound/speak/toast — but a webhook still fires | Shipped in v0.6.0 |
| Actionable toasts (Allow/Deny on Windows notification) | Unfocus the app, make a session hit a permission ask; buttons on the toast must work without focusing the app; answering in-app withdraws the toast | Dan got NO toast at all 2026-08-15 — possibly OS focus assist / notification settings during dev; INVESTIGATE before re-test (installed build should have AppUserModelId) |
| Notify-when-done (⋯ menu tick-box) | Tick one session, task it, alt-tab; toast on finish for that session only; survives restart | Same no-toast caveat as above |
| Phone push (ntfy/Pushover) + webhook | Palette → "phone push" → paste an ntfy topic → Send test; then a real needs-permission while unfocused | Deferred by Dan; needs 2-min setup |
| Themes under the new UI | High-contrast + daylight, re-run find/copy/chips/jump-latest checks | Deferred by Dan |
| Sounds 🔊 / TTS announce 🗣 | Title-bar toggles; per-session cue from ⋯ menu; announce speaks label while unfocused | Shipped 0.5.0, not yet exercised |
| Webhook payload | POST target you own; fires regardless of focus, includes finished turns | Never exercised |
| Service-health incident path | Needs a real/mocked Anthropic incident — dot, Events entry, corroboration banner | Hard to test on demand; low priority |

## Re-test after a fix ships

| What | Shipped in | Re-test |
|---|---|---|
| **Clear Session leaves the old text until a SECOND clear (#748)** | **MERGED to main, NOT RELEASED** — in `0.8.8 — unreleased`; latest release is v0.8.7 | **This is your 2026-09-03 report. The one case that matters most is the one you never tried: clear a session you have JUST OPENED.** That was guaranteed broken and is the actual bug — Claude Code does not announce a conversation's identity until the session has replied once, so switchboard was comparing the new identity against nothing. **(1)** Start a fresh Direct session, and before typing anything else use **⋯ → Clear conversation**. The text must go on the FIRST clear, with the *"Conversation cleared — context starts fresh"* divider. **(2)** Same again but typed: `/clear` in the prompt box, on a session that has not replied yet. **(3)** The worst case, which failed every single time: **resume** a session with a screenful of history (quit and relaunch, or reopen a card), then clear it immediately — the replayed history must go. **(4)** The case that always worked, to prove nothing regressed: talk to a session for a few turns, then clear — one wipe, one divider, not two. **(5)** `/compact` on a session with some history — the conversation must **stay on screen**; measured not to trigger a wipe, and this is the check that it stayed that way. **(6)** Clear while Claude is mid-reply and confirm the half-written answer goes with everything else | ⚠️ **UNTESTED. NOT RELEASED — do not run this until 0.8.8 is cut.** Root cause MEASURED against the real CLI, and **the ticket's own explanation was wrong** — it blamed a late announcement; the announcement is prompt, there was simply nothing to compare it against. Automated: 14 tests, **all 14 mutations caught**, including the real message sequence and its measured ordering. Also measured, because keying the wipe on the wrong signal would have been a worse bug: **`/compact` does not emit the reset and keeps its id**; a **`--resume` spawn emits nothing at all** (so a resumed card's replayed history is safe); and **plan-mode exit** emits nothing and does not rotate the id. **What no test can do is prove it on your machine with the real CLI** — items 1, 2 and 3 |
| Find highlight in session view | **v0.6.0** | PARTLY VERIFIED — marks work where jumping works; retest with the #557 branch |
| Find on ANY resumed session | **MERGED to main** (PR #561 — #557 #496 #495) | Ctrl+F on a session you have restarted AND then prompted again: the bar alone should carry it — count, Enter/Shift+Enter, Esc — and **the results list must never appear unless you press ▸**. Hits from before the restart may say "earlier than the conversation on screen"; hits since it must jump. **Note the premise was measured wrong in the ticket:** an idle resumed session was always fine; it is the new turn on top that broke it |
| New session from popout | **v0.6.0** | Affordance exists, tab lands beside current card in the popout |
| Conversations sitting at the TOP instead of the tail (#555) | **MERGED to main** (PR #560) | Click every session in the sidebar in turn, especially with two cards side by side: each must show its NEWEST message, and stay there when you click its own row again. Also: scroll up, switch away, come back — you should be where you were reading, not at the bottom. The restart was never the cause — clicking a card was |
| Stale **"N need you"** counters after dismissing (#621) | **MERGED to main** (PR for #621) | Let two or three sessions raise events (a permission ask, a finished turn). Read the four counts: the Events **tab**'s number, the **"N need you"** at the right of the strip across the top, and each **group header** + the **footer** of the Sessions list. Open the drawer (`Ctrl+E`) and **✕** one entry — every count must drop by one *the moment you click*, and at zero the words should vanish entirely (the group headers go back to **calm**). Then check the deliberate half: a session that is genuinely still holding a permission must KEEP its coloured, bold row in the Sessions list — dismissing is meant to clear the count, not to pretend the session is unblocked. Finally click a **Done.** entry instead of dismissing it: opening the session relaxes it to **Ready**, and that should also drop the counts | Automated: unit tests pin the derivation (dismiss, acknowledge, `none`-silenced sessions, per-group counting) and an e2e drives a real hook → real ✕ → all three rail/strip readouts. Only your eye judges whether a still-loud row next to a zero count reads right — that is the boundary with #528 |
| Dock-back slot (#558) | **FIXED, in Dan's PR queue** (PR for #558) | Your own four-step repro, with the grid split into two side-by-side sessions (A left, B right) so a wrong slot has somewhere to be wrong: **(1)** pop A out and click ⤡ — A returns to the LEFT half, not into B's tabs. **(2)** pop A out again, click ＋ inside that window to start C in it. **(3)** dock A back — A goes to the left half, alone. **(4)** dock C back — C arrives as a TAB beside A rather than taking the left half, and A is still there. Then the same in reverse (dock C first, then A): C lands beside B, A gets its left half back. Nothing should show a **Resume** button at any point, and no 1px sliver of a pane should be left in the layout. The full sequence in both orders is in `e2e/popout-dock-back.spec.ts` — the machine already checks placement and aliveness, so what is worth your eyes is whether the result FEELS right |

## Tested and passing (2026-08-31 pass, v0.8.6 — work laptop)

- **The MCP panel shows every server the session really has — #729 PR 1 of 2,
  verified on the machine that had the bug.** The work laptop showed 3 of 16
  servers; it now shows them all. This is the defect the whole MCP arc was filed
  for, and it is closed out on real hardware rather than a one-server fixture.
  The rest of that row (scope-heading wording, read-only rows, the *connecting…*
  settle window, the fallbacks, Remove) was **not** exercised and stays in
  Untested above.

## Tested and passing (2026-08-16 pass, v0.6.0)

- Update installs **and restarts itself** — #525 verified in the wild
- Right-click menus (composer cut/copy/paste, feed copy) — "seems to be working now"
- Links in replies open the browser — "working, thank you"
- Documents: a tab per file, no pin; Ctrl+F in the viewer — both "working well"
- Side-by-side diff toggle — "working well"
- Composer draft survives tab-switch / popout / restart — "this is working"
- New session from a popped-out window (creation half) — "all worked fine"
- Resume-link repair — "appears to be working now" (orphaned cards reattached)

## Tested and passing (2026-08-15 pass, v0.5.0)

- Resume after restart — conversation replays (one caveat: watch for #484 orphan cards)
- Live document re-render — "looks great"; scroll held, delete-strip works
- Paste image into composer → chip → model sees it (verified end-to-end with a real screenshot)
- Drag file into composer → chip ("seems to work fine")
- Copy buttons (code header, Bash IN/OUT, popped-out card) — "works very well"
- ↓ Jump to latest — working
- Attention queue / Ctrl+Space walk — "seems okay"
- Trust chip greyed on Direct + no pre-write — "looks good"
- Status bar (health dot, usage, count, CLI version) — "seems okay"
- New session correctly lands in main app while a popout is focused (placement rule holds)

## Found-a-bug log (dogfood sessions)

| Date | Report | Ticket |
|---|---|---|
| 2026-08-14 | Blank session view on resume (was #395, unreleased at the time) + resume-link destruction | #484 |
| 2026-08-14 | Find jump paints no highlight | #520 |
| 2026-08-14 | No discoverable file-open; wants Files tab | #521 |
| 2026-08-14 | Update installs but never restarts the app | #525 |
| 2026-08-14 | No right-click context menus anywhere | #526 |
| 2026-08-14 | Links in replies dead on click | #527 |
| 2026-08-14 | Attention coloring not intuitive (design sitting) | #528 |
| 2026-08-14 | Focused-card border not prevalent enough (design sitting) | #529 |
| 2026-08-15 | Popping out a card deletes typed prompt text | #485 (widened) |
| 2026-08-15 | Viewer pin model rejected — always-new-tab wanted | #530 |
| 2026-08-15 | No new-session path from a popped-out window | #531 |
| 2026-08-15 | Diff stuck inline, no side-by-side option | #532 |
| 2026-08-15 | Ctrl+F dead in the document viewer | #533 |
| 2026-08-15 | Find goes all-list-only on a busy session | #496 (repro added) |
| 2026-08-16 | Every session restores scrolled to the TOP, never the tail | #555 |
| 2026-08-16 | Events drawer has no visible close button (tab only) | #556 |
| 2026-08-16 | Find auto-opens the results list; wants bar-only interaction | #557 |
| 2026-08-16 | Find refuses to jump on an IDLE resumed session (worked after new turns) | #495 (evidence added, priority up) |
| 2026-08-16 | Popout-born session docks back into the WRONG grid slot | #558 |
| 2026-08-16 | Wants drag-to-reorder sessions within a rail group | #559 |
| 2026-08-16 | "N need you" counters stay stale after dismissing events (Events header + group headers) | #621 |
| 2026-08-16 | Streaming reply shows raw markdown until the turn ends; wants progressive rendering | #635 |
| 2026-08-16 | Turn boundary above each user prompt too subtle; wants it unmissable | #640 |
| 2026-08-22 | Background-task notifications render as NEW PROMPT with raw XML (BrainHarbor session) | #704 |
| 2026-08-24 | Switching away from a WORKING session sometimes closes another session's dock panel (rail unaffected; owner can reproduce) | #708 — FIXED in v0.8.2 (#558 dock-back rework), owner-verified same day |
| 2026-08-26 | Composer typing lags — keystrokes buffer then appear in bursts (severe on laptop, mild on desktop) | #716 |
| 2026-08-26 | Turn boundary (#640 fix) still not easily spotted — wants round 2 | #717 |
| 2026-08-26 | Busy spinner in the sessions rail too quiet; wants marquee-style working treatment (6 options filed) | #718 |
| 2026-08-26 | switchboard pegged laptop CPU → forced reboot; 26h uptime, log dead mid-session, 4.6x busier minimized; forensic report + 5 watcher bugs | #719 |
| 2026-08-29 | Stacked session cannot be dragged into a full-height column — only above/below offered, no root-edge split | #731 |
| 2026-08-30 | Multi-question panel: wants radio answers to auto-advance to the next tab (checkboxes stay); the #567 skip strikethrough on tab text read as a rendering glitch | #733 |
| 2026-09-03 | Switching models updates the footer model text only on Claude's next reply, not immediately (owner-confirmed; tracks the transcript cost field, not the switch); dialog wants select-then-OK with Cancel reverting | #746 |
| 2026-09-03 | Wants the footer model name clickable — one-click quick-switch popup menu, no OK step | #747 |
| 2026-09-03 | /clear (menu or typed) sometimes leaves old session text on screen; a second /clear wipes it — intermittent | #748 — **FIXED, moved to Re-test.** Not intermittent: it failed on any session that had not replied yet, and every resumed card |
| 2026-09-05 | Wants a setting: when a session needs attention, pop its window to the forefront (the DESIGN.md focus-stealing policy, for the future settings screen) | #756 |
