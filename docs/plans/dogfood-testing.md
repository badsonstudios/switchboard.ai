# Dogfood testing tracker

> **Live state — maintained automatically, without asking** (standing rule in
> `.claude/CLAUDE.md`, added 2026-08-15). Every session updates this file when:
> a user-facing feature merges (add it as UNTESTED), Dan reports a hand-test
> result (move it to TESTED or FOUND-A-BUG with the ticket number), or a fix
> for a found bug ships (move it back to UNTESTED for re-verification).
> When Dan asks "what should I test?", answer FROM this file: the UNTESTED
> and RE-TEST sections, phrased as steps + expected result.

Last updated: 2026-09-23 — **#923's performance instrument merged** (E21-01).
Also **not released**, like #483 below it. When a build does ship, **do the
#923 row first**: P2-E21-02 is you switching its capture on and working a day
on the laptop, and the whole Responsiveness epic is blocked until that happens.
Its step 6 is a privacy check worth doing by hand even though a test covers it.

Previously, 2026-09-23 — **#483's missed-events digest merged** (PR #921).
It is **not released**: the version bump is manual, so there is nothing
installable to hand-test yet. The #420 and #719 rows from v0.8.98 are still
UNTESTED and are the ones worth doing first, since those ARE installed.

Previously, 2026-09-19 — **v0.8.91 SHIPPED, and it clears the "not released"
backlog on this page again.** Six rows were untestable purely because the code
sat on `main` and in no installed build; every one of them is now installable.
**Install v0.8.91 before running any of them** — `gh release list` is the
authority, and it carries `switchboard-Setup-0.8.91.exe` plus its `.sha256`.

**Start with fork (#801).** It is the newest, it is the one no automated test
can judge, and the question it asks — *is the forked session actually useful?* —
only a real conversation answers. Its twin question matters more: **is the
original genuinely untouched?** That is the damaging failure if it is wrong.

⚠️ **One row is NOT in this build: #864** (the main window returning to its
monitor) lands in 0.8.92. It also needs your real multi-monitor rig, which no
CI runner can stand in for.

Previously, 2026-09-08 — v0.8.8 shipped and cleared the backlog before this one;
the rows it unblocked are the numbered list below.

Ordered by how fast they answer, not by importance:

1. **#763 — sessions can see each other.** Open `/mcp` in a Direct session and
   confirm your own servers are still there beside the new **switchboard** row.
   Seconds. **If anything you had configured is missing, stop and report it** —
   that is the one failure mode the whole item was built to avoid.
2. **#747 / #746 — the model controls.** Click the model name at the bottom of a
   session; confirm it switches in one click and that the name updates
   immediately. Minutes, and it wants your judgement on whether one click with
   no confirmation is right.
3. **#748 — Clear Session.** One clear should be enough now. A minute.
4. **#635 — streaming markdown.** Ask for something long and structured and
   watch it format as it arrives. The question only you can answer is whether
   the settling reads as busy.
5. **#742 — long-history sessions.** Open this repo or BrainHarbor and watch for
   the quarter-second freeze that used to happen. Also the one most worth a
   regression check: the history must still come back COMPLETE.

Still open from v0.8.7 and still worth doing: **#716** (deliberately half a fix
— the laptop is the real question), **#719** (2 of 5 bugs; needs the app left
running a full working day, so start it FIRST and let it run underneath the
rest), and the two MCP rows that **only the work laptop can test at all**,
because this machine has no claude.ai connectors.

Previously, 2026-08-31 — **laptop pass on v0.8.6. #729 PR-1 PASSES: the MCP
panel now shows every server on the work laptop** (owner, 2026-08-31) — the
3-vs-16 bug that this whole MCP arc was filed for is fixed and confirmed on the
machine that had it. Moved to the 2026-08-31 pass section below. **Still
untested on that trip: everything you DO to a server** — Turn off / Reconnect /
Load it now (#729 PR-2) and Sign in / Sign out (#734). Those rows stay UNTESTED
and are the next laptop asks.

⚠️ **Also from that trip, and the thing to not misread: composer typing still
feels sluggish on the laptop — but v0.8.6 does NOT contain the #716 fix.** That
fix is in the `0.8.7 — unreleased` CHANGELOG section, along with #724. So the
observation is a **baseline confirmation** (the bug is real and severe on that
machine), NOT a verdict on the fix, and it does not settle whether #740 is
needed. **Nothing about #716 or #724 can be hand-tested until v0.8.7 is cut** —
which is now the blocker on two rows, and an argument for cutting it soon.

Previously: **v0.8.6 SHIPPED** (2026-08-30) carrying **both halves of #729**
plus **#734** (signing in to a connector); v0.8.5 could never have fixed the
short list — what it carried was #723's footer EXPLAINING it, which is why
testing it on 2026-08-29 looked like a broken feature rather than an absent one.
#734's row is the one no machine here could test at all — the OAuth success path
has never run, only its refusals are measured. Previously: v0.8.4 shipped with the MCP Manager (#632),
its add/remove half (#714) and a not-started session's rail row (#687); v0.8.3
carried the eleven-item merge train (#268 #577 #648 #295 #606 #656 #657 #502
#491 #642 #471). Owner statement 2026-08-20: **"we're not using terminals anymore
within the app"** — every Terminal-pane check is marked MOOT below rather than
deleted; they revive if PTY-transport sessions come back into use.

## Untested — never exercised by hand

| What | How to test | Note |
|---|---|---|
| **The performance instrument — summary command + detailed capture switch (#923)** | **NOT RELEASED** — needs a version bump. **This is P2-E21-02's tooling, and the whole epic is waiting on it, so it is the one to do first once it ships.** **On the LAPTOP.** **(1)** `Ctrl+Shift+P` → *Show performance summary*. With the app just launched it should say nothing has been measured yet rather than showing a table of zeros. Use the app for a minute, reopen it: rows appear for typing, switching session, opening Settings. **(2)** Read the sentences under the table. The background-delay line says "not measured yet" for the first minute after launch, then gives numbers. **(3)** THE SWITCH: `Ctrl+,` → **Diagnostics** → *Detailed performance capture*. It must be **off** on a machine that has never had it on. Turn it on. Quit and relaunch — it must still be on. Turn it off; relaunch; still off. **(4)** With it on, type in a composer for a bit, then reopen the summary. It should now report keystrokes captured, the largest conversation sampled, and — THE ONE THAT MATTERS — whether layout was measured while you typed. **That line should say zero.** If it is amber and non-zero, stop and tell me: that is PR #739's fix having regressed, and it is the single most valuable thing this feature can find. **(5)** **Settings → Diagnostics → Show the file.** It should open the folder with `performance-capture.jsonl` selected. The button is greyed until a capture exists. **(6)** **THE PRIVACY CHECK, and please actually do this one:** open that file in a text editor and search it for something you typed, a file name from one of your projects, and a phrase from a conversation. **None of the three may appear.** You should see numbers, the words `keystroke`/`session-switch`/`palette-open`, and your machine's details (version, OS, CPU, RAM). Nothing else. **(7)** THE ONE ONLY YOU CAN JUDGE: with capture ON, does typing feel any worse than with it off? On the desktop the difference was too small to measure; the laptop is the machine that decides. If it feels worse, say so — that would make the instrument unusable for its own purpose. **(8)** Then the actual job (P2-E21-02): leave it on, work a normal day with 3+ sessions, and send me the file. | UNTESTED. Automated: the percentile maths and the "no mean anywhere" rule; the capture file's switch, rotation, append-across-restarts and fail-open; **that the file contains no string we did not author** (a walk over every string in a real capture line, so a careless field added later goes red); the Settings section and its wiring; and the load-bearing one — **that with the switch off the instrumentation is genuinely absent**: the module is never imported, no listener is attached, the layout accessors are the browser's own objects by identity, and the composer's source imports nothing from the perf family. **Measured, not asserted:** `spike/findings/e21-923-instrument-cost.md` — on the dev desktop the capture's cost is below the noise floor (under 0.05 ms/keystroke), but that is the wrong machine, which is the entire reason this feature exists. **What no test can judge:** whether the summary screen actually answers "is it better?", and step 7. |
| **The missed-events digest — what quiet hours held while you were away (#483)** | **NOT RELEASED** — needs a version bump; nothing to test until it is in an installed build. **Setup:** command palette → *quiet hours*, set a window that covers the time you are testing; the dialog's bottom line should say it is on right now. **(1)** THE MAIN ONE: click away to another app so switchboard is unfocused, then make a session need you (ask it to write a file, so it asks permission; or let one finish). You should get **no** sound, no pop-up, no spoken announcement. **(2)** Come back and open the Events drawer (Ctrl+E). The top should read **"1 notification was held while it was quiet"** and name that session, its state and the time. Check the name is the session's real name and the time is right. **(3)** Hold several, from two or more different sessions. The heading counts them and adds **"across N sessions"**. Past six, the extras should collapse to **"and N more"** rather than filling the drawer. **(4)** Press **Clear**. The summary goes. Reopen the drawer — still gone. Quit and relaunch — still gone. The events themselves should still be listed underneath as normal. **(5)** Open the drawer WITHOUT pressing Clear, close it, reopen it: the digest must still be there. Losing it by glancing in is the failure this is designed against. **(6)** Turn quiet hours off, let a session finish normally, open the drawer: there should be **no** digest at all. **(7)** Judgement calls: does the summary read well at the drawer's width, and is six named rows the right number before it collapses? **(8)** If you can leave it running across midnight, older rows should show a date, not just a bare clock time. | UNTESTED. Automated: the ordering, counts, overflow, session tally and earlier-day marking (unit); the notice's rendering, its accessible name and that it is absent when nothing was held; an e2e carrying the whole chain — hold an event inside a simulated quiet window, see the digest on return, Clear it, and assert the workspace file's held list is empty. Eleven mutations verified red. **What no test judges:** whether the summary reads well, whether six is the right cut, and the real overnight case. **Scope to know:** it digests what quiet hours SILENCED. Events that happen while the app is merely unfocused are not silenced (that is when pop-ups do fire) and already appear as ordinary Events rows, so they are deliberately not repeated here. |
| **Answer permissions and read questions from the Events drawer; All · Needed · By session filters (#420)** | **RELEASED in v0.8.98** (2026-09-22). **(1)** Open two sessions and get both asking permission (ask each to write a file). Open the Events drawer (Ctrl+E). Each entry shows the tool and the file, plus **Allow · Allow all · Deny**. Press **Deny** on one: that session carries on without it, and the session you were working in does NOT change. The other entry's buttons are still there. **(2)** Press **Allow** on the other: the file appears, and the entry drops out. **(3)** In one session, ask for two files in one go, so the entry says **+1 more**. Press **Allow all**: both get written, and the next thing that session asks for runs with no prompt at all. **(4)** Ask a session something that makes Claude ask you a multiple-choice question. The entry says **1 question waiting**. Click it to see the question and its choices, then **Answer in session**, which opens the session's own question panel. **(5)** Try **Needed** (seen entries disappear) and **By session** (entries in your Sessions-list order). Close and reopen the drawer: the filter you picked is still on. **(6)** Judgement call: does a row with three buttons read clearly at the drawer's width, or is it cramped? | Automated: unit tests for the filter and join rules and the panel's rendering (a click anywhere in the answer area never opens the card). The e2e runs on the stream fake: deny one session while the other stays held, allow runs the tool (checked on disk), Allow all answers both held calls and the follow-up (mutation-verified red), and the question list plus filters. What no test judges: how it looks and feels, and real `claude` question prompts |
| **Sessions shut down cleanly, and the log counts leftover programs (#719)** | **RELEASED in v0.8.98** (2026-09-22). **On the LAPTOP**, which is the machine this is for. **(1)** THE MAIN ONE: use switchboard normally for a day, with your work projects and their Azure DevOps helper. Then open `%APPDATA%\switchboard\logs\switchboard.log` and search for `cpu heartbeat`. Each line now carries `sysProcs` (programs on the whole machine), `sysTop` (the most common ones by name, e.g. `"node.exe": 12`) and `children` (switchboard's own). **Watch whether `node.exe` in `sysTop` climbs over the day.** If it does, note roughly when (during sessions, or after closing cards / Restart), and send the log. **(2)** Close a session card, and Restart another. Each should go away as quickly as before, with no lag. **(3)** Quit switchboard with a session mid-answer. It may take up to about 3 s longer to disappear than before, and only then; it should never hang. **(4)** After you quit, `Get-Process node` in PowerShell should not show a batch of helpers left behind from switchboard's sessions. | UNTESTED. **Not a proven fix:** probing the real CLI showed the old close didn't normally leak helpers, and the one leak reproduced is inside the CLI (it abandons a slow-starting helper mid-session). This ships a cleaner shutdown plus the counts that will say where the laptop's 53 came from. Automated: graceful close / tree-kill backstop / quit drain, all mutation-verified; quit verified in the real app against 33 live Direct sessions |
| **Settings… in the File menu (#908)** | **RELEASED in v0.8.97** (2026-09-21). **(1)** Open the **File** menu: it reads **Open File…**, **Settings…** (with Ctrl+, shown beside it), a line, then **Exit**. Click **Settings…** and the Settings window opens. **(2)** Click into a session's prompt box, type a few words, then choose **File ▸ Settings…**: Settings still opens, and your words are still there after you close it. **(3)** Pop a session out (⤢), and from the popout window choose **File ▸ Settings…**. The main window should come forward with Settings open over it. **(4)** Ctrl+O and Ctrl+, still do what they did before. | Automated: the menu order, the shown-not-claimed shortcut and the click reaching the action (unit); an e2e that the menu click opens Settings with the composer focused, proven red by mutation. What a machine can't check: the popout case and how the real menu looks. Settings is ignored if another dialog is already open, same as Open File… |
| **The open tab is shaded in the session's colour; the name leaves the header (#905)** | **RELEASED in v0.8.96** (2026-09-21). **(1)** THE MAIN ONE: dock three sessions as tabs in one group. The open tab and the bar under it should be one shaded piece in that session's colour, with the tab name in bold. The other two tabs should sit flat on the strip in lighter text. Click through them: the shade follows, and the tabs don't shift sideways. **(2)** The header shows the badge and the task label (or a dashed **+ task label** box), with no session name. Click **+ task label** and type one. **(3)** Try all four themes (Ctrl+, → theme). Is every word on the shaded bar easy to read, especially **context**, the status pill and the label? **(4)** SPLIT: put two sessions side by side. Both open tabs are shaded; only the one in the group you last clicked is bold, and that group has the blue frame. **(5)** Pop a session out (⤢). Its window's tab and header are shaded the same way. **(6)** Double-click an empty part of the header: it still maximizes, and double-clicking again puts the layout back. **(7)** Is 24% shade the right strength? It's one number to change. | Automated: `--text` on the shade clears AA in 4 themes × 8 colours (unit); e2e measures the painted tab = header colour, every word on both AA in all four themes (label and prompt states), bold only in the focused group, and tab widths stable on switch. Each e2e half was proven red by mutation. What a machine can't judge: whether it *looks* right, and the strength |
| **Updates: notes for every release you missed, and an "Installing" window (#899)** | **RELEASED in v0.8.95** (2026-09-21), and **only shows on the update AFTER it**: install 0.8.95 the usual way (that install is still silent, and its notes are 0.8.95 only), then when 0.8.96 is offered: **(1)** if you are more than one release behind, the update box lists every missed version under its own heading, newest first; one release behind looks as before. **(2)** Press **Update**: the app closes and a small **"Installing, please wait…"** window appears (note which icon it shows), with no wizard and no "switchboard is running, click OK" box. **(3)** The app reopens by itself, and the Events drawer says **"You're now on v0.8.96"**. **(4)** Your desktop and Start-menu shortcuts are unchanged. | Unit tests pin the notes (order, eligibility, cap) and the installer arguments; mutation 7/7. **The installer window itself cannot be tested here**: running it would replace the live install. That needs your machine |
| **The Settings window, and a title bar that finally fits (#885 / #879)** | **In v0.8.93.** **(1)** THE MAIN ONE: press **Ctrl+,**. A **Settings** window opens with three sections — Appearance, Attention, Advanced. Change the **theme** in it and confirm the app repaints instantly, with the window still open. There is no Save button; that is deliberate. Press **Escape** to close. **(2)** THE BAR: look at the title bar. The five theme buttons, the two language buttons and **⑂ fork sessions** should be GONE. **🔔 notifications**, **🏷 labels**, **🔊 sounds**, **🗣 announce** and **🔓 auto-trust** must all still be there — those are the ones you need to kill mid-screen-share, and burying one would be a bug this item caused. **(3)** #879, THE REASON: make the window narrow — about 1024px wide, or just drag it in until it is roughly half a 1080p screen. Nothing in the title bar should fall off the right-hand edge, and the page must not scroll sideways. This used to be ~11 controls off screen. **(4)** MUSCLE MEMORY: press **Ctrl+Shift+P** and type *quiet hours*. It should open Settings scrolled to the Attention section, not a dialog of its own. Same for *phone push* and *task label size*. **(5)** ABOUT GOT SHORTER: click the version stamp at the top left. It should carry ONE **Settings…** button where it used to have three rows, plus **Check for updates…** (that one stayed on purpose). **(6)** THE ONE ONLY YOU CAN JUDGE: is a single scrolling window the right shape, or does it want a left-hand nav once there are more settings? It is ~15 preferences today. | Automated: the modal contract (Escape / click-away / focus), every absorbed control still writing through, all four themes still clearing AA, the RTL path, and an e2e guard that the bar fits at 1024px with no sideways scroll. What a machine cannot judge: whether the SHAPE is right, and whether anything you reach for daily is now too far away. |
| **Claude writes the task label, and keeps it up to date (#758)** | ✅ **Shipped in v0.8.92 — installable now.** ⚠️ **THIS ONE SPENDS YOUR SUBSCRIPTION** (it shipped OFF in v0.8.92; #883 turns it on by default from 0.8.93). **(1)** THE SWITCH, which is now a THREE-STATE chip rather than a second one: with it reading **🏷 auto labels**, work a session for a while and confirm its label never changes by itself. Click the chip once — it should read **✨ AI labels**. (A second click reads **🏷 labels off**, a third returns to the start; the cycle exists because the title bar had no room for another chip, #879.) **(2)** THE MAIN ONE: give a session some real work and let it finish a turn. Within a few seconds its task label should become a short phrase describing **what it is actually doing** — not the folder name, and not the conversation's opening title. That is the whole feature and only you can say whether the phrase is any good. **(3)** THE POINT OF THE ITEM — DRIFT: keep working in that session until the job genuinely changes (finish one thing, start another). The label must **follow**, not stay stuck on the first job. This is the ask; if it never updates, the feature has failed even if step 2 looked fine. **(4)** IT DOESN'T PESTER: leave a session idle for half an hour with the switch on. Nothing should happen at all — no label change, no cost. Then send one trivial prompt ("say ok") and confirm it does **not** relabel for that either. **(5)** YOUR WORDS WIN: while a session is finishing a turn, type your own label into the card header. Your label must stay, permanently, and must never be overwritten. **(6)** OFF MEANS OFF: click the chip on to **🏷 labels off**. The last label it wrote stays on the card (that is deliberate), but nothing new is ever written, and every auto label leaves the cards and the rail. Clear the box by hand if you want a label gone for good. **(7)** AND BACK: click once more to return to **🏷 auto labels** — the labels you had should reappear instantly, because hiding never deleted them. **(8)** COST SANITY, the one worth doing properly: with it on across a normal working day, check `/cost` or your usage and confirm the labeler is a rounding error rather than something you can feel. If it is noticeable, say so — the cadence numbers are a judgement call I made and they are one constant each to change. | ⚠️ **UNTESTED.** Automated: the containment was **measured against the real CLI, not assumed** (`spike/probes/758/`, findings in `spike/findings/758-label-containment.md`, claude 2.1.272) — and the measurement changed the design. **`--restricted` ALONE leaves 34 tools and your MCP server, keeping Read/Glob/Grep and SendMessage/ListAgents** — the exact surface #760 used to read transcripts and message six sessions across four unrelated projects. Only `--tools ""` empties it and only `--strict-mcp-config` evicts the servers, so the labeler runs with **nothing**: 0 tools, 0 servers, proven against a 33-tool control. Plus 41 unit tests on the decision rules (when to spend, the ordering of the gates, the last-writer discard) and the runner (the prompt is never an argument, every failure resolves, a timeout kills the process tree). **What no test can judge: whether the labels are any good, and whether the drift in step 3 actually tracks the work** — which is the entire feature. ⚠️ Also worth knowing: the probe caught the model **refusing** a prompt that read like an injection, so the label prompt's wording is load-bearing; if labels ever silently stop updating, that is the first thing to suspect. |
| **The card names itself the instant you prompt it, and AI labels are on by default (#883)** | **In v0.8.93.** Owner feedback on #758 after using v0.8.92. **(1)** THE MAIN ONE: open a brand-new session and send your first prompt. The card's task label should become **your own words** immediately — no waiting, no cost. **(2)** THEN IT IMPROVES: let that turn finish. Within ~15 s Claude's own label should REPLACE the one from your prompt, and it should read better than your raw prompt did. **(3)** A SLASH COMMAND: start a session and send `/clear` (or any command). The label must read the command as a person writes it — **not** raw `<command-name>` markup, which is the bug the history picker shipped once. **(4)** ON BY DEFAULT: on a machine that has never had this setting, the chip should already read **✨ AI labels**. Clicking once gives **🏷 auto labels**, again gives **🏷 labels off**, again returns to the start. **(5)** IT ONLY FILLS A BLANK: on a session that already has a label, send another prompt — the label must NOT be replaced by your new prompt text. **(6)** YOUR WORDS STILL WIN: type your own label, then send a prompt — it must survive untouched. **(7)** A MENTION: send a prompt that starts with an `@mention` of another session. The label should stay blank until Claude's label arrives, rather than reading "Context from @other-session" — refusing is deliberate. | ⚠️ **UNTESTED.** Automated: 32 tests on the label decisions (including the instant label's command handling, its refusal on an injected-context turn, and that it never replaces an existing label) and 256 on the IPC wiring, plus the store's default flip asserted in both directions. **What no test can judge: whether the instant label is actually useful for the second or two before Claude's replaces it**, and whether the default-on setting feels right rather than presumptuous — it reverses the opt-in the ticket originally specified, on the owner's own instruction. |
| **Task labels get three lines, and a size setting (#877)** | **In v0.8.93.** Owner feedback on #758: "at least three lines max", then "default to the full width and fill the space, but have options to make it smaller". **(1)** THE MAIN ONE: with **✨ AI labels** on, let a session earn a real label. It should now be a fuller phrase and wrap onto **up to three lines** — on the card header AND in the sessions list. A long label must be clamped at three with an `…`, not run on for ever. **(2)** THE ROW LAYOUT, which is the other half of the ask: in the sessions list, a session's state (*working*, *needs you*, *idle*) now sits to the RIGHT OF ITS NAME rather than on the second line. Drive a session until it needs permission and confirm **both** the state and the task label are visible at once — before this, a session that needed you lost its label entirely. **(3)** THE SETTING: `Ctrl+Shift+P` → *task label size*, or the button in the **About** panel. Pick **Compact** — every label everywhere should drop to one line **immediately**, on cards that are already open, without a restart. **Medium** gives two, **Full** three. **(4)** IT STICKS: quit and relaunch — the size you picked must still be in force, and the dialog must show it selected. **(5)** NOTHING IS LOST: on Compact, click a clamped label to edit it — the whole label must be in the box, not the truncated version. **(6)** THE JUDGEMENT CALL, and the only part no test can make: whether three lines is actually the right amount of room, and whether the fuller phrases Claude now writes are better than the six-word ones or just longer. If the rail feels too tall at Full, say so — the default is one constant to change. | ⚠️ **UNTESTED.** Automated: 7 tests on the shared size vocabulary, 9 on the dialog (every size offered, the stored one checked, the pick written through with no Save button, Escape and click-away write nothing), 7 on the rail row (the clamp comes from the SETTING and not a constant; the em dash holds the space open before a label arrives; **the label reaches a screen reader even on a row that needs you** — the regression this item fixed, and one invisible from the pixels), plus store, session-store and IPC tests that the size survives a reload and that a value outside the vocabulary cannot be written. All six load-bearing fixes were mutation-checked — reverted one at a time, each killing exactly its own test. **What no test can judge: whether the labels read better with the room, which is the entire point.** |
| **The Terminal tab and the ⋯-menu Terminal/Direct switch are gone (#873)** | ✅ **Shipped in v0.8.92 — installable now.** **(1)** THE MAIN ONE: open any session. The card must have **three** tabs — **Session**, **Changes**, **History** — and no Terminal tab at all. **(2)** THE MENU: open a session's **⋯** menu. There must be no *Transport: … — switch to …* entry and no *Restart session now*. Everything else in that menu must still work — Clear conversation, Notify when done, Fork, Sound, the autonomy rows. **(3)** THE KEY: press ``Ctrl+` `` on a focused session — nothing should happen, and no card should change tabs. Open the palette (`Ctrl+Shift+P`) and type *terminal*: there should be no **Toggle Terminal view** command. **(4)** THE MIGRATION, and the one that matters if you ever set a session to Terminal mode: that card must come back on Direct and keep working — same card, same name, same conversation, nothing lost. Then check `%APPDATA%\switchboard\logs\switchboard.log` for a line about sessions moved off the **removed Terminal transport**, with a count. If you have no such card there will be no line, which is correct. **(5)** THE TRUST CHIP: hover **🔓 auto-trust / 🔒 ask trust** in the title bar. It stays greyed, and the tooltip must now explain that **nothing can ask** and that your choice is kept — it must NOT tell you to switch a session to Terminal from its ⋯ menu, which is the instruction this item exists to remove. **(6)** FIND: `Ctrl+F` in a session should show a single position (`3 of 14`) with **no** *Terminal (scrollback only)* group beside it and no *open the Terminal tab* notice. **(7)** NOTHING ELSE MOVED: send a prompt, answer an approval, open **Changes**, pop a card out and dock it back, hide a card and bring it back — all exactly as before. | ⚠️ **UNTESTED.** Automated: the workspace **v1→v2 migration** is proven by unit tests (a stored `pty` choice is CLEARED so the card follows the Direct default *and* still honours a `SWITCHBOARD_TRANSPORT` override; an explicit `stream` choice is left alone; a card that never chose is untouched; the log line fires only when something actually moved), plus the panel registry (three panels, nothing kept mounted), the retired command, the unregistered Terminal find provider, and the reworded trust and transcript-fallback copy. **What no test can answer: whether anything you relied on the Terminal tab for is now unreachable.** The PTY transport itself is untouched and still works — only the way to LOOK at one was removed — so if you find yourself needing it, say so, because that is the signal this removal was wrong. ⚠️ Also note what the automated suite LOST with the tab: the end-to-end proofs of input→pty→render, of the CLI keeping every key inside a terminal, and of grouped find over two surfaces. Those are recorded in PROGRESS.md and in the specs themselves. |
| **The main window comes back to the monitor it was on after the displays sleep (#864)** | ✅ **Shipped in v0.8.92 — installable now.** **This one needs your real rig, and the monitor must be POWERED OFF (or genuinely asleep), not locked or blanked** — a screensaver does not reproduce it, because the bug is the display actually leaving the bus. **(1)** THE MAIN ONE: put the switchboard window on a SECONDARY monitor, leave it a moment, power that monitor off, then power it back on. The window must return to that monitor at the same size and position. Today it reappears on your primary. **(2)** THE PERSISTENT VARIANT, which is the half that made it permanent: with the window on the secondary, power the monitor off, then QUIT switchboard while it is still off. Power the monitor back on and relaunch — it must open on the secondary, not the primary. **(3)** MAXIMIZED: repeat (1) with the window maximized on the secondary. It must come back maximized **on that monitor**. **(4)** NOTHING ELSE CHANGED: with a session popped out onto the secondary, repeat (1). The popout keeps its old behaviour — it does **not** jump back on its own, Events still offers you the restore. Only the main window returns by itself, and that difference is deliberate. **(5)** A DISPLAY IT HAS NEVER SEEN: plug in something new — a projector, a TV, a monitor in a new position. The main window must stay exactly where it is. It must never be pulled onto a display it has not lived on before. **(6)** THE COMMUTE: work at the desk, undock to the laptop alone, work there, dock again. Each arrangement should remember its own position. | ⚠️ **UNTESTED, and the automated tests genuinely cannot reach this one** — CI runners have a single display and no way to detach it, so no amount of green CI is evidence here. Automated: 19 unit tests on the decision logic (it restores only to an arrangement the window has already lived on; never onto bounds no current display can show; never moves a window already in place; carries the maximized state back; evicts the oldest arrangement past the cap) plus store tests proving the damaging half — that a save made while a monitor is asleep **cannot** overwrite the note for the arrangement that included it, that it survives quit/relaunch, and that a workspace file written before this change loads unchanged. **What no test can answer: whether it actually comes back on YOUR hardware.** The bug is a real DisplayPort detach and nothing here can produce one. |
| **Fork a session so a second one starts knowing everything the first does (#801)** | ✅ **Shipped in v0.8.91 — installable now.** ⚠️ **EXPERIMENTAL and OFF by default**, so nothing below is reachable until you turn it on. **(1)** THE SWITCH: with it off, open any session's **⋯** menu and confirm there is **no** fork entry at all — absent, not greyed. Now turn on **⑂ fork sessions** in the title bar. **(2)** NOTHING TO FORK YET: open a brand-new session and check its **⋯** menu — the entry should still be absent, because it has no conversation yet. Send it a prompt, then look again: now it appears. **(3)** THE MAIN ONE: in a session with some real history (a few prompts, let it edit something), choose **Fork into a new session**. A new card opens in the same folder. **Ask the new session something only the original could know** — *"what did I ask you to do earlier?"* — it must answer from the inherited conversation. That is the whole feature and nothing else proves it. **(4)** THE ORIGINAL IS UNTOUCHED, which is the damaging failure if it is wrong: go back to the first session. It must be exactly as you left it — not restarted, not reset, nothing appended, and still usable. Send it a prompt and confirm it still works. **(5)** THEY DIVERGE: tell the fork a new fact, then ask the original about it. The original must know nothing. Then the reverse. **(6)** TWO CARDS, TWO CONVERSATIONS: confirm both cards keep working side by side and neither one's Session view starts showing the other's messages — if they ever converge on the same content, stop and report it. **(7)** THE SWITCH GOES BOTH WAYS: with a forked session open, turn **⑂ fork sessions** back off. The open fork must keep working normally; only the menu entry goes. **(8)** RESTART: quit and relaunch with the flag off, and confirm no session tries to fork itself on restore. | ⚠️ **UNTESTED.** Automated: the CLI contract was **measured, not assumed** — two committed probes drove the real `claude` 2.1.272 (`spike/probes/801/`, write-up `spike/findings/e11-801-fork-adoption.md`) and proved the fork carries the history, leaves the source transcript **byte-identical** (sha256, size and mtime), lands in the target folder's directory, and announces **our** session id rather than the parent's — the last one being what stops two cards tailing one transcript. Plus 28 unit tests: the capability gate (both fakes deliberately withhold it, so the path is unreachable for them), the argv and its refusal to spawn an unpinned fork, every IPC refusal (flag off, no capability, source not on disk, a dash-led or traversing id, a bad folder), and the one that matters most — that the card records the **fork's** conversation and never the source's. **What no test can answer: whether the forked session is actually useful** — whether it picks up the work as if it had been there, which is the entire point and only a real conversation can judge it. Also unmeasured: forking a conversation that is **live in another process** (every probe forked an idle one). |
| **#815 Report a problem — Help menu, with a GitHub issue or email destination** | Open **Help ▸ Report a problem…** (or the palette, `Ctrl+Shift+P` → *report*). Check **Send report** is dead until you type a subject. Try all three destinations: **Just build the zip** (a zip should appear in Explorer — open it and confirm it holds `logs/`, `workspace.json` and `bundle-info.txt`, and **no** transcripts); **Open an email** (your mail app opens, addressed, with the zip path in the body); **Create a GitHub issue** — on the laptop this should need NO setup if `gh` is signed in, and the issue should appear on the repo carrying your text plus version/system details. Confirm the token field is **not** offered when `gh` already works, and that the box always says the zip is not attached | UNTESTED. The GitHub path has never run against the real API from inside the app — only against a stubbed one. Also worth checking the report survives with no network: the zip should still be written and revealed |
| **#719 CPU heartbeat — the app records its own per-process CPU every minute** | On the LAPTOP: use switchboard normally for an hour or two, then open `%APPDATA%\switchboard\logs\switchboard.log` and search for `cpu heartbeat`. Expect one line a minute. Each names every process with a `cores` figure (`1.0` = one core fully busy, on any machine) and `lagMaxMs` (how long the app was unresponsive). A quiet minute legitimately lists no processes at all — only ones using a measurable share are named — so `procs: []` on an idle app is correct, not a broken reading. **If a freeze happens, do not clear the log**: the busy minutes are recorded at `warn` and name the responsible process | UNTESTED. Instrumentation only — it fixes nothing, and the real test is whether the NEXT freeze names its own burner. Verified end-to-end on the dev desktop only, which has never reproduced #719 |
| **Hand one session's context to another by dragging its chip (#799)** | ✅ **Shipped in v0.8.91 — installable now.** Open **two** sessions in different folders and give the first one some real work to do (a few prompts, let it edit something) so it has a conversation worth handing over. **(1)** THE GESTURE: find the small **context** chip in the first session's header and drag it onto the *second* session's prompt box. A dialog should open — it must NOT inject anything on the drop itself. **(2)** THE NUMBERS, which are the point: all three options (**Last response**, **Summary handoff**, **Full excerpt**) show a rough token size, and they should differ substantially. **Summary handoff** must be pre-selected — it is the documented default. Sanity-check the sizes against the session: a long conversation should not report a 20-token handoff. **(3)** CHOOSE ONE: press **Add to prompt box**. The text appears in the second session's prompt box as a block headed *Context from @<first session>* — and **nothing is sent**. **(4)** THE ENTER: type something of your own ("carry on from here") and press Enter. The other session should receive the context AND your line, and should be able to act on it — this is the real question: *is the handoff good enough that the second session can actually continue the work?* Only you can answer that. **(5)** CANCEL: drag another chip, press **Cancel** — nothing should appear anywhere. **(6)** SELF-DROP: drag a session's own chip onto its own prompt box; it should refuse with a note rather than doing nothing. **(7)** A THIN SESSION: open a brand-new session and drag its chip somewhere — options should read *nothing recorded* rather than looking full. **(8)** A LONG ONE: do it from a very long conversation (this repo) and confirm the dialog says it covers only the most recent part, and that the same caveat appears in what the other session receives. | Automated: the offer builder (sizes copied off the package's own section estimates, not recomputed — pinned with deliberately wrong estimates; coverage carried into every option; empty marked; control characters stripped), the dialog (default is Level 2, Escape/Cancel/click-away commit nothing, thin options marked), the composer (drop opens the dialog and injects nothing, self-drop refused, a broker refusal treated as no offer, the drop swallowed so it can't also open a session), and the seam (the parked block goes verbatim on the next Enter with no sibling header, and an Enter before it settles doesn't send it). **What no test can answer: whether the handoff is actually good enough to continue the work.** |
| **A session can fetch another session's handoff itself (#800)** | ✅ **Shipped in v0.8.91 — installable now.** The agent-pulled twin of the context chip above: no dragging, the session asks. Open **two** sessions in different folders; give the first one real work to do (a few prompts, let it edit something). **(1)** THE MAIN ONE, and the whole point: in the *second* session, say *"pick up where &lt;first session&gt; got to"* — in your own words, naming no tool. It should go and fetch the handoff on its own and then tell you what that session was doing. **The real question only you can answer: did it choose the right tool?** It has `get_session_output` (raw recent output) sitting next to `get_session_context` (the structured handoff), and reaching for the wrong one gets a worse answer rather than an error. **(2)** THE LEVELS: ask for *"just the last thing it said"* and then *"the whole handoff"* — the first should come back noticeably smaller. **(3)** A THIN SESSION: open a brand-new session that has done nothing and ask another session to pick up its work. It must be told plainly there is nothing recorded — and, if it asked for one of the smaller levels, that the fuller handoff may still have something. It must **not** be told a fuller level is pointless, and it must not conclude that session "did nothing" when the truth is there was nothing to read. **(4)** A LONG ONE: do it from a very long conversation (this repo) — what the asking session receives must say it covers only the most recent part. **(5)** IT ONLY READS: confirm nothing appears in anyone's prompt box and nothing is sent to the session being read. **(6)** BAD INPUT: ask it for a detail level that doesn't exist (*"give me the 'everything' level of X"*); it should be told the three real ones rather than quietly getting the default. | Automated: the vocabulary and its default (shared with the drop dialog, so a fourth level can't be offered by one and refused by the other), the unknown-level refusal, the level reaching the query core untouched (`['state','excerpt','everything',7,undefined]` pinned exactly), the coverage statement surviving verbatim, the fence, the empty-at-one-level wording, the in-flight bound, and a full end-to-end run through a real transcript on disk. **What no test can answer: whether an agent that was never told this tool exists actually finds it and picks it over `get_session_output`** — the descriptions are what do that, and only a real session can judge it. |
| **Sessions can leave each other notes on a shared board (#796)** | ✅ **Shipped in v0.8.91 — installable now.** There is no UI at all for this: it is a thing sessions do, so every check is a plain-English request to a session. Open **two** sessions. **(1)** THE MAIN ONE: in session A say *"pin the current build status on the shared board where the other sessions can find it"*, then in session B say *"check the board for a build status"*. B should find it and tell you what A wrote, including that A left it. **The question only you can answer: did A pick the right tool?** `send_to_session` sits right beside this one and promises to reach a *person* — an agent that wanted to notify you and pinned a note instead has left it somewhere nobody is looking, and nothing will report that. **(2)** DISCOVERY: in a third session (or B again) ask *"what's on the shared board?"* without naming a key. You should get a list of names with who left them and how big they are — and **not** the contents. **(3)** NOTHING IS DELIVERED: confirm that pinning a note in A causes **nothing** to appear in B's prompt box, no badge, no sound. B must have to go and look. **(4)** A MISSING NAME: ask B to read a name nobody has pinned. It should say so plainly and tell you what *is* there — it must not conclude the board is empty or that A did nothing. **(5)** AFTER A IS GONE: close session A, then ask B to read A's note again. The note must still be there, and B should be told its author is no longer running rather than trying to go and ask it. **(6)** IT DOES NOT SURVIVE A QUIT: restart the app and ask about the board — it should be empty. That is deliberate, not a bug. | Automated: every cap (key, value, key count, and the board's total — including the one accounting case that matters, an overwrite that SHRINKS a value not being refused by a full board), refusal-rather-than-truncation, the unknown-key answer that names real keys, attribution resolved from the token rather than the child's argv, the publisher-has-exited flag, list-without-values, insertion order, and a real A-publishes/B-reads round trip over two separate endpoints plus the same end to end through the bus child. **What no test can answer: whether an agent reaches for the board when it should, and — more importantly — whether it reaches for it when it actually wanted to tell YOU something.** |
| **Previous conversations are described by what you asked, not by markup (#846)** | ✅ **Shipped in v0.8.91 — installable now.** Owner-reported while hand-testing #836 on v0.8.90. **(1)** THE MAIN ONE: open the **🕘** picker in a folder you have real history in (this repo). **No row may contain angle-bracket markup** — no `<command-name>`, no `<command-message>`. Rows that previously read `<command-name>/clear</command-name>…` should now read the first real thing you asked in that conversation. Measured: 28 of the 40 newest conversations here open with a slash command, so there should be plenty of before/after to see. **(2)** COMMANDS-ONLY: find (or make) a conversation that was *nothing but* commands — start a session, run `/clear`, close it. Its row must read **`/clear`** — not blank, not markup. **(3)** WITH ARGUMENTS: in the picker, a conversation that opened with a command that took an argument should show it in full (`/next-item 818`), not the bare name. **(4)** THE FEED, which changed too: in a session, run a slash command that takes an argument. The collapsed block in the **Session** view should now be labelled with the argument included — before this it showed only the command name. **(5)** THE INVISIBLE HALF: have one session read another's work (`@`-mention it, or ask it to). The context it receives must not carry `<command-name>` markup as the other session's stated goal — this was the same bug reaching session hand-offs, where you would never have seen it. **(6)** NOTHING ELSE MOVED: an ordinary conversation that began with a real question must be described exactly as before. | Automated: the shared helper over the real measured shapes (including the multi-line blob the CLI actually writes, and the deliberate non-matches — a prompt that merely *mentions* `<command-name>` is still a prompt); the prompt reader skipping a command block; the commands-only fallback; a hand-off package refusing to take a command as its goal; and the full feed-blocks and FeedView families for the renderer change. What no machine can judge: whether the descriptions now actually help you recognise a conversation at a glance on a machine with real history — which is the judgement that found this bug in the first place. |
| **A long-held maximize no longer replays a days-old layout (#818)** | ✅ **Shipped in v0.8.91 — installable now.** **Your laptop is the test rig**: it has a real stale maximize (the chip reads `▦ Grid · maximized`, taken days ago on a background tab, 15-card snapshot). **(1)** THE MAIN ONE, and do it before anything else while the stale state still exists: find the maximized session, make sure it is **not** currently filling the workspace, and double-click its header. It must **maximize that session** — not fold your workspace back to an old arrangement. Before this fix that gesture restored the layout from the day the maximize was taken. **(2)** THE ORDINARY UNDO, unchanged: double-click a session's header to maximize it, then double-click again within a minute — the workspace must come back exactly as it was, including anything you had hidden by hand. This is the case the fix must not have cost anything. **(3)** THE PARTIAL RESTORE: maximize a session, then go and re-open a couple of the folded sessions from the Collapsed strip and leave them open. Now undo the maximize — the ones you re-opened must **stay** open, while the ones you left folded come back as they were. **(4)** NOT A TRAP, still: with a maximize held, click a folded session in the strip to go look at it — it must come back and stay; then undo the maximize and the arrangement still restores. **(5)** A SESSION CREATED SINCE: maximize, add a new session, then undo — the new one must be left exactly where it is, not swept into the old arrangement. **(6)** `Ctrl+Shift+M` must behave identically to the double-click in (1) and (2). | Automated: the standing check and the snapshot filter as pure unit tables (a card re-expanded since, one pushed further down, one still folded as the maximize left it, one the maximize was never allowed to fold, the maximized card judged against `expanded`, closed cards); the inherited-snapshot trap (a stale maximize must not hand its snapshot to the next one); and the existing e2e that reveals a folded card mid-maximize and still restores. What a machine cannot judge: whether (1) feels like the app doing the obvious thing on **your** actual stale state — that state exists on exactly one machine and cannot be reproduced in CI. |
| **Open a previous conversation from a session card (#836)** | **RELEASED in v0.8.90 (2026-09-16) — ready to hand-test.** **(1)** THE MAIN ONE: open a session in a folder you have used with Claude Code before (this repo is ideal — it has hundreds). Click the **🕘** in the card header. You should get a list of that folder's past conversations, newest first, each with a real description — Claude's own title, or your opening question in quotes — and when you last worked on it. Pick one: a **NEW** card opens on that conversation, and the card you clicked from must be completely untouched. Scroll the new card's Session view: the old conversation's history should be there. **(2)** SEARCH: type a few letters of a conversation you remember; the list narrows. Type nonsense; it says nothing matches. **(3)** ALL PROJECTS: click **This folder** to switch it to **All projects** — conversations from your other repos appear, each with its folder beside it. Pick one from a *different* folder: the new card must open in THAT folder, not this one. **(4)** NO DOUBLE-OPEN: with a conversation already open in a card, open the picker again — that row should be greyed out and say *already open*, and clicking it should do nothing. **(5)** `+ SESSION`: click **+ session** and choose a folder you have history in. The picker should appear by itself; **Start a new conversation instead** at the bottom, and `Esc`, must both give you a normal new session. Choose a folder with NO history and no picker should appear at all — straight to a new session, as before. **(6)** A POPPED-OUT CARD: pop a card into its own window and use its 🕘 there; the new card should land in that window. | Automated tests already cover the listing, the descriptions and their fallback, the refusals, the dialog, and one full end-to-end open-and-resume. What needs a human: whether the descriptions actually help you RECOGNISE a conversation on a real machine with real history, and whether the `+ session` interruption is welcome or annoying. ⚠️ **PARTLY ANSWERED, AND IT FAILED:** the owner ran exactly that check on v0.8.90 and found rows showing raw `<command-name>/clear</command-name>` markup — filed as **#846**, fixed for 0.8.91. Re-run the description half of this row together with #846's, not before it. |
| **`@Name` brings that session's recent work into your message (#798)** | **RELEASED in v0.8.90 (2026-09-16) — ready to hand-test.** Needs two or three sessions in different folders, and the session you mention must have said something first. **(1)** THE MAIN ONE: in session A, ask it something so it has a few replies. In B's message box type ``take @A's last output and summarise it`` and press **Enter**. Your turn in B should show A's recent conversation **in front of** your words, headed *Recent output from A*, inside a marked block — and your `@A` should read `"A" (session)`, not `@A`. **(2)** NOTHING IS EATEN: send `mail me at dan@example.com about @nosuchsession please` — it must arrive exactly as typed, with nothing attached. **(3)** SAME NAME TWICE: open one repo twice so two sessions share a name, then mention it. **Nothing should send.** A note under the box names both candidates *with their folders*, and your draft stays put. Now mention it by the session id from that note — that should send, with context. **(4)** YOURSELF: in B type `note to @B` and send — your own session is never resolved, so it goes as typed. **(5)** KEEP TYPING: type `ask @A about the cache`, press **Enter**, and immediately keep typing a few more words. The extra words must still be in the box afterwards. **(6)** TERMINAL MODE: repeat (1) in a session running in Terminal rather than Direct — the context arrives as one pasted block. **(7)** TIMING, EXPECTED NOT A BUG: mention a session the instant its reply appears and you may get "has not produced any readable output yet" — its conversation has not reached disk yet. A second later it works. | Automated: the finder and prompt builder as pure tables (longest name wins even when the shorter one also ends legally, names with spaces and apostrophes including the typographic one, code fences and inline code, email addresses, `@` at end, case, the typed-vs-list spelling, no second mention inside a matched one, the total size cap and its boundary); the resolver against a **real** `SessionQueries` and real transcripts (attribution, #764's wording, the fence, no-transcript, own session, renamed-away, ids, two sessions differing only by case, ambiguity); the IPC handler (argument checks never reach the query core, a throw becomes `null`, the capability, the preload's channel); the composer's send path (refusal keeps the draft, failed lookup fails open, slash commands and forwarded messages never resolved, the send guard released, held sibling messages forwarded once, text typed during the lookup kept, a trimmed trailing space still clears the box); and an e2e driving two real Direct sessions end to end. Two mutation rounds, 46 mutants — every survivor killed except three equivalents, documented. What a machine cannot judge: whether the injected block reads as *context* rather than as something you said, at a glance and in all four themes; and (6), which has no automated coverage at all |
| **Mention another session by typing `@` in the message box (#797)** | **RELEASED in v0.8.90 (2026-09-16) — ready to hand-test.** Open two or three sessions in different folders first. **(1)** In one session's message box, type `@`: the others appear with their colours, and **the session you're typing in is not listed**. **(2)** Type a few letters of a name: the list narrows. Press **Tab**: `@ThatSession ` goes into the box with the cursor after it, and nothing is sent. **(3)** Mid-sentence: type `please look at @`, then pick a session. The words before the `@` are still there. **(4)** Type a session's **whole** name after `@` and press **Enter**: the message sends straight away rather than "completing" to itself. **(5)** THE SUBSTRING RULE: with a session named, say, `TradingApp`, type `ping @app` and press **Enter**. It sends `ping @app` exactly as typed and must NOT turn into `@TradingApp`. Then retype, press **↓** first, then **Enter**: now it completes to `@TradingApp`. **(6)** **Esc** closes the list; typing more of the same word keeps it closed, and a new `@` further along opens it again. **(7)** Click into the middle of an `@name` you already wrote and press **Enter**: it just sends, and nothing gets replaced. **(8)** An email address (`dan@example.com`) never opens a list and is sent unchanged. **(9)** A session whose process has ended is still listed, marked **exited**. **(10)** The `/` command list still behaves as before, in all four themes | Automated: the pure trigger and list rules (every boundary and name-ending character, the caret must be at the END of the word, prefix-first ranking, own session out, exited kept, the Enter action table), the channel's handler and read-only capability, `accentColor` present/absent, and component tests through the real composer (list, one fetch per opening, colour dot and fallback, Tab caret position, full-name Enter sends, substring Enter sends the literal, moved-to-row and hovered-row complete, sticky Esc and its forgetting, caret inside a mention, Enter/Tab held while the list loads, refused and rejected reads, no stale list on reopen, highlight reset, `/(@x` precedence). Two mutation rounds, every survivor now killed. Since #798 the mention also brings that session's recent work with it at send — test the two together. What a machine cannot judge: whether the popup's colour dot and **exited** tag read clearly at 10–11px in all four themes |
| **Token counts on the card stop counting each reply several times (#807)** | **RELEASED in v0.8.90 (2026-09-16) — ready to hand-test.** **(1)** THE BIG ONE: open a session that has been running a while, ideally one that used a helper agent. `↑`, `↓` and `⛁` on the card header should be noticeably **smaller** than you remember, often by half or more. The `~$` estimate built from them shrinks by the same factor. **(2)** In that session's **Terminal** tab type `/exit`. The `~` estimate becomes Claude Code's own `$` figure. The two should now be close; before this fix the estimate could be several times higher. **(3)** Ask a session to start a helper agent ("use a subagent to count the files here"). While it works, `↓` should climb steadily and never jump by thousands per step. **(4)** Restart switchboard with a card open. After that card's next reply its figures should be at the corrected size, not the old inflated one. **(5)** A session that never uses a helper agent: its tokens-in and cache figures shrink too (every reply was counted once per content block), but less dramatically | Automated: a real (sanitized) helper-agent transcript with growing copies, which failed on the old code with exactly the inflated totals; the reset on rebind; hostile values; the thinking breakdown replacing with its copy; two mutation rounds (14/15, then 13/14 — the one survivor is equivalent on every measured transcript). Measured against Claude Code's own ledger over 25 sessions: input/cache 0.98–1.013 (21 exact), output 0.97–1.00. ⚠️ **Not measured: a RESUMED conversation.** If a resumed card reads well above Claude Code's `/exit` figure, report it. That's the one case the corpus couldn't check. What a machine cannot judge: whether the smaller numbers match what `/cost` tells you inside the session |
| **The usage readout shows how much output was thinking (#789)** | **RELEASED in v0.8.90 (2026-09-16) — ready to hand-test.** **(1)** In an **Opus or Fable** session, ask something that needs reasoning. The card header's tokens-out figure should become `↓ <n> (<m> thinking)`, the parenthesised part fainter, `<m>` smaller than `<n>`. **(2)** Hover the thinking part: a percentage, and "already counted in the output figure". Hover `↓` itself: still "Output tokens". **(3)** Keep working: both climb together; the thinking figure is NEVER larger than the output one. **(4)** A Haiku session or a trivial no-reasoning prompt shows no `(… thinking)` at all. **(5)** A narrow card, all four themes: the extra text must not shove the branch or cost off the header, and the grey must be legible at 10px. **(6)** Restart switchboard: a card that showed a thinking figure shows it again. | Automated: per-line reader (11 hostile-JSON cases incl. the real `null` the CLI writes on `<synthetic>` lines), watcher accumulation (output stays 10, not 17), render guard + 1–99% clamping, estimate unchanged, strip text/tooltip against real `en.json`, drift detector reporting a rename or sibling of `thinking_tokens`, persistence on both write paths, and a 23-mutant round (23/23). What a machine cannot judge: whether the header still fits in a narrow card, and whether the faint grey reads. |
| **The Feed names which subagent is talking (#788)** | **RELEASED in v0.8.90 (2026-09-16) — ready to hand-test.** **(1)** In a **Terminal mode** session, ask Claude to do something that fans out to several agents at once ("use three parallel agents to …"). In the Session tab you should see their work indented behind a dashed line, and each stretch should open with a small grey caption like `Subagent · Explore`. **(2)** THE POINT OF THE ITEM: with two or more running at once their replies arrive mixed together, and a NEW caption must appear every time the speaker changes. If you see one long indented run with a single caption at the top, the grouping did not take. **(3)** If two of the helpers have the SAME name — e.g. two `deep-research-specialist`s — each caption should gain a short code (`· a3f9c1`) so you can tell them apart. Only then; a single helper of that name must NOT show a code. **(4)** The caption must line up exactly with the dashed line of the blocks under it — no half-pixel step. **(5)** Switch the detail chip to **quiet**: the helper work and its captions should vanish together. Back to **normal**: both return. **(6)** Check that "NEW PROMPT" no longer rules off a helper's *task* prompt — it should appear above YOUR prompts only. **(7)** A session that never spawns a helper must look exactly as it did before: no captions, nothing new. **(8)** A **Direct mode** session shows no helper work at all — that is expected, not a bug. | Automated: 51 unit/component tests + a 23-mutant round (23/23 killed) + an e2e case that writes two real interleaved subagent files and asserts three captions. What a machine cannot judge: whether the caption reads as a margin note rather than a heading at a dozen per turn, and whether the grey is legible in all four themes at 10px. |
| **Cost on the card: the rate table is corrected, and Claude Code's own figure supersedes the estimate at the end (#787)** | **RELEASED in v0.8.90 (2026-09-16) — ready to hand-test.** **(1)** Open any session and work in it until the header shows a cost. It reads `~$x.xx` with a tilde — that is our estimate. Hover: it must say it is an estimate, a floor, and that you are billed by subscription. **(2)** THE BIG ONE, and the reason this row exists: **the number itself should now be roughly 2.4× SMALLER than you remember for an Opus session.** The old table priced Opus at $15/$75 when it is $5/$25. If a long session still reads like it cost $200, the fix did not take. A Fable session moves the other way — about 2.6× *larger*. **(3)** THE EXACT FIGURE, and it only arrives one way: open the session's **Terminal** tab and type `/exit`. The tilde should disappear and the number should change to Claude Code's own — hover says so. **Restarting or closing the session from switchboard will NOT do this** (switchboard stops reading the transcript before the CLI writes its accounting), so do not test that path expecting it to work. **(4)** THE RESUME CASE, which is what the review round caught: after (3), resume that same conversation and send one more prompt. The tilde must come **back** — the exact figure was the total as of last time, and it must not sit there frozen while the session spends again. **(5)** `/clear` a session with a cost showing: tokens and cost must both go to zero together, and the cost must not keep the old conversation's number. **(6)** The status bar's workspace total should stay sane throughout and never read `$NaN`. **(7)** A brand-new card should read `$0.00`, not `<$0.01` | Automated: 40 unit tests + 23 mutants across two rounds, incl. a fixture that reconciles our arithmetic against the CLI's own figure exactly. What a machine cannot do: judge whether the `~` / `≥` markers and the three tooltips actually read as honest rather than fussy, and confirm the corrected number matches what `/cost` told you inside the session |
| **The Changes tab stops calling every problem "Not a git repository" (#785)** | **RELEASED in v0.8.90 (2026-09-16) — ready to hand-test.** The easiest trigger is a **damaged repository**, which you can make in ten seconds and throw away: create an empty folder, put a file in it called `.git` (a FILE, not a folder) containing the single line `gitdir: /nope`, open a session on it, and open its **Changes** tab. Expected: *"switchboard couldn't read this project's git — not a git repository: …"*, in the attention colour — **not** "Not a git repository" and **not** "Working tree clean". (What follows the colon depends on your git build: this machine's echoes `/nope`, the CI runners' print `(null)`. Either is correct.) **(2)** THE CONTROL, and do not skip it: open the Changes tab on an ordinary folder that is genuinely not a repository. It must still say plainly **Not a git repository**, in grey, with no reason and no alarm — that is the common case and turning it into a warning would be a worse bug than the one being fixed. **(3)** The card header: on the damaged folder, the little `⎇ branch ·N changed` line must be **absent**, not showing `⎇ ?` with no changes. **(4)** If you have a project using **Git Large File Storage installed for that project only** (`git lfs install --local`), open its Changes tab — this used to be the documented case that read as "not a repository", and should now quote git's own complaint about the filter. **(5)** The bus half: ask one session to look at a **damaged** sibling's changes. It should report a reason it can act on, rather than "it is not working inside a git repository". **(6)** Judgement, and the only thing a machine cannot do here: read the reason sentences in `docs/manual/08-changes-and-git.md` and say whether they read as something you could act on, or as jargon — half of them are git's own words quoted back, which is deliberate and may or may not land | Automated: 21 cases driving **real git** against real damaged repositories (a `.git` file pointing nowhere, an unparseable one, a corrupt config, a missing folder, a bare repo), plus stand-ins for the cases real git cannot be made to produce portably. **28 mutants across four rounds, all dead**, including one e2e mutant that proves the sentence reaches glass — nothing else renders that string, so swapping it left 7,758 unit tests green while the fix silently reverted. ⚠️ **Review found a blocker inside the fix, and it is why item 1 is worth doing on a slow or network drive if you have one:** a capability probe that merely *stalled* was remembered as "your git is too old", app-wide, for the life of the process — so one hiccup would have told you to upgrade a git that was fine, on every project, until you restarted. Now reported as "could not check" and forgotten. ⚠️ **CI then found a second one the dev machine could not show:** on Linux a folder that turned out to be a file made the whole read *reject* rather than answer, so the pane would have gone blank instead of explaining. **What no machine covers is item 6** |
| **A backgrounded conversation no longer freezes the Session view (#790)** | **RELEASED in v0.8.90 (2026-09-16) — ready to hand-test.** Open a **Direct** session, send a prompt so it has some history, then use Claude Code's own "push this conversation into the background" gesture inside the card. Expected: the Session view keeps showing the conversation rather than going silent, and **Help → Open log folder** holds `conversation continued elsewhere — rebinding` at info, naming both ids. ⚠️ **The trigger itself is the part nobody has reproduced** — see the note, and treat "I could not make it happen" as a useful result to report, not a failed test | Automated: 9 cases, 14 mutants (13 kill, 1 documented survivor). ⚠️ **The `continued-in` record has never been seen on this machine** — zero across ~3,300 transcripts — and four command-line variants (`--bg`, `--bg --fork-session`, the documented "starts a copy", `claude stop`) each failed to produce one. The shape comes from the CLI binary's own code, not a capture: `spike/findings/e11-790-continued-in.md`. So a hand-test that cannot trigger it confirms the probes rather than contradicting them |
| **Nothing to click, one log line if you insist (#753)** | **RELEASED in v0.8.90 (2026-09-16) — ready to hand-test.** Internal: switchboard now believes Claude Code when it says it cleared a conversation, instead of working it out from the conversation's identifier changing. No screen, button or behaviour changes, and the "Conversation cleared" marker in the feed was never affected — it comes from somewhere else. If you want to see it: open a **Direct** session, quit and relaunch so the session is restored, run `/clear` **before** sending any prompt, then **Help → Open log folder**. You should find `conversation cleared — rebinding to the new transcript` at info. The old line was `transcript mis-bind corrected (same-cwd race)` at **warn**, describing a problem that was not happening. Note this may well have looked right before the change too, for a reason only found in review — see #793 | Automated: 45 cases, 7 mutants (6 kill, 1 documented survivor). The one real bug this cannot cover is **#793**, filed rather than hidden: a hook can land the new id untagged first, and that race is unmeasured |
| **Nothing to click (#779)** | **There is nothing for you to test by hand, and that is the honest answer rather than a gap.** This item only changed switchboard's written record of what Claude Code puts in a session's history file, so that the app stops logging warnings about fields it should already know about. No screen, button or behaviour changes. If you want to see it anyway: **Help → Open log folder**, work a session for a few minutes, and search the log for `schema drift` — you should find nothing. Finding something is genuinely interesting news (a Claude Code release changed the format again) and worth a ticket, not a bug in this change | Automated: 49 cases, 35 mutants, plus two committed probes that measure the whole 246,000-line transcript corpus on this machine and re-run against any later CLI (`spike/probes/779/`). The real gate is `npm run check:transcripts`, which drives the installed CLI and reports drift — it says clean against 2.1.261 |
| **The Changes tab still works, on every project you have (#776)** | **RELEASED in v0.8.90 (2026-09-16) — ready to hand-test.** This is a hardening change with *no new behaviour*, so the whole test is "nothing got worse", and it wants breadth rather than depth: open the **Changes** tab on as many different projects as you can — this repo, BrainHarbor, anything with submodules, anything on a network drive, and **anything using Git Large File Storage if you have one**. Each should list exactly what `git status` lists in a terminal in the same folder, no more and no less. The failure to watch for is an LFS project suddenly showing every large file as modified, or the tab going empty on a project where git works fine from the command line. Then ask one session to look at another session's changes and confirm the diff still comes back | Automated: ~60 cases driving real git — seven ways a repository can try to run a command, on both read paths, each with its own control proving plain git *does* run it, plus submodules, a redirected submodule git directory, and a linked worktree — over 30 more on the argument building, and 27 mutants. What a machine cannot cover is the variety of real repositories on your disks; that is the whole of this row |
| **A waiting-message mark in the sessions rail (#774)** | Open two sessions in different folders. Ask session A to send session B a message. **(1)** Without touching B, its row in the left-hand list should show a **1**; open B and the count goes when you press Enter or dismiss. **(2)** Repeat with B **collapsed**, or sitting on its Terminal tab — the row count should still appear; this is the case the whole item is for. Check the number is legible in your theme and does not crowd the status glyph or make the row jump. **(3)** Put both in a group, collapse the group, send B a message: the heading should read **1 waiting** beside the needs-you summary instead of **calm**. **(4)** Send two or three and confirm the counts climb — and that nothing flashes, no sound plays, and the window does not come forward. That last one is deliberate and is the judgement call worth vetoing if you disagree. | ⚠️ **UNTESTED** — released in v0.8.90 (2026-09-16). |
| **One send at a time from a prompt box (#774)** | Attach a screenshot to a session's prompt box and hammer **Enter** two or three times: it should send once, with the Send button greyed until it lands. Then, with a send in flight, switch that card to its Terminal tab and back and press Enter again — still one send. If a sibling's message was waiting, confirm it was passed on only once. | ⚠️ **UNTESTED** — released in v0.8.90 (2026-09-16). |
| **“That session was closed” instead of “it is waiting for you” (#774)** | Ask A to send B a message and close B immediately. A should be told B was closed and that sending again will not help. The window is small and hard to hit by hand — if you cannot land it, leave it; the automated tests cover the refusal itself. | ⚠️ **UNTESTED** — released in v0.8.90 (2026-09-16). |
| **Sessions can see each other — a "switchboard" MCP server in every session (#763)** | **The one thing to check hardest is the negative: YOUR OWN MCP SERVERS MUST STILL BE THERE.** In a **Direct** session open `/mcp` and confirm you see **DeepWiki** (or whatever you have configured) **and** a new read-only row called **switchboard**, connected. On the work laptop that means your claude.ai connectors too — the laptop is the only machine with a real connector set, so it is the only place the merge is tested at any scale. If ANY server you had is missing, stop and tell me immediately: that is the failure mode this whole item was built to avoid. **(2)** The thing the automation cannot do at all, because it needs a model: in a session, ask *"what other sessions am I running?"* with two or three other sessions open in different folders. It should name them, with their folders and whether each is working or idle — without you telling it the tool exists. **(3)** Check it names the RIGHT ones: it should list your other cards, not itself twice, and the names should be the card titles you see in the rail. **(4)** The read-only row: confirm **switchboard** has no Remove button, and say whether having an unremovable row you did not add is annoying enough to want it hidden instead — that was a judgement call I made rather than asked about, and it is one filter line to reverse. **(5)** Restart a session (**⋯ → Restart**) and confirm the row comes back connected rather than going stale. **(6)** A **Terminal**-mode session: it gets the server too, so `/mcp` typed in the terminal should list it. | ⚠️ **UNTESTED. RELEASED in v0.8.8** (2026-09-08) — installable now. Automated: 56 new tests. `npm run check:mcp-attach` drives the **real logged-in CLI** and proves the whole chain without a model — our server `connected`, our `serverInfo`, our tool listed, and **DeepWiki still present alongside it**. The eviction flag `--strict-mcp-config` is asserted absent across the whole spawn matrix AND by a source-level guard that fails if the string appears in any shipping file (it evicts every server you configured — measured, three runs). A provider without the capability spawns **byte-identically**, compared against literals. ⚠️ **Review caught a real leak before this shipped:** a session that failed to start (no CLI on PATH, a failed spawn) left a listening socket and a live credential behind for the life of the app — five failed starts, five leaks — and the code comments claimed that could not happen. Fixed with an undo on the failure path, pinned by tests that fail without it. **What no test can judge is items 2, 3 and 4** — whether an agent finds and uses the tool unprompted, and whether the extra row is worth its cost |
| **A session can read another session's recent work and its uncommitted diff (#764)** | **Do the #763 row above first — this builds straight on it, and if the switchboard server is not connected none of this can work.** Setup: two or three sessions open in **different** folders, at least one of them a git repo with uncommitted changes and a conversation with some history in it. **(1)** In session A, ask *"what has \<the other session's name\> been working on?"* — it should go and read, and come back with a recognisable account of that session's recent conversation. Check it against what is actually on that other card. **(2)** The one that pays for the whole item: in A, ask *"look at the uncommitted changes in \<other session\> and tell me if anything looks wrong"* — it should produce a real review of real edits it has never been shown. Compare against `git diff` in that folder. **(3)** Ask about a session that is **not open** ("what has Homebrew been doing?"). It must say **no such session** and list the ones that are — the failure mode to watch for is a confident *"it hasn't done anything"*, which is the answer this item was built to make impossible. **(4)** Two sessions with the **same name** (two checkouts of one repo): asking about that name must report the ambiguity and show both, not silently pick one. **(5)** A **long** conversation: ask for its output and confirm the session tells you it only got the recent part rather than presenting it as the whole story. **(6)** A session whose folder is **not** a git repo: asking for its diff must say *not a repository*, not *no changes*. **(7)** Judge the wording — these strings are read by a model, not by you, so say whether the answers it gives back sound like it understood what it was handed | ⚠️ **UNTESTED — released in v0.8.90 (2026-09-16); this row is ready to run.** Automated: `npm run check:bus` now drives the **real compiled bus server over real pipes against the real query core**, over a real temp git repo and a real transcript — so items 5 and 6 and the caps are machine-proven, and the "unknown session is an error, not an empty success" rule in item 3 is asserted end to end. Plus 40-odd unit tests, five of them mutation-verified by applying the mutant and watching it go red. ⚠️ **One of my own new tests was decoration and the mutant caught it** — it asserted "nothing was logged as an error", which stayed true with the bug in as well as out; it now pins the branch itself. **What no machine can do is any of items 1, 2, 3, 4 or 7, because every one of them needs a model to actually call the tool** — `mcp_status` proves the server is reachable for free, but a tool call costs a turn, so it can never be a CI job. Item 2 is the one to spend time on |
| **Sessions can send each other messages — held in the prompt box until you press Enter (#765)** | **Needs the #763 row working first.** Setup: two **Direct** sessions, A and B, in different folders. **(1)** In A, ask it to *"send B a message asking it to list the files in its folder"*. B's prompt box should show a highlighted block **From @A** with the message — and **B must do nothing**: no reply, still idle. A should say the message is waiting in B's box and has **not** been sent. **This is the safety property; if B acts on its own, stop and tell me.** **(2)** Click into B's box and press **Enter** with nothing typed: B gets it and answers; its conversation shows the message with a header naming A and saying you passed it on. Repeat with some words typed — they should go after the message, in one prompt. **(3)** Send another; click the **×** on the block — it goes away and nothing is sent. **(4)** B's **⋯** → tick **Accept messages from other sessions automatically**; send from A again — B should act straight away, and A should say it was sent. Untick it afterwards. **(5)** The loop check: tell A and B *each* "whenever the other session messages you, reply to it". With the box unticked the exchange must stop at one message sitting in a box. (Optional, costs tokens: tick it on BOTH — it should stop after ~5 each way, with the next message waiting for you.) **(6)** Switch B to its **Terminal** tab (or collapse it), send from A — A should be told B's conversation isn't on screen, and the block should be there when you go back. **(7)** A **Terminal-mode** B with the box ticked: the message should still wait in the box, and A should be told why | ⚠️ **UNTESTED — released in v0.8.90 (2026-09-16); this row is ready to run.** Automated: the non-submission is asserted directly at four levels — the delivery policy (calls to submit counted, never text inspected), the composer (no call to either send route until Enter), `check:bus` through the real compiled bus child and pipe, and an e2e through the real app's wiring. The loop is **exercised**, not argued: two simulated agents that answer every message — one hop with defaults, bounded at 11 sends with both boxes ticked. 18 mutants applied across two review rounds, all went red. ⚠️ **Review found a real hole before this merged:** a message could carry hidden terminal escape codes, so in a Terminal-mode session your ONE Enter on text that looked harmless would have typed extra keys — now refused, along with invisible characters that read differently to a model than to you. **Extra things worth a glance:** a message that appears a split second before you press Enter on your own prompt should stay in the box (it waits one second on screen); and the **Session** tab shows a number while messages wait. **What no machine can do is 1, 4 and 5 with a real model** — whether an agent uses the tool sensibly and heeds "don't wait for a reply", and whether the block reads well |
| **Reading another session never locks its git, and a pile-up of reads is turned away (#772)** | **RELEASED in v0.8.90 (2026-09-16) — ride along with the #764 row, don't do it on its own.** **(1)** While session A is reading B's changes (the #764 row's step 2), have B stage or commit something at the same moment — ask B to *"git add everything and commit it"* right as A starts reading. B's commit must **never** fail with *"index.lock: File exists"*. Hard to hit on purpose; mainly: if you ever see that message in a session while another one is looking at it, report it. **(2)** Optional: ask A to *"use four or five subagents at once to each read what B has been doing"*. Any that are turned away should say to **wait for one of the others to finish** — not "switchboard is unreachable", and the app should not stutter while they run. | Internal-heavy: the index-lock fix, the four-at-once limit, the ten-second git limit and the smaller transcript reads are all pinned by unit tests with their mutants applied, and `check:bus` runs the new diff against a real repo. What only you can judge is (2)'s "does the app stutter" — on the laptop, not the desktop. |
| **Replies are formatted as they arrive, instead of showing raw markdown until the turn ends (#635)** | **This is your report: "it just shows the raw markdown as it scrolls up, and then once it's done, it renders it."** Needs a **Direct** session (Terminal mode waits for whole messages and always did). **(1)** Ask for something long and structured — "explain X with headings, a bulleted list, a table and a code example" — and watch it arrive. Headings, **bold**, bullets and code blocks should be formatted **as they stream**, and there should be **no moment at the end where the whole reply re-flows**. **(2)** The thing you are best placed to judge and no test can: **is the settling distracting?** A word goes plain → bold as the closing `**` lands, and a line becomes a table header only when the row beneath it arrives. Some of that is unavoidable; say if it reads as busy rather than as live. **(3)** Watch a **code block** specifically: the code should appear as it is written, but the **Copy button only appears when the reply finishes** — deliberate, because copying a half-written command would put half a command on your clipboard. Confirm the button then works. **(4)** The block cursor: it should sit at the end of the text while Claude writes, and inside the code box when the reply is mid-fence. **(5)** THE RISKIEST ONE — **scrolling.** With a session streaming a long reply, confirm the view still sticks to the bottom; then scroll **up** mid-reply and confirm it does **not** yank you back down; then `↓ Jump to latest` and confirm it re-sticks. Do it again in a **2×2 split with two sessions replying at once**. **(6)** Performance, on the **work laptop** rather than the desktop: with several sessions replying simultaneously, does typing in the prompt box stay responsive? **(7)** Prose that merely *looks* like markdown must come through untouched — ask for a reply containing `[1]`, `2 ** 8`, `**/*.ts` and `array[0]`; none of it should sprout stray brackets or asterisks | ⚠️ **UNTESTED. RELEASED in v0.8.8** (2026-09-08) — installable now. Automated: **all 17 mutations caught**; e2e proves markup renders *while the turn is still running* off deltas that never reach the transcript, and `feed-tail-pin` / `feed-restore-position` stay green. **Both of the ticket's premises were measured and found wrong:** an unclosed code fence needs no auto-closing (`marked` terminates it at EOF), and the cost needs no window cap — median assistant block is **100 characters** and the whole pipeline costs **0.07 ms** on it (corpus: 8,846 transcripts, 19,589 blocks). ⚠️ **Review caught three real bugs, and item 7 plus the dev-mode note are what they became.** The worst: under React StrictMode the block **froze after its first chunk** and only unfroze at end of turn — dev-only, but that is the owner's own environment, and it would have been *worse* than the bug being fixed. Second: the repair mangled ordinary prose (`see the note [1] below` → `see the note [1] below](#)))`) because every test case was a constructed partial and none was plain English — item 7 is that gap. Third: CRLF drifted the token offsets. **What no test can judge is items 2, 5 and 6 — whether the settling is distracting, whether scrolling still feels right, and whether the laptop keeps up** |
| **Click the model name at the bottom of a session to switch it in one click (#747)** | **This is the express lane you asked for, and the thing that pays for the extra click #746 added — judge whether it does.** In a **Direct** session that has replied at least once, click the small grey model name at the bottom, next to the autonomy chip. A menu should open — **upward**, if the card is near the bottom of the window — with a ✓ on the model you are on. Click a different one: it should apply **immediately**, the menu should close, and the name on the button should become the model you picked, with no OK anywhere. **Then the check no machine can make: send a prompt and confirm the reply really comes from that model** — only the real CLI proves `set_model` took effect rather than being merely acknowledged. Second, the placement, which is the reason this does not use the anchoring the ⋯ menu uses (#695): open it on a card in the **top** row of a 2×2 split — the menu should open **downward** and must not cover the button you just clicked — and again on a card at the very bottom, where it should open upward. **Third, the popout, which is the case most likely to be wrong:** pop a session out with **⤢**, click the model name in the popped-out window, and confirm the menu appears **in that window**, anchored to that button, and switches **that** session. Fourth, the fresh card: open a brand-new session that has not replied and confirm the button reads **model?**, opens anyway, ticks nothing, and explains why — then pick one and confirm it takes. Fifth, the one that must NOT be a button: switch a session to **Terminal** mode from **⋯** and confirm the model name there is **plain text**, with a tooltip pointing at `/model` in the Terminal tab. Do the same on a **stopped** session — also plain text, and its tooltip must NOT mention a Terminal tab it never had. Finally the feel: `Esc`, clicking away, `Tab`, and clicking the button a second time should all just close it, and the arrow keys should walk the rows. **Say whether one click with no confirmation is right here, given the picker deliberately asks** | ⚠️ **UNTESTED. RELEASED in v0.8.8** (2026-09-08) — installable now. Automated: 32 tests, **all 25 mutations caught**. Pinned — one click is exactly one `set_model` carrying the model's id rather than its display name; clicking the model it already runs sends **nothing** (a no-op switch would make "it did something" indistinguishable from "it didn't"); a refusal **holds the menu open** in the CLI's own words with nothing moved; exactly one row ticks on the default model (`default` and `opus[1m]` share a resolved id — a per-row test lights both); the menu never asks main which model is current, because the button already knows; Terminal mode and a dead session both keep the plain span. ⚠️ **Review caught two blockers before this shipped, and they are what items 3 and 5 are really checking.** First, the button was a way to tear the menu down **while a switch was still on the wire** — reachable by keyboard — which would have lost the CLI's refusal silently and left the session on the old model while you believed it had changed. It now goes dead, visibly, until the answer lands. Second, a session **ending or restarting** under an open menu left the menu floating, aimed at a session id that no longer resolved. **What no test can judge is the feel of one-click-no-confirm, or whether the menu lands in the right place on a real screen** — items 1, 2, 3 and the last one |
| **The model name at the bottom of a session updates the moment you switch (#746 part 1)** | **This is the bug you reported on 2026-09-03.** Open a session that has replied at least once (so it has a model to show — a brand-new card has not told anyone yet, which is by design), note the small grey model name along the bottom, then `/model`, pick a different one, press OK. **The name at the bottom must change immediately** — not on the session's next reply, which is what it used to wait for. Then the second half of the same claim: open a session that has been running a while and confirm the name is there at all, without you switching anything. Third, the one that would be a regression rather than a miss: the **cost** figure and token counts must still behave exactly as before — those are deliberately still fed from the conversation itself, not from the picker, because they are about what actually ran | ⚠️ **UNTESTED. RELEASED in v0.8.8** (2026-09-08) — installable now. Automated: the footer moves on the push and does so without a new turn (mutation-verified — putting the old wiring back shows **the stale model winning over the new one**); a card mounting mid-session pulls the model it missed; a session that changes nothing announces nothing (the init arrives every turn, so an unconditional push would be one message and one repaint per turn per session); a refused switch pushes **nothing**; and a usage snapshot can no longer overwrite a model the picker has reported, which is the bug itself. Merged 2026-09-03 as PR #750 (`28ef273`). ⚠️ **Review caught a blocker before this shipped, and it is the thing to check hardest:** the first cut read ONLY the picker's source, which would have made the model name **vanish entirely** on Terminal-mode sessions and on any session that had not replied yet — the picker's source is fed only by Direct-mode traffic. So please confirm the name is present on a **Terminal-mode** session too, and on a **resumed** card the moment it appears |
| **The model picker asks you to confirm — select, then OK (#746 part 2)** | **You asked for this shape; only you can say whether it lands.** `/model`, click a model — it should mark your choice and **nothing should happen yet**. Press **Cancel**: the session must be exactly as it was, no switch, nothing to undo. Do it again and press **Esc**, then again and click outside the dialog — all three must behave identically to Cancel. Then click a model and press **OK**: it switches and the dialog closes. **Judge the trade honestly**: this is one more click than before on a thing you may do often, which is exactly why the footer quick-switch (#747) exists as the express lane — say whether the dialog now feels too slow on its own, and whether closing-on-success is enough confirmation or you want a message first. Also check the small stuff: OK should be dead until you have actually chosen something different, and picking the model the session is already on should leave it dead | ⚠️ **UNTESTED. RELEASED in v0.8.8** (2026-09-08) — installable now. Automated: a row click sends nothing (mutation-verified — restoring apply-on-click puts it back on the wire and reddens five tests), OK sends exactly once and sends the model's id rather than its display name, Cancel/Esc/click-away all send nothing, OK is disabled until the choice would change something, a refusal keeps the dialog open in the CLI's own words with the selection intact, and a second press cannot put two switches on the wire. **What no test can judge is whether the extra click is worth it** — that is the whole of item 1 |
| **Opening a session with a long history no longer freezes the app (#742, bug 2 of #719's 5)** | **You need a genuinely big conversation for this — a short one never showed the problem.** Open a session on a folder whose conversation has thousands of messages in it (this repo and BrainHarbor both qualify), and watch the moment it picks the conversation up. Before this, the whole app went unresponsive for about a quarter of a second as it swallowed the file: terminals stopped printing, the window stopped repainting. It should now come up without any stall you can feel. **The regression that would matter most is the one to check hardest: the conversation must still come back COMPLETE and from the beginning.** Scroll right up to the top of that session and confirm the history is all there — that replay is deliberate (it's what fixed resumed cards showing an empty Session view) and this change was only allowed to alter *when* the lines arrive, never which ones. Second, open several big sessions at once, or relaunch with a workspace full of them, and confirm the app stays usable the whole way through rather than freezing once per session. Third, and this one is a judgement call only you can make: a very long history now fills in over a few hundred milliseconds instead of appearing all at once — say whether that reads as the app working or as the app being slow. | ⚠️ **UNTESTED. RELEASED in v0.8.8** (2026-09-08), merged as PR #749 (`958ac42`) — installable now. Automated: 12 tests, every one mutation-verified red first, with the defect's own numbers — putting the bug back reads all 200 lines inside a single tick and the event loop gets **zero** turns across the whole replay. Also pinned: the sliced replay produces a byte-identical snapshot to the unsliced one; a multi-byte character (é / 日本語 / 😀) split across a slice boundary is not corrupted; a JSON line split across a boundary parses exactly once; a poll tick landing mid-replay neither duplicates nor skips (removing the guard reads 24 lines where 4 were held); a session that dies mid-replay still gets its last words (removing that reads **6 lines of 100**); and a stopped watcher runs no further slices. **Measured, not estimated: 269.5 ms of frozen main thread on the 33.22 MB / 11,295-line transcript, and that figure excludes the per-line work entirely.** ⚠️ **Known limit, deliberate:** a session that dies *while* its history is still loading still pays the old freeze once, because the alternative is discarding transcript you can never get back |
| **switchboard stops using more and more CPU the longer it's open (#719, bugs 1+3 of 5)** | **This is the row for the incident that cost you a forced reboot.** It cannot be checked in a minute — the whole defect was that cost grew with UPTIME, so the test is *leave it running*. On the work laptop, open your usual set of sessions and **leave switchboard up for a full working day**, ideally longer, then look at its CPU in Task Manager while sessions are idle. It should sit near nothing. **The specific thing to watch for is the shape from the report: CPU that is unremarkable for the first few hours and creeps up over a day**, and the app burning hard while MINIMIZED. Both should be gone. Second check, cheaper: with several sessions actively replying at once, watch CPU during the replies — it should not spike in proportion to how many conversation files are being written. Third, and this is the regression that would matter most: **start a brand-new session and confirm it still finds its conversation as fast as it always did** — that is the thing this change traded against, and if a session now takes visibly longer to come alive, say so immediately. **Two caveats.** This is 2 of the 5 bugs in that report — the other three are filed (#742, #743, #744) and not fixed, so a recurrence is not a surprise and IS worth telling me about. And **the spinning line was never found**: this removes a large amount of measured pointless work, which is not the same as proving it was the cause. | ⚠️ **UNTESTED. RELEASED in v0.8.7** (2026-09-01) — installable now. Automated: the discovery ladder now climbs through an append storm (mutation-verified — putting the defect back gives **40 sweeps per 10 seconds instead of 6**), a busy turn costs a bounded number of sweeps, the conversation-head memo is not re-read every sweep (mutation-verified — **64 quarter-megabyte reads where there should be 0**), and the memo invalidates when a file gains its first identifying line (mutation-verified — keying it by name alone means a session **never binds at all**, which would be a worse bug than the one being fixed). The fail-open fence — a brand-new conversation file is still noticed immediately — is asserted and passed before AND after, deliberately. **What no test can do is run for 26 hours on your laptop**, which is the entire claim |
| **Typing in the prompt box lags less (#716) — HALF a fix** | ⚠️ **EXPECT THIS TO BE BETTER, NOT FIXED, AND SAY WHICH.** Only the composer half shipped; the feed half was built, failed CI and was reverted (see the note), so the honest prediction is: **clearly better on the desktop, possibly still heavy on the laptop in a long session.** Your answer decides whether #716 closes. Open the **longest** session you have — this does nothing measurable on a short one, because the cost it removes was priced by conversation length. Type a couple of sentences at your normal speed and say whether characters appear **as you press them**. Then: (1) with a **long draft** already in the box — paste a paragraph or two so the box is at its twelve-line cap, then keep typing at the bottom of it; (2) while the session is **actively working**; (3) scrolled **up** into the history rather than at the tail. Then confirm the box itself still behaves: it should still grow as you type, still stop at twelve lines and scroll inside itself, still shrink back when you delete, and still give the conversation its room back. **Typing at the bottom of a full box must not jump you to the top of your own draft** — that hazard is now structurally impossible rather than guarded against, so if you see it, something is badly wrong. Finally, on the desktop, compare a long session against a fresh one: typing should now feel the **same** in both, which is the specific thing that changed. | ⚠️ **UNTESTED and deliberately INCOMPLETE. RELEASED in v0.8.7** (2026-09-01) — installable now, and this row is finally answerable. ⚠️ **2026-08-31, owner, work laptop: typing still sluggish — but he was on v0.8.6, which does NOT contain this fix. That is a BASELINE confirmation, not a test of it.** Do not read it as "the fix didn't work"; the fix was not present. **Install v0.8.7 first, then re-run this row** — his answer is what decides whether #716 closes or waits for #740. Automated: the arithmetic that decides where the box stops, the structural guard that typing reads no layout and writes no height (the regression that would bring the lag straight back), and — unchanged on purpose, as the evidence behaviour survived — the real-engine e2e that grows the box with wrapped text, caps it at twelve lines on both branches, shrinks it back and re-fits it on window shrink. **Measured at 400 turns with a long draft: unthrottled, median keystroke latency 80ms → 40ms and the stalls went 58 → 0 — typing now costs what an EMPTY conversation costs. Under a 4× CPU throttle (the laptop proxy) there is NO measured improvement: 152ms/key either way.** The reverted feed half took that to 56ms and 0 stalls, and is what the follow-up item is for. **So the laptop is the real question and only you can answer it** — if it still bursts for you, that is expected rather than a surprise, and it means the follow-up has to land before #716 closes. **ANSWERED 2026-09-16, owner, work laptop on v0.8.8 (contains the composer half): STILL BURSTS** — ~10 characters flush at once, then a pause while he keeps typing. The expected incomplete-fix outcome, now confirmed on real hardware: **#740 (the feed half) is the operative ticket; #716 stays open until it lands.** Both issues annotated same day |
| **Auto-trust actually applies now (#724)** | Set the title-bar chip to **🔓 auto-trust** (the default), then start a **Terminal**-mode session in a folder Claude Code has never run in — auto-trust only has a visible effect there, because Direct mode never gets asked the trust question at all. You should go straight to the composer with **no trust prompt**. **The case this fixes needs a lower-case drive letter**, which Windows produces readily: open a folder by dragging it in from Explorer, or via a shell integration, rather than through the folder picker — before this, that wrote the trust flag under `c:/...` while Claude Code read `C:/...`, so the prompt appeared anyway and kept appearing on every later run. Do it twice on the same folder: the second run is where the old bug was worst, because our own phantom entry made switchboard think it had already succeeded. **Then the diagnostic one:** this repo is one of five folders on the dev desktop that already has TWO entries in `~/.claude.json` under different drive-letter cases. Run a Terminal session here and check the log for `folder has more than one entry in the CLI config` — it should appear once, with both spellings named, and both should end up trusted. | ⚠️ **UNTESTED. RELEASED in v0.8.7** (2026-09-01) — on `main` as #738 (`f4ff920`), installable now. v0.8.6 did NOT contain it, which is why this row could not be run before today. Automated: the key lookup (reuse an existing spelling, invent one only when nothing matches, never add a third), the second-run trap, the array/malformed-config guards, and — the one review caught — that two case-different spellings are confirmed by RESOLUTION rather than spelling, because a `//wsl.localhost/...` UNC path is a win32 path over a case-SENSITIVE backend and trusting both would hand the hooks-enabling flag to a directory you never opened. Measured on this machine; the live config already carries such a key. **What no test can do is prove the real `claude` binary skips its trust prompt on your disk** — that is items 1 and 2. |
| **Sign in to an MCP connector, and sign out again (#734)** | ⚠️ **THE WORK LAPTOP IS THE ONLY MACHINE THAT CAN TEST THIS AT ALL.** The dev machine has no claude.ai connectors, so the entire success path was built without ever being seen — only the CLI's *refusals* are measured. On the laptop, open `/mcp` in a **Direct** session and find a connector row. If any row says **needs sign-in**, it should now have a **Sign in** button beside it — before this it said the words and offered nothing. Click it and report what actually happens: does a browser window open? **Then leave the panel open and watch the row for a full minute** — it keeps re-checking for about that long and should update itself when the sign-in lands, without you closing anything. Say whether it actually does. **Three specific outcomes worth telling apart, because we could not:** (a) it works and the row changes — say how long it took; (b) you get *"Claude Code hasn't answered yet…"* after about ten seconds — that is the control channel's timeout, it is EXPECTED if Claude Code waits for the browser flow, and the question is whether the row still updates afterwards; (c) nothing happens at all — a real result, worth a ticket, not you doing it wrong. Next, **Sign out**, which only appears on rows that are already not working (needs-sign-in, not-connecting, unknown) — never on a connected one, deliberately, because we cannot tell an OAuth connector from a server using an API-key header and signing the wrong one out would break it with no proven way back. Confirm it really does clear the sign-in rather than just saying so. Then the negative case, which IS measured: a stdio row (`claude mcp add selftest -- claude mcp serve`, then reopen) must show **neither** button, because the CLI refuses both by type. Finally the clutter judgement: a row can now carry Sign in, Sign out, Turn off and Reconnect at once — say whether that is too many. Clean up with `claude mcp remove selftest -s local` | ⛔ **UNTESTED — and the 2026-08-31 laptop trip did NOT cover it either.** Dan confirmed the panel lists every server; he did not click Sign in or Sign out. Still untestable anywhere but the laptop, and still the only path in the app whose success case has never once run. **RELEASED in v0.8.6** (2026-08-30), as #736. Automated: the transport reader (including the regression that started this — an absent `config` must report `unknown`, never `stdio`, or every connector gets labelled as the one transport that cannot authenticate), the full sign-in/sign-out rule table, both IPC gates on both channels, the request builders, the post-sign-in watch window (mutation-checked — review caught the panel promising a self-updating list it did not actually deliver), and — the one that matters most — an explicit test that `ok: true` is **never** rendered as "Signed in", because that payload has never been observed. **What no test here can do is exercise a single real OAuth flow.** Outcomes (a), (b) and (c) in the how-to-test column are that gap, and (b) in particular is a guess about the CLI's own behaviour that only your laptop can settle |
| **Turn MCP servers off/on and reconnect them without restarting (#729, PR 2 of 2)** | In a **Direct** session open `/mcp`. Every row now has **Turn off** and **Reconnect**. Click **Turn off** on a server you can live without — it should go grey and read *turned off*, and the message must say it stays off **for this project**, NOT "for this session". **Then prove that claim, because it is the one a machine cannot**: close the session, start a new one in the same folder, open `/mcp` — the server must still be off. Turn it back on and confirm it reconnects. Next the add flow end to end: with a session running, run `claude mcp add selftest -- claude mcp serve` in a terminal, reopen `/mcp`, and the server should appear under **In your files, not loaded by this session** with a **Load it now** button — click it and the row should move up into the main list as *connected*, with no restart. Then **Reconnect all** and confirm it reports how many came back. **The judgement call:** with per-row Turn off and Reconnect on every row plus three buttons at the bottom, say whether the row now reads as cluttered — this is the most controls a row has ever carried. Finally, the case I could NOT measure: **try Turn off on a claude.ai connector row** (the laptop has them, this machine has none). Either it works, or the CLI refuses and the panel shows its reason — tell me which, because the manual currently promises neither. Clean up with `claude mcp remove selftest -s local` | ⛔ **STILL UNTESTED — the 2026-08-31 laptop trip did NOT cover this row.** Dan confirmed the panel LISTS every server (#729 PR-1); he did not turn one off, reconnect one, or use Load it now. This is now the **top laptop ask**, because the list being right is exactly what makes the actions worth trusting. **RELEASED in v0.8.6** (2026-08-30) — on `main` as #732 (`ce04bf4`). The hold that kept it off v0.8.5 is over; this is now installable and testable. Automated: the strict-boolean validator (the CLI treats an absent `enabled` as "disable" and answers success — measured), both IPC gates, the control-channel failure vocabulary, the toggle/reconnect wiring, the not-loaded Load-it-now button, and the Reconnect-all partial-failure count. 6579 tests. **What no test can do is prove the toggle survives a restart on your machine, or that it works on a real connector** — items 2 and the last one |
| **The MCP panel shows what the session REALLY has (#729, PR 1 of 2)** — ✅ **core claim PASSED, remainder still untested** | ✅ **2026-08-31, work laptop: the panel shows all the servers now.** The 3-vs-16 defect this arc was filed for is CONFIRMED FIXED on the machine that had it — do not re-run the count. **What is still unchecked on this row, all of it judgement or edge cases:** the scope headings (*Added at startup*, *Built into Claude Code*, *Set by your organisation*) reading sensibly to a human; a connector row showing *read-only* with **no Remove button at all** (not a greyed-out one); the ~2s *connecting…* window settling to *connected* on its own within ~5s without reopening the panel; the three fallbacks naming themselves (suspended card, Terminal-mode session); and Remove still working on a config-file server (`claude mcp add selftest -- claude mcp serve`, remove from the panel, confirm it goes). Original instructions follow. — **This is the row the work laptop exists for** — the machine with 16 servers where the panel showed 3. On it, open `/mcp` in a **Direct** session and count: the list should now match what the CLI's own `/mcp` picker shows, connectors and plugin servers included. Check the grouping reads sensibly with the new headings (*Added at startup*, *Built into Claude Code*, *Set by your organisation*) — **that wording is a guess at what those scopes mean to a human and only you can say whether it lands**. Then the read-only rule: a connector row should show the words *read-only* and **no Remove button at all** — confirm it is not a greyed-out button, because "not right now" and "not ever" are different sentences. Hover it for the explanation. Then the timing, which is the finding nobody predicted: **start a brand-new session and open `/mcp` within about two seconds**. Everything should say *connecting…*, and then settle to *connected* on its own within about five seconds **without you closing and reopening the panel** — judge whether the wait reads as working or as broken. Then the three fallbacks, each of which should name itself at the bottom of the panel: open `/mcp` on a **suspended** card (should say to start the session), and on a **Terminal**-mode session (should say Terminal mode can't be asked, and should NOT tell you to switch to a terminal you are already in). Finally confirm Remove still works on a server that IS in a config file — `claude mcp add selftest -- claude mcp serve`, remove it from the panel, confirm it goes | **RELEASED in v0.8.6** (2026-08-30) — on `main` as #730 (`ed70ef6`). ⚠️ **v0.8.5 did NOT contain it**, which is what cost Dan a laptop trip on 2026-08-29: v0.8.5 carries #723's footer EXPLAINING the short list rather than fixing it, so the released build showed the same three servers and always would. **Install v0.8.6 before testing this row.** Automated: the parser against the real captured cold AND warm payloads, the config join (including the two cases where a name match must be REFUSED so Remove cannot delete the wrong scope), all four `mcp:status` failure reasons, the second folder gate, the settle poll and its bound, and the read-only row. 6540 tests. **What no test can do is show you sixteen real servers** — the fixture has one. Item 1 is that check, and it needs the laptop. Your eye also judges the new scope headings and whether the *connecting…* window reads as patience or as failure |
| **Change a session's model with `/model` (#721)** | In a **Direct** session, type `/model` and press Enter. A picker should open listing the models Claude Code offers — Opus/Sonnet/Haiku and friends, each with its own description. Click **Haiku**: it should tick immediately and say so, with no restart and the conversation intact. Then send a prompt and confirm the reply actually comes from the model you picked — **this is the check no machine can make**, because only the real CLI proves `set_model` took effect rather than merely being acknowledged. Then the case that looks like a bug and is not: open `/model` on a **brand-new** card that has never replied. Nothing should be ticked, and a line should explain that Claude Code only reports its model once a session has answered. Judge whether that line reads as honest or as broken. Then switch a session to **Terminal** mode from **⋯** and type `/model` there — you must get the CLI's own picker, NOT this dialog. Finally check the near-misses reach the CLI untouched: `/model sonnet` and `/model-x` should both be sent as ordinary commands rather than opening anything | **RELEASED in v0.8.5** (2026-08-28). Automated: the correlator (timeouts, refusals, out-of-order replies, the nested request_id trap), the alias-vs-resolved tick matching, the unknown-model state, refusal wording, and the whole path end to end against the fake provider — 39 tests. **What no test covers is the real `claude` binary applying the switch**, which is item 1. Your eye also judges the not-known-yet wording and whether the picker's rows read clearly |
| **The MCP panel says what it does NOT show (#723)** | Open `/mcp` in any session and read the bottom of the panel. Below the buttons there should now be a line saying the list comes from your configuration files, that Claude Code also connects claude.ai account connectors and plugin-supplied servers which cannot be shown yet, and how to see the full list. **Judge the stacking, which is the one thing no test can**: the panel can now show up to four grey footnote blocks at once (health-check-unavailable, a click notice, this one, and the existing add/remove note) — say whether the bottom of the panel now reads as a wall of small print. Then check it in the empty case, which is what the bug was about: open `/mcp` on a folder with no configured servers and confirm you get BOTH "No MCP servers are configured in this session's files" AND the new line — an empty list with no explanation was what made 3-vs-16 look like data loss. Finally confirm the escape route is true for you: the line says to run `/mcp` in a terminal, and in a **Direct** session there isn't one, so check it does not tell you to use a Terminal tab that has nothing in it | **RELEASED in v0.8.5** (2026-08-28). This is a wording/disclosure change only; **the servers themselves are unchanged and your sessions were never missing any of them** — the panel was reading three config files, and connectors and plugin servers live in none. The real fix (reading the session's live list) needs #721. Automated: the note renders with rows, renders in the empty case, and stays away when there is no session — all three mutation-checked. Only your eye judges the small-print weight and whether the wording lands |
| **`/mcp` opens the MCP Manager (#632, PR 1 of 2)** | In a **Direct** session, type `/mcp` in the prompt box and press Enter. The panel should open and the draft should clear — before this, typing it did nothing at all (the CLI answers `/mcp` with a picker in its own terminal, and Direct mode has none). **The check that matters most is the status column**, because the bug review caught was that it could never work on Windows: run `claude mcp add selftest -- claude mcp serve`, reopen the panel, and confirm the row reads **connected** within a few seconds rather than sitting on *status unknown*. Then `claude mcp add broken -- no-such-binary` and confirm that one reads **not connecting** — both rows visible at once, the list appearing instantly and the states filling in after. Then the things only you can judge: switch a session to **Terminal** mode from **⋯** and type `/mcp` there — you must get the CLI's own picker, NOT this panel, because in Terminal mode the real picker works and can do things this pane deliberately cannot. Press Escape and confirm the cursor lands back in the prompt box. Pop a session out, type `/mcp` in it, and confirm the main window comes forward carrying the panel rather than your draft vanishing into nothing. Finally, if you have a repo with its own `.mcp.json`, open a session there and confirm those servers show as *waiting for your approval* rather than as broken. Clean up with `claude mcp remove selftest` / `claude mcp remove broken` | **RELEASED in v0.8.4** (2026-08-27). **Read-only: no add/remove yet (that is #714).** Automated: the three scope readers on real captured file shapes, the health parser on real captured CLI output, the folder gate, the two-call shape, approval-beats-health, secret redaction, and the CLI launch spec on both platform branches — four mutation-checked. **No automated test can prove the launch actually works on your machine, which is exactly how the blocker got in** — item 2 is that check. Your eye also judges whether the three scope headings read clearly and whether *waiting for your approval* explains itself |
| **Add, remove, Reconnect and Reset approvals in the MCP panel (#714, PR 2 of 2)** | Open `/mcp` in a session and click **Add server…**. Add a real one — name `selftest`, kind *A program on this machine*, command `claude`, arguments `mcp` and `serve` on separate lines — and confirm it appears in the list immediately, then reads **connected** when you reopen the panel. **This is the check no machine can make**: the whole family launches the CLI through a hardened Windows path that was rewritten twice in this item, and only a real run proves it. Next, add one with an **Environment variable** (`API_KEY` = anything) and confirm the row says *Carries: API_KEY* and never shows the value anywhere. Try a name with a space, and a name starting with `-`: both should be refused in the form, before anything runs. Try typing a `"` into any box — it should be refused with a sentence explaining why. Then click **Remove** on `selftest`, confirm it asks first, and confirm the row goes. Add a server while a **Terminal**-mode session is open and click **Reconnect**: `/mcp` should be typed into that terminal and the CLI's own picker should open. Do the same in a **Direct** session: nothing should be sent, and it should tell you to restart. Finally, in a repo with its own `.mcp.json`, confirm **Reset approvals** appears (it should be absent otherwise), asks first, and afterwards the session asks about the shared servers again | **RELEASED in v0.8.4** (2026-08-27). Automated: the argv builders against the real CLI's documented forms, validation on both sides, the folder gate on all six channels, the reconnect transport decision, the re-list-after-failure behaviour, and the Windows launcher end-to-end through a real `cmd.exe` shim including eleven recorded injection payloads. **What no test covers is the real `claude` binary on your machine** — item 1. Your eye also judges whether the form's field labels and the "put keys in Environment variables" line actually read clearly |
| **A session that didn't start is still in the Sessions list (#687)** | Start a new session on a folder you can make disappear — a USB drive, or a folder you rename right after opening the card. Open the card so it tries to start and fails: it shows **Session didn't start** with **Try again** and **Close**. Now look at the **Sessions list** — before this the card was simply not there. It should now have a row reading *not started*. Then work through what that buys, because the row is the whole point: click the row (the card comes to the front), press its `Ctrl+1`…`Ctrl+9` number, and **double-click another session's header to maximize it** — the workspace should fold around the not-started card properly instead of ignoring it, and un-maximizing should put everything back. Right-click the row: **Rename** must be visibly greyed out and do nothing when clicked, and the **Move to group** section must be absent entirely (make a group first, or you will not be able to tell an absent section from an empty one). Try to **drag** the row onto a group card — the cursor should show no-drop and nothing should happen. Double-click the row: no rename box may open. Pin it and Close it — both must work. **The one that matters most:** maximize another card so the not-started one collapses away, then bring it back from the list — it must reopen with its overlay and its **Try again** button intact. Finally plug the drive back in (or rename the folder back) and click **Try again**: the row should turn into an ordinary running session, and Rename and Move to group should both come back | **RELEASED in v0.8.4** (2026-08-27). Automated: the store join and its dedupe, the rail row's paint, the two withdrawn actions and both of their second doors (double-click, drag), and the layout plan (maximize now resolves) — each with an explicit pre-fix control, and five of them mutation-checked. **The collapse-and-reopen case is the one review caught**: giving the card a row made it collapsible for the first time, and without the reveal fix it would have been a row you could see, click and never open. Your eye judges whether *not started* reads clearly against *suspended* in the list |
| **REGRESSION CHECK ONLY — right-click menus and the rail edge still behave (#642)** | Nothing should look different in English; this row exists because the arithmetic under **every** pointer-anchored menu was rewritten to be direction-aware, and "identical in English" is the claim worth one human glance. Right-click a session row in the rail: the menu appears at the pointer, arrow keys walk it, Escape closes it and returns focus to the row. Shrink the window to about half your screen height and right-click a row low in the rail — the whole menu, including the bottom **Focus** section, must be on screen. Open it a few times and watch for a flicker in the window's top-left corner before it lands at the pointer (there should be none). Then drag the rail's right edge in and out: it must follow the pointer exactly and survive a restart | **v0.8.3.** Filed **Internal** in the changelog because no right-to-left language ships yet, so there is no new behaviour to see. Automated: 13 unit cases on the placement arithmetic, 5 on the rail-width mirror, 2 e2e (short-window squeeze + a forced `dir="rtl"` window), and the RTL case was mutation-checked (menu lands 726px off with the fix backed out) |
| **A prompt shows what you attached to it (#491)** | In a **Direct** session, paste a screenshot (`Ctrl+V` in the prompt box), type a question beside it and press Enter. The prompt in the conversation should carry a small grey *1 image attached* line where the chips used to be — judge its weight: it is meant to read as a footnote on the prompt, not as another status line. Then the case that used to vanish entirely: paste a screenshot and send it with **nothing typed**. You should get an entry reading just *1 image attached* — before this there was no entry at all and Claude's reply hung under thin air. Then mixed kinds: drag two source files and a picture on in one go, send, expect *1 image and 2 files attached* — **and say whether calling a `.md` a "file" and a `.png` an "image" reads naturally, that is the one wording choice here**. Then attach a picture to a prompt long enough to collapse (500+ characters): the marker must stay visible above the folded header, since that is the case where hiding it would hide the only evidence left. Finally send a few plain prompts and confirm nothing changed for them at all | **v0.8.3.** Automated: the derivation (counts, plurals, attachment-only turns, tool-returned images not miscounted), the wording in every singular/plural combination, survival of a collapsed prompt, and one full e2e round trip pasting a real PNG. Only your eye judges the weight of the line and the file/image wording |
| **Reviewed events are quiet, not faded (#268)** | Open the Events drawer (`Ctrl+E`) with a few sessions in it, and get at least one row into **Ready** (open a session that says *Done.* — that acknowledges it). Compare a Ready row with a live one: it should still read as the log tail rather than the to-do list, but every word on it should be plainly readable, including the small task label under the title and the word **Ready** itself. Do it on **daylight** and on **nordic**, the two where the old treatment measured worst. **This is a look Dan tuned by hand (0.82 opacity, 2026-07-26) and the fix changes it — say if the row no longer recedes enough, or recedes too much.** Three specific things to look at: the title is now the app's secondary grey instead of a faded white, and on a Ready row it is therefore the same colour as the task label under it (only the bold weight separates them); and the two things the old opacity used to fade that now sit at full strength are the 3px coloured session stripe down the left edge and the **Dismiss** chip in the bottom-right corner | **v0.8.3.** Automated: the row's fill and its ink are measured in all four themes by the drift test (4.7-14.2:1, was 3.6-3.7:1 on the label). Only your eye judges whether "already dealt with" still reads at a glance |
| **A stronger frame around session windows and group cards (#648)** | With two or more sessions on screen, look at the border around each session window in the grid and around each group card in the sessions rail — on **nordic** and on **daylight**. It should read as a real boundary against the workspace behind it, against the card's own body, and against the strip along its top (the session tabs in the grid, the group's name row in the rail). This is a bigger nudge than the last two — say if it now reads as a cage rather than an edge. Nothing else about the frame changed (same colour family, same one-edge-everywhere treatment); the question is only whether the new strength looks right or now shouts | **v0.8.3.** Automated: seven surface pairs x four themes in the drift test (nordic's tightest was 2.29:1, now 3.07:1; daylight's 2.66:1, now 3.08:1), plus the running-app check in the rail e2e, whose floor moved 1.55 -> 3. Only your eye judges "right, not shouty" |
| **One focus ring over the whole Events drawer (#577)** | With the drawer SHUT, `Tab` around until focus lands on the **EVENTS** tab on the right edge — it should get switchboard.ai's blue ring, the same one every other control gets, not the browser's default outline. Then open it with `Ctrl+E`: the drawer itself takes focus and now draws the ring around its own edge, where before it drew nothing at all. Then `Tab` through everything inside — the ✕, any **Restore layout** / **Update now** / **Got it** / **Not now** buttons on the notices, then each row's body and its **Dismiss**. Nothing along that walk should draw a differently-coloured outline | **v0.8.3.** Automated: a unit test walks every focusable the open drawer renders and fails any that is not on the ring rule read out of `tokens.css`. Only your eye confirms the ring reads the same on all four themes |
| **Docking a lone session back (#656)** | Make a persistent group in the sidebar (**Create a persistent group**) and start a session inside it. Pop that session out with **⤢** so it is alone in its own window, then dock it back with **⤡**. It must come back to its own spot, still listed under its group heading, still running (no **Resume** button), with no sliver of an empty pane left behind. Then repeat with the session sharing its window with a second one, so both branches of ⤡ are covered — the two should now behave identically | **v0.8.3.** **Note the ticket's premise was measured wrong:** the group was never actually lost on `main` (an unrelated ordering inside dockview was preventing it), so the group half of this is a guard rather than a fix. What genuinely changed is where the card lands. Automated: an e2e drives the round trip and reads the membership back out of the app |
| **A session started in a pop-out never steals a slot (#657)** | Split the grid so two sessions sit side by side (A left, B right). Pop **A** out, then press **＋** inside that window to start **C** in it. Now close **A**'s card from the sidebar (its **✕**), leaving C alone in the window with nobody's slot to inherit. Dock C back with **⤡**: it must arrive as a tab beside **B**, never alone in the left half A used to own, and no 1px sliver of a pane may be left behind. Then do the whole thing again but close the pop-out **from its title bar** instead of ⤡: same placement, except this time C comes back **suspended** with a **Resume** button, which is correct | **v0.8.3.** Automated: both paths are e2e'd, including the suspend. Your eye judges whether the layout looks settled rather than merely correct |
| **Expanding a stacked session comes back full size (#502)** | Get three sessions with two of them sharing one half of the grid. Stack the third and then one of the pair (`Ctrl+Shift+P` → **Stack session with the tabbed sessions**). Pop the remaining card out of that half with **⤢** — the half it leaves is now an empty placeholder. Now expand the stacked session back (**Expand session to its full card**): it must come back at full size in that half. Before this it came back about a pixel wide — in the layout, on the right window, and invisible | **v0.8.3.** Automated: the e2e MEASURES the width, because `toBeVisible()` passes at 1.33px and that is exactly how this class of bug hides |
| **Pop-ups speak the app's language (#471)** | Title bar → click the language chip to **pseudo**. Every label in the window turns into accented look-alikes wrapped in `⟦ ⟧`. Now make a session ask for permission while switchboard is NOT the front window (start something that needs approval, then click away). The desktop pop-up must be in `pseudo` too — the body, and the **Allow**/**Deny** buttons on it — *except* the command itself (`npm run build`) and the session's own name, which must stay exactly as they are. Switch back to **en** without restarting and trigger another one: it must be English immediately. If you have the phone push set up, check the message on the phone matches. Turn **🗣 announce** on and confirm the SPOKEN sentence follows the language too. Windows only: open the Action Center and confirm the pop-up is filed under **switchboard**, not *Electron* — then let a permission pop-up **time out** and press **Allow** on it from the Action Center: the verdict must still reach the session (Windows documents re-activating an expired toast as needing a registered ToastActivatorCLSID, which the installer does not write, so this is the one behaviour the AUMID line may NOT have fixed). Last: `npm run dev` now claims a brand-new notification identity, so confirm dev pop-ups still appear at all | **v0.8.3.** Automated: the locale switch, the ICU plural, the Allow/Deny labels and the untranslated command are all unit-pinned, and an e2e asserts the real bundled main process got its interpolator. What only you can see is a REAL OS toast rendering the accented text (no harness can read one) and the Action Center identity in a packaged build |
| **Turn boundaries you can scan for (#640)** | Open a session with a long conversation in it — several of your prompts, with replies and tool boxes between them. Above every prompt except the first there should now be a full-width rule with **NEW PROMPT** under it and a clear gap above. Scroll fast through the whole session and judge the actual ask: does your eye land on the boundaries without reading? Then switch through all four themes (title bar → theme) and check it reads the same on each — especially **daylight**, where the old line was effectively invisible. Last check, and the one that decides whether it is too loud: get a session to ask you for permission while you look at another one's feed, and confirm the boundary still reads as furniture next to the attention signal, not as competition for it | Superseded: owner hand-tested on v0.8.2 (2026-08-26) — improved but NOT "easily spotted"; follow-up #717 filed. Was: Not yet released — on main after #640. Automated: contrast is measured in all four themes by the drift test (5.20 / 6.00 / 15.57 / 8.76:1, was 1.30:1), and unit tests pin one boundary per turn and none above the first block. Only your eye judges "unmissable at a glance" and whether the **NEW PROMPT** caption earns its place or reads as clutter |
| **Claude's questions, answerable in the card (#563)** | In a **Direct** session ask for something genuinely ambiguous ("I want to add caching — ask me which approach before you start"). A panel appears above the prompt box with the question and clickable answers: round buttons = pick one, boxes = pick several, always an **Other** with a text field. Answer, hit **Send answer**, and Claude should carry on using what you chose — including free text typed into Other. Try the arrow keys/Space instead of the mouse. Half-answer it, go to **Changes**, come back — your ticks should still be there. Unfocused: the pop-up says "A question for you: …" and has **no Allow button**. Note a **Terminal**-mode session keeps its questions in the terminal by design | **v0.7.0.** The fake-provider path is e2e-covered; only the real CLI proves the model actually *reaches for* the tool, and only you can judge whether Other feels like a first-class answer |
| **Several questions arrive as tabs (#566)** | Get a Direct session to ask you **two or more** questions in one go ("ask me three separate questions before you start"). They should arrive as tabs across the top of the question panel, labelled with Claude's own short names, one question showing at a time. Check: every tab carries ✓ or ○ for answered/unanswered; with nothing answered **Send answer** is dead and the panel says "Still to answer: …" naming the ones still empty (it goes live at one answer since #567 — see the row below); **Left/Right** move between tabs and **Up/Down** stay inside the question you are on; half-answer it, go to **Changes** and come back — it should reopen on the question still missing an answer, with your ticks intact. Then ask ONE question and confirm there are no tabs at all | Not yet released — on main after #566. The fake-provider path is e2e-covered; only the real CLI proves a multi-question call happens naturally and that the headers read well as tab names |
| **Answer only SOME of Claude's questions (#567)** | Get a Direct session to ask you **two or more** questions at once, then answer only ONE of them. **Send answer** must go live at that first answer. Before you send, check you can see what you are leaving out: the unanswered tab goes **dashed and struck through**, the question itself (click its tab) says *"Not answered — will be sent as skipped"*, and the line beside the button reads *"Sending now skips: …"* rather than *"Still to answer: …"*. Send it, and watch what Claude does with the gap — it should treat that question as skipped (usually saying so and offering to ask again), never as though you answered it with silence. Then confirm the floor: with NOTHING answered, **Send answer** stays dead — a blank answer is what **Don't answer** is for | Not yet released — on main after #567. The CLI's reading of a partial answer is MEASURED (probe, 2026-08-19, findings §3a) and the partial round trip is e2e-covered against the fake; only the real CLI shows you how Claude phrases the gap, and only you can judge whether the skip warnings read as clear rather than as a telling-off |
| **Feeds restore at the tail (#555)** | Scroll to the end of a busy session, quit switchboard, relaunch. It should come back showing the END of the conversation, not the top. Then drag the panel to another part of the workspace — it should keep its position rather than jumping | **v0.7.0.** Was: every session restored scrolled to the top |
| **Find is a bar first (#557)** | Ctrl+F in a session — you should get the bar with a match count, and Enter / Shift+Enter walking the hits. The results list should stay shut behind its **▸** until you click it | **v0.7.0** |
| **Find in a RESUMED session (#496 + #495)** | Resume a session, send one new turn, then Ctrl+F for something that appears both before and after the resume. Post-resume hits should jump normally; only the older hydrated ones stay unjumpable, and the notice should be about that one hit rather than over the whole session | **v0.7.0.** The ticket's premise was measured wrong first — see PROGRESS |
| **File menu: Open File / Exit (#569)** | A **File** menu at the top left. **Open File…** browses — it should start in the folder of the session you are looking at, and after that wherever you last browsed. The file opens in a document tab BESIDE that session, in its dock section, never inside the session’s own tab strip. Try it with two sessions docked side by side and the SECOND one focused: the document must appear next to that one. Also try it right after typing a prompt (composer focused) — it must still open | **v0.8.0.** |
| **Ctrl+O, and the CLI’s own ctrl+o (#569)** | **Ctrl+O** anywhere in the app opens the file browser — including while typing a prompt. **But click into a session’s Terminal tab and press Ctrl+O there: it must reach Claude Code** (its transcript view), NOT open our dialog. That is the P7 line, and it is the one check no automated test in this repo can make | **TESTED (app half) 2026-08-20:** Dan pressed Ctrl+O in a (Direct) session — dialog opened to that session's folder, as designed. **Terminal-pane half MOOT** while terminals are unused (owner, 2026-08-20); revive with PTY use |
| **No empty window after a restart (#494)** | Open a file in the viewer (the ↗ beside a changed file), pop it into its own window with the ↗ in the document header, then **quit switchboard while that window is still open** and start it again. You should get exactly ONE window back — the document viewer is not restored (by design, for now) and neither is the empty window it was in. Then do it again with a **session** popped out into that same window: that window MUST come back, with its session in it | **0.8.1.** Was: an empty second window could be left on screen, depending on machine timing |
| **Popped-out window comes forward (#571)** | Pop a session out, put another window in front of it, then click that session's row in the sidebar — its window should come to the front. Then click the main switchboard window: your popped-out windows must NOT come forward with it | **v0.8.0** |
| **Changes tab remembers your file (#562)** | Open **Changes** on a session, pick a file, scroll into the diff. Switch to **Session**, switch back — same file, same line. Then commit or discard that file and come back: the tab should open CLEAN, not on a blank comparison | **v0.8.0** |
| **Documents keep their place (#562)** | Open two files in the viewer (the ↗ beside a changed file). Read halfway down one, click the other tab, click back — you should still be halfway down | **v0.8.0** |
| **[UNRESOLVED] Terminal scrollback across a panel move (#562)** | Needs a session with real scrollback: run something long in the **Terminal**, scroll UP into the output, then click that card’s row in the sidebar (with two sessions docked side by side) and see whether the terminal keeps its position. The automated harness could not fill one screen, so this is the one panel measured only halfway | **MOOT** while terminals are unused (owner, 2026-08-20). If PTY sessions return: if it jumps to the bottom it needs the same fix the document viewer got |
| **Right-click menu fits a short window (train fix)** | Shrink the switchboard window to a short height (or use a small screen), right-click a session row near the bottom — the menu should flip above the pointer or clamp to the edge, with every item reachable; a menu taller than the window scrolls | Not yet released — lands with the 2026-08-20 train |
| **A visible ✕ on the Events drawer (#556)** | Open the drawer (tab, `Ctrl+E`, or the palette). There should be a **✕** at the top right of the drawer, beside the word EVENTS — the thing you went hunting for. Click it: the drawer closes and your cursor goes back where it was, exactly as `Esc` does. Then open it again and press **Tab** once — focus should land on that ✕ | Not yet released — on main after #556 |
| **Close all documents + honest tab ✕ (#543)** | Open four or five files in the viewer, then `Ctrl+Shift+P` → **Close all documents (keeps popped-out ones)**. Every document tab should vanish at once, with no confirmation, and your sessions and their **Changes** tabs must be untouched. Then repeat with one document popped out to another monitor (⤢ in its header): that window must survive, and if it is the ONLY document open the palette entry should be greyed out saying "No documents to close". Also hover a document tab's ✕ — the tooltip must read **Close document**, not "Close (ends the session)", and clicking it must not ask you to confirm | Not yet released — on main after #543. Automated tests cover the docked close-all and the three tooltip strings; the popped-out window is unit-pinned only |
| **An attached file survives a tab switch (#546)** | In a **Direct** session, paste a screenshot into the prompt box and type NOTHING. Switch that card to **Terminal**, then back to **Session** — the chip must still be there and the send button still lit. Repeat with a file dragged in from Explorer, and with words typed beside it. Then quit switchboard with a file attached and relaunch: your **words** come back, the chip does **not**, and a line under the box should read "Not restored: <the file name>…". That last part is the point — it must never just empty itself | Not yet released — on main after #546. Automated tests cover the remount and the relaunch notice against the fake provider; only a hand-test proves a real pasted screenshot behaves |
| **Permission-mode hover copy (#534)** | Hover the 🛡 chip in the **title bar**, the 🛡 button **under the prompt box**, and the small mode marker in a **card's header** (visible once a session is on anything but `ask`). Each should pop a description of the mode plus a line saying what that particular control does — and the three must agree. Cycle to **full-auto** and read it: it must say `bypassPermissions` / `--dangerously-skip-permissions`, not imply guardrails. Judge whether the copy reads like something a person would want on hover, or is too long | Not yet released — on main after #534. Unit + component tests already pin that all three surfaces carry the same text and that the full-auto wording matches the CLI; only you can judge readability and hover feel |
| **`ask` mode actually asks (#587)** | Start a session at **ask** and open its **Terminal** tab. The CLI's status line should say **`⏸ manual mode on`** — before this fix a Pro/Max account on CLI 2.1.228+ would have shown **`⏵⏵ auto mode on`** instead, meaning a classifier was reviewing actions rather than you. Then check nothing got taken away: **Shift+Tab** inside that terminal must still cycle to auto. Repeat on a **Direct** session (there is no status line there — the check is only that it still starts, resumes and holds normally). Watch for whether `ask` now prompts noticeably more than it used to; a few more is the fix working, a flood is worth a ticket | Not yet released — on main after #587. No automated test can read the CLI's own status line, so this row is the only proof the flag landed where it counts |
| **Maximizing a suspended session (#216)** | Open two sessions. Pop one out into its own window, then close that window with its **X** — the card comes home showing **Session suspended**. It should now have a proper header (name, colour dot/badge, the word *suspended*). **Double-click that header**: the workspace should fill with that one card and the other session should fold into the Collapsed strip. Double-click again to put it back. Through all of it the card must stay suspended — the **Resume** button still there, nothing restarted | Not yet released — on main after #216 |
| **A pinned session stops scrolling away (#295)** | Open enough sessions that the Sessions list has to scroll (or shrink the window until it does). Pin one near the BOTTOM — it jumps to the top, as before. Now scroll the list to the end: the pinned row must stay parked at the top while everything else slides underneath it. Pin a second one and check the two park as a pair rather than overlapping. Tab through the list from the top and confirm the highlight never lands on a row hidden behind the pinned block. Then the judgement call: with GROUPS, a pinned session only stays put while its group card is on screen — scroll past the whole group and it goes with it. Decide whether that reads as sensible or as the promise half-kept | **v0.8.3.** Automated: an overflowing rail, scrolled to its end, is e2e-checked to still show the pinned row, and the keyboard guard with it. Only your eye judges whether a floating row without its group header reads right, and whether it needs a shadow or a divider to separate it from the rows sliding under |
| **A dead card that finally has a header (#606)** | Get a card into the **Session didn't start** state — easiest is to open a session on a folder, quit, rename that folder, and relaunch. That card should now carry a header like every other card: the session's name, its colour and badge, and the words **not started**. Before this it had no header at all. Check it has no buttons of its own (**Try again** and **Close** stay in the panel below), and that double-clicking the header does not start anything. Worth knowing, and worth your opinion: that card still has NO ROW in the Sessions list, because the session was never registered — so double-clicking its header cannot maximize it the way a live or suspended card's can. Tell me if the missing row bothers you more than the missing maximize | **v0.8.3.** Automated: unit + e2e cover the header, the identity, the pill word, the absence of controls, the maximize round trip through the store, and that nothing respawns. Only your eye judges whether a card with nothing running in it wearing a header reads as informative or as a card pretending to be alive |
| **The app says when it moved a card's conversation (#539)** | On your **next launch after this ships**, open the Events drawer (the tab on the right edge, `Ctrl+E`). Expect a notice saying **Switchboard.ai-2** and **Switchboard.ai** were both pointing at one conversation, that **Switchboard.ai** kept it and Switchboard.ai-2 starts a new one — plus, if the sweep reattached an orphaned card, a second notice naming that card. Then open both: **Switchboard.ai** must be in the conversation it was already in, and **Switchboard.ai-2** must be EMPTY (a new conversation) — never the same transcript as its twin, and never some unrelated session's history. Now the part that matters most: quit **without** pressing Got it and relaunch — the notices must still be there. Press **Got it** on each, relaunch again: gone for good, and both cards land where they were left | Not yet released — on main after #539. Automated tests cover the policy, the persistence, the dismissal and the notice UI; only your real workspace has the actual duplicate pair |
| **Reorder the Sessions list (#559)** | Open three sessions and drag one onto the top half of another — a thin line should show where it lands, and letting go should put it there. Quit and relaunch: the order must come back exactly as you left it. Then right-click a row → **Move up** / **Move down** under *Order in this group*, and try **`Ctrl+Alt+↑`** / **`Ctrl+Alt+↓`** while you are in a session. Now **pin** the top session and try to drag another above it — it should stop just underneath, and **Move up** should go grey there. Dragging onto a *different* group must still just move it there, at the bottom | Not yet released — on main after #559. Automated: the drag + relaunch round-trip and the menu path are e2e; only your eye judges whether the insertion line reads right mid-drag |
| **Find works on a Terminal you never opened (#517)** | Start a fresh session in **Terminal** mode and let it print something, WITHOUT clicking the Terminal tab. From the Session tab press **Ctrl+F** and search for a word you know it printed: you should get a real count in the **Terminal (scrollback only)** group, plus a line saying to open the Terminal tab if you want to scroll to them. Now open the Terminal tab and search again — the same matches should highlight in place and step with Enter. Also check a **Direct** session still shows no Terminal group at all | Not yet released — on main after #517 |
| **The Events drawer (0.6.0)** | The 220px column is gone — look for the slim badge tab on the right edge; it shows the attention count, tinted by the hottest event. Click it, or the hotkey, or the palette; the drawer overlays the grid. Check the update/reconnect/incident notices appear inside it, Esc closes and gives focus back, and the status bar now carries a queue count | Shipped in v0.6.0 |
| **Quiet hours (0.6.0)** | Palette → quiet hours (or the About button); set a window covering now; a session needing you must NOT sound/speak/toast — but a webhook still fires | Shipped in v0.6.0 |
| Actionable toasts (Allow/Deny on Windows notification) | Unfocus the app, make a session hit a permission ask; buttons on the toast must work without focusing the app; answering in-app withdraws the toast | Dan got NO toast at all 2026-08-15 — possibly OS focus assist / notification settings during dev; INVESTIGATE before re-test (installed build should have AppUserModelId) |
| Notify-when-done (⋯ menu tick-box) | Tick one session, task it, alt-tab; toast on finish for that session only; survives restart | Same no-toast caveat as above |
| Phone push (ntfy/Pushover) + webhook | Palette → "phone push" → paste an ntfy topic → Send test; then a real needs-permission while unfocused | Deferred by Dan; needs 2-min setup |
| Themes under the new UI | High-contrast + daylight, re-run find/copy/chips/jump-latest checks | Deferred by Dan |
| Sounds 🔊 / TTS announce 🗣 | Title-bar toggles; per-session cue from ⋯ menu; announce speaks label while unfocused | Shipped 0.5.0, not yet exercised |
| Webhook payload | POST target you own; fires regardless of focus, includes finished turns | Never exercised |
| Service-health incident path | Needs a real/mocked Anthropic incident — dot, Events entry, corroboration banner | Hard to test on demand; low priority |

## Re-test after a fix ships

| What | Shipped in | Re-test |
|---|---|---|
| **Prompt box takes clicks and paste but not typing, until you switch apps (#909)** | **RELEASED in v0.8.97** (2026-09-21) | **Your 2026-09-21 report.** The cause was the "close this session?" question. **(1)** THE MAIN ONE: click a session tab's **✕**, press **Cancel** (or Escape), click in that session's prompt box and type. The letters must appear straight away, with no switching apps. Before the fix, nothing appeared, every time. **(2)** Same with the sessions list's close, and with **Close all sessions** from the palette: cancel it, then type. **(3)** A POPOUT: pop a session out (⤢), click **✕** on its tab inside the popout, cancel, then click into the popout's prompt and type. Say which window you end up in and whether typing works: this case was never measured. **(4)** Nothing flickers or jumps to another app when the question closes (a very brief flicker of the window behind is possible; say if you see it). **(5)** If typing ever dies again, note what you did just before; the log will say `refocus after dialog: focus did not come back` if the repair was refused. Automated: reproduced and fixed with real mouse and keyboard input on this machine (probe in `spike/probes/909/`, reverting the fix brings the bug back), unit tests on the repair and a guard that no new pop-up skips it. CI cannot run real OS input, so your hand test is the real check |
| **Report a problem: Send report looked disabled and never closed the dialog (#896)** | **RELEASED in v0.8.94** (2026-09-21) — installable now | **Your 2026-09-21 report.** Open **Help ▸ Report a problem…**. **(1)** With no subject, **Send report** is a faded green (hover it: it says to give it a subject first). **(2)** Type a subject: it turns solid green, clearly different from **Cancel**. **(3)** Pick **Just build the zip** and press **Send report**: the folder with the zip opens and the dialog CLOSES by itself. **(4)** Pick **Create a GitHub issue** and send: the new issue opens in your browser and the dialog closes (check the issue on the repo, then close it). **(5)** Unplug the network (or turn Wi-Fi off) and send to GitHub: the dialog STAYS open, the reason sits next to the button, and your subject and description are still there. **(6)** The form must not scroll sideways | Automated: unit tests for close-on-success, stay-on-failure, reason placement, the button colours and the unwritten-zip message; e2e measures the real computed fill and the sideways overflow (both proven red on the old code). The real GitHub post and the browser opening need your machine |
| **A card vanishes when you click another session (#813)** | **RELEASED in v0.8.90** (2026-09-16; PR #819, `dec5181`) — installable now | **Your 2026-09-15 report, and it was never a close:** a maximize left on from days earlier (on *SOLOServer-buiuld-pipeline-ignores*, by then a background tab) folded every WORKING card you clicked away from into the Collapsed strip — finished sessions were spared because they still count as needing you. **Right now, before any update, look at the layout chip: it should read `▦ Grid · maximized` — that is the proof.** Undoing it is safe again once **#818** ships: a maximize that is no longer filling the workspace is simply re-maximized by the gesture, and a real undo now puts back only the parts of the arrangement you have not rearranged since. (Before #818 it replayed the layout from the day it was taken.) After installing the build with the fix: **(1)** with a session WORKING, click another session in the Sessions list — the one you left stays. **(2)** Same, clicking another card's tab in the workspace. **(3)** Double-click a header to maximize, visit a folded session from the strip, click back — the visited one stays; double-click again restores the prior arrangement. **(4)** Focus layout still folds the card you leave — that is Focus, not the bug. **(5)** `switchboard.log` has `[ladder] moving session-…: from → to (why)` for every fold/hide/tab, and `[dock] session-… closed` for a close | ⚠️ **UNTESTED — released in v0.8.90 (2026-09-16); ready to run.** Automated: unit tests RED→GREEN on the rule and the breadcrumb; e2e RED→GREEN on a tab click and a rail click; mutation 12/12. Not a #708 regression: the rule dates from E9-07 (2026-08-04). Only your laptop has the real stale maximize |
| **Clear Session leaves the old text until a SECOND clear (#748)** | **RELEASED in v0.8.8** (2026-09-08) — installable now | **This is your 2026-09-03 report. The one case that matters most is the one you never tried: clear a session you have JUST OPENED.** That was guaranteed broken and is the actual bug — Claude Code does not announce a conversation's identity until the session has replied once, so switchboard was comparing the new identity against nothing. **(1)** Start a fresh Direct session, and before typing anything else use **⋯ → Clear conversation**. The text must go on the FIRST clear, with the *"Conversation cleared — context starts fresh"* divider. **(2)** Same again but typed: `/clear` in the prompt box, on a session that has not replied yet. **(3)** The worst case, which failed every single time: **resume** a session with a screenful of history (quit and relaunch, or reopen a card), then clear it immediately — the replayed history must go. **(4)** The case that always worked, to prove nothing regressed: talk to a session for a few turns, then clear — one wipe, one divider, not two. **(5)** `/compact` on a session with some history — the conversation must **stay on screen**; measured not to trigger a wipe, and this is the check that it stayed that way. **(6)** Clear while Claude is mid-reply and confirm the half-written answer goes with everything else | ⚠️ **UNTESTED — released in v0.8.8 (2026-09-09); ready to run.** Root cause MEASURED against the real CLI, and **the ticket's own explanation was wrong** — it blamed a late announcement; the announcement is prompt, there was simply nothing to compare it against. Automated: 14 tests, **all 14 mutations caught**, including the real message sequence and its measured ordering. Also measured, because keying the wipe on the wrong signal would have been a worse bug: **`/compact` does not emit the reset and keeps its id**; a **`--resume` spawn emits nothing at all** (so a resumed card's replayed history is safe); and **plan-mode exit** emits nothing and does not rotate the id. **What no test can do is prove it on your machine with the real CLI** — items 1, 2 and 3 |
| Find highlight in session view | **v0.6.0** | PARTLY VERIFIED — marks work where jumping works; retest with the #557 branch |
| Find on ANY resumed session | **MERGED to main** (PR #561 — #557 #496 #495) | Ctrl+F on a session you have restarted AND then prompted again: the bar alone should carry it — count, Enter/Shift+Enter, Esc — and **the results list must never appear unless you press ▸**. Hits from before the restart may say "earlier than the conversation on screen"; hits since it must jump. **Note the premise was measured wrong in the ticket:** an idle resumed session was always fine; it is the new turn on top that broke it |
| New session from popout | **v0.6.0** | Affordance exists, tab lands beside current card in the popout |
| Conversations sitting at the TOP instead of the tail (#555) | **MERGED to main** (PR #560) | Click every session in the sidebar in turn, especially with two cards side by side: each must show its NEWEST message, and stay there when you click its own row again. Also: scroll up, switch away, come back — you should be where you were reading, not at the bottom. The restart was never the cause — clicking a card was |
| Stale **"N need you"** counters after dismissing (#621) | **MERGED to main** (PR for #621) | Let two or three sessions raise events (a permission ask, a finished turn). Read the four counts: the Events **tab**'s number, the **"N need you"** at the right of the strip across the top, and each **group header** + the **footer** of the Sessions list. Open the drawer (`Ctrl+E`) and **✕** one entry — every count must drop by one *the moment you click*, and at zero the words should vanish entirely (the group headers go back to **calm**). Then check the deliberate half: a session that is genuinely still holding a permission must KEEP its coloured, bold row in the Sessions list — dismissing is meant to clear the count, not to pretend the session is unblocked. Finally click a **Done.** entry instead of dismissing it: opening the session relaxes it to **Ready**, and that should also drop the counts | Automated: unit tests pin the derivation (dismiss, acknowledge, `none`-silenced sessions, per-group counting) and an e2e drives a real hook → real ✕ → all three rail/strip readouts. Only your eye judges whether a still-loud row next to a zero count reads right — that is the boundary with #528 |
| Dock-back slot (#558) | **FIXED, in Dan's PR queue** (PR for #558) | Your own four-step repro, with the grid split into two side-by-side sessions (A left, B right) so a wrong slot has somewhere to be wrong: **(1)** pop A out and click ⤡ — A returns to the LEFT half, not into B's tabs. **(2)** pop A out again, click ＋ inside that window to start C in it. **(3)** dock A back — A goes to the left half, alone. **(4)** dock C back — C arrives as a TAB beside A rather than taking the left half, and A is still there. Then the same in reverse (dock C first, then A): C lands beside B, A gets its left half back. Nothing should show a **Resume** button at any point, and no 1px sliver of a pane should be left in the layout. The full sequence in both orders is in `e2e/popout-dock-back.spec.ts` — the machine already checks placement and aliveness, so what is worth your eyes is whether the result FEELS right |

## Tested and passing (2026-09-21 pass, v0.8.95)

- **The multi-question panel walks itself, Next question, and the "skipped" word (#733)** — Dan, live, answering a real four-question call: a pick-one answer jumped straight to the next tab; ticking all four boxes stayed put and **Next question** moved on; typing into **Other** kept the tab and arrived as "Test."; the blank fourth question went back as skipped, and its tab read *skipped* in words with no strikethrough. Verdict: *"that looked great."* Not exercised by hand: keyboard-only answering (automated, incl. real-window focus in e2e).

## Tested and passing (2026-08-31 pass, v0.8.6 — work laptop)

- **The MCP panel shows every server the session really has — #729 PR 1 of 2,
  verified on the machine that had the bug.** The work laptop showed 3 of 16
  servers; it now shows them all. This is the defect the whole MCP arc was filed
  for, and it is closed out on real hardware rather than a one-server fixture.
  The rest of that row (scope-heading wording, read-only rows, the *connecting…*
  settle window, the fallbacks, Remove) was **not** exercised and stays in
  Untested above.

## Tested and passing (2026-08-16 pass, v0.6.0)

- Update installs **and restarts itself** — #525 verified in the wild
- Right-click menus (composer cut/copy/paste, feed copy) — "seems to be working now"
- Links in replies open the browser — "working, thank you"
- Documents: a tab per file, no pin; Ctrl+F in the viewer — both "working well"
- Side-by-side diff toggle — "working well"
- Composer draft survives tab-switch / popout / restart — "this is working"
- New session from a popped-out window (creation half) — "all worked fine"
- Resume-link repair — "appears to be working now" (orphaned cards reattached)

## Tested and passing (2026-08-15 pass, v0.5.0)

- Resume after restart — conversation replays (one caveat: watch for #484 orphan cards)
- Live document re-render — "looks great"; scroll held, delete-strip works
- Paste image into composer → chip → model sees it (verified end-to-end with a real screenshot)
- Drag file into composer → chip ("seems to work fine")
- Copy buttons (code header, Bash IN/OUT, popped-out card) — "works very well"
- ↓ Jump to latest — working
- Attention queue / Ctrl+Space walk — "seems okay"
- Trust chip greyed on Direct + no pre-write — "looks good"
- Status bar (health dot, usage, count, CLI version) — "seems okay"
- New session correctly lands in main app while a popout is focused (placement rule holds)

## Found-a-bug log (dogfood sessions)

| Date | Report | Ticket |
|---|---|---|
| 2026-08-14 | Blank session view on resume (was #395, unreleased at the time) + resume-link destruction | #484 |
| 2026-08-14 | Find jump paints no highlight | #520 |
| 2026-08-14 | No discoverable file-open; wants Files tab | #521 |
| 2026-08-14 | Update installs but never restarts the app | #525 |
| 2026-08-14 | No right-click context menus anywhere | #526 |
| 2026-08-14 | Links in replies dead on click | #527 |
| 2026-08-14 | Attention coloring not intuitive (design sitting) | #528 |
| 2026-08-14 | Focused-card border not prevalent enough (design sitting) | #529 |
| 2026-08-15 | Popping out a card deletes typed prompt text | #485 (widened) |
| 2026-08-15 | Viewer pin model rejected — always-new-tab wanted | #530 |
| 2026-08-15 | No new-session path from a popped-out window | #531 |
| 2026-08-15 | Diff stuck inline, no side-by-side option | #532 |
| 2026-08-15 | Ctrl+F dead in the document viewer | #533 |
| 2026-08-15 | Find goes all-list-only on a busy session | #496 (repro added) |
| 2026-08-16 | Every session restores scrolled to the TOP, never the tail | #555 |
| 2026-08-16 | Events drawer has no visible close button (tab only) | #556 |
| 2026-08-16 | Find auto-opens the results list; wants bar-only interaction | #557 |
| 2026-08-16 | Find refuses to jump on an IDLE resumed session (worked after new turns) | #495 (evidence added, priority up) |
| 2026-08-16 | Popout-born session docks back into the WRONG grid slot | #558 |
| 2026-08-16 | Wants drag-to-reorder sessions within a rail group | #559 |
| 2026-08-16 | "N need you" counters stay stale after dismissing events (Events header + group headers) | #621 |
| 2026-08-16 | Streaming reply shows raw markdown until the turn ends; wants progressive rendering | #635 |
| 2026-08-16 | Turn boundary above each user prompt too subtle; wants it unmissable | #640 |
| 2026-08-22 | Background-task notifications render as NEW PROMPT with raw XML (BrainHarbor session) | #704 |
| 2026-08-24 | Switching away from a WORKING session sometimes closes another session's dock panel (rail unaffected; owner can reproduce) | #708 — FIXED in v0.8.2 (#558 dock-back rework), owner-verified same day |
| 2026-08-26 | Composer typing lags — keystrokes buffer then appear in bursts (severe on laptop, mild on desktop) | #716 |
| 2026-08-26 | Turn boundary (#640 fix) still not easily spotted — wants round 2 | #717 |
| 2026-08-26 | Busy spinner in the sessions rail too quiet; wants marquee-style working treatment (6 options filed) | #718 |
| 2026-08-26 | switchboard pegged laptop CPU → forced reboot; 26h uptime, log dead mid-session, 4.6x busier minimized; forensic report + 5 watcher bugs | #719 |
| 2026-08-29 | Stacked session cannot be dragged into a full-height column — only above/below offered, no root-edge split | #731 |
| 2026-08-30 | Multi-question panel: wants radio answers to auto-advance to the next tab (checkboxes stay); the #567 skip strikethrough on tab text read as a rendering glitch. **Refined 2026-09-19:** tabbed panels also get an explicit button row — Next question · Send answer · Don't answer | #733 |
| 2026-09-03 | Switching models updates the footer model text only on Claude's next reply, not immediately (owner-confirmed; tracks the transcript cost field, not the switch); dialog wants select-then-OK with Cancel reverting | #746 |
| 2026-09-03 | Wants the footer model name clickable — one-click quick-switch popup menu, no OK step | #747 |
| 2026-09-03 | /clear (menu or typed) sometimes leaves old session text on screen; a second /clear wipes it — intermittent | #748 — **FIXED, moved to Re-test.** Not intermittent: it failed on any session that had not replied yet, and every resumed card |
| 2026-09-05 | Wants a setting: when a session needs attention, pop its window to the forefront (the DESIGN.md focus-stealing policy, for the future settings screen) | #756 |
| 2026-09-05 | Wants a small identifying icon beside each feed tool-block name (agent, question, read, shell, …) — spot the block type by shape, not by reading | #757 |
| 2026-09-05 | Wants AI-generated task labels that follow the session — app runs Claude over the transcript and refreshes the label as work drifts (evolves P2-E7-06) | #758 |
| 2026-09-15 | Clicking another rail session closes the WORKING session's card (survives in the rail; click restores). Repro'd across multiple sessions, v0.8.8 laptop, days of uptime — #708's family back; first read blamed prompt submit, owner then pinned it to the rail click (and a card click; a collapse, WORKING sessions only) | #813 — FIXED, PR #819 merged, NOT released → RE-TEST. Cause: a days-old held maximize in `ui.layoutMode`, not #708's family |
| 2026-09-15 | Wants Help-menu one-click diagnostic bundle: zip logs + workspace.json + version info, reveal in Explorer, and hand to email pre-addressed to the owner — born from hand-collecting #813's logs | #815 |
| 2026-09-16 | CPU pegging RECURRED on the laptop — 100% processor ~2h after updating to the latest build, whole system unusable (mouse barely moved), app closed with difficulty. No logs captured (#815 would have). Happened WITH the bugs-1+3 (v0.8.7) and bug-2 (v0.8.8) fixes present | #719 — reactivated in the queue; remaining scope bugs 4+5 + instrumentation. **THIRD occurrence 2026-09-17 (second that day; restart of the app clears it)** — 3× in under 2 days, all on v0.8.90, after 7 clean days on v0.8.8; owner directive: FIX THIS NEXT. Version correlation + rollback stopgap noted on the ticket |
| 2026-09-19 | Main window forgets its monitor after display sleep — comes back on the primary while every other app returns to its old monitor. Popouts have rescue machinery (E8-06); the main window never got any | #864 |
| 2026-09-19 | Owner decision: remove the Terminal tab + ⋯-menu transport switch (UI only, code stays); migrate persisted PTY sessions to Direct | #873 |
| 2026-09-20 | **v0.8.92, dogfooding:** the AI task label shows on the card header but the SESSIONS LIST row shows no label. **Diagnosed — already fixed by #877, merging.** Not a data bug: the row and the header read the same `taskLabel`, so a genuinely blank line is impossible there. v0.8.92's row renders `p.needsYou ? t(p.labelKey) : (s.taskLabel ?? …)` — it substitutes the STATE/ASK for the label whenever the session needs you, and `done` (finished, unreviewed) is in that set deliberately, which is the state a session sits in the moment it stops typing. So the row was reading *Finished — review changes* rather than the label. The `aria-label` dropped it too (`!p.needsYou && s.taskLabel`), so it was lost to a screen reader as well. #877 moves the state beside the NAME and gives the label its own lines, and it was filed BEFORE this report — the owner found by hand the defect the layout change was already fixing. | #877 |
| 2026-09-21 | **v0.8.92:** Help ▸ Report a problem… — **Send report** draws as a black outline with black text (looks disabled), and pressing it leaves the dialog open. Diagnosed: the button used a session-only colour that is undefined at the root, and the result line sat off screen below the form. Fixed; a successful send now closes the dialog | #896 |
